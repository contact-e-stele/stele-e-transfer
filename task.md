# Stele App — Task Board

> Session-Scratchpad für aktuell offene Arbeit. Feature-Historie/Roadmap: `P-UEBERSICHT.md`.
> Architektur-Details: `docs/ARCHITECTURE.md`.

## NOCH OFFEN
-8. **Preis-Fundament Teil 3B (2026-09-13, Draft #90, wartet auf manuelle Freigabe):** "Verkaufszahlen je Varianten-SKU" — reines Lesen, keine DB-Migration. Voraussetzung (Teil 3/PR #89 gemergt, v1.11) verifiziert.
   - **Zentraler Befund:** die Bestellpositionen liegen **NICHT in der DB** — es gibt nur 5 Tabellen, `order_notes` speichert pro Bestellung nur Zusatzinfos (Tracking, Rechnung, manueller EK) und keine Positionen. Die Verkäufe je SKU sind deshalb **nur über die eBay Sell Fulfillment API** zu bekommen (`getAllOrders()` → `lineItems[].sku`). Dieser API-Aufruf ist zwingend nötig und rein lesend — wie im Auftrag verlangt ausdrücklich benannt.
   - **eBay-Positionsbetrag ergänzt** (`ebay.ts`): `lineItemCost` wurde bisher nicht durchgereicht. Ohne ihn ist der Umsatz einer einzelnen Varianten-SKU bei Mehrpositionen-Bestellungen nicht bestimmbar (`order.total` ist der Gesamtbetrag). Rein additiv.
   - **Aggregation als reine, getestete Funktion** (`src/api/variant-sales.ts`, 10 Tests): Verkäufe gesamt + letzte 90 Tage getrennt, Umsatz, Gewinn je Verkauf nach der zentralen Gebührenformel. `findProductForSku` aus `index.ts` nach `src/api/order-matching.ts` extrahiert (reiner Extract), damit Bericht und Bestellansicht dieselbe Zuordnung nutzen.
   - **Bericht** `scripts/export-variant-sales.ts`: je Variante SKU/Name/EK/heutiger Preis/Zielpreis aus Teil 3/Verkäufe gesamt/90T/Umsatz/Gewinn je Verkauf/`Auswirkung_bei_Umsetzung` = (Zielpreis − heutiger Preis) × Verkäufe 90T; je Produkt Summenzeile mit Gesamtauswirkung, **Ankeranteil der Verkäufe** und **Anzahl Varianten ohne Verkauf**. Varianten ohne Bestellung werden als `OHNE VERKAUF` gekennzeichnet, nicht weggefiltert.
   - **Abgleich (#5):** jede Bestellposition wird gezählt und entweder zugeordnet oder mit Grund ausgewiesen (4 Gründe unterschieden); der Bericht prüft selbst, ob zugeordnet + nicht zugeordnet = gesamt aufgeht. Nichts wird stillschweigend verworfen.
   - **P-49 (#6):** fehlt der EK, steht "nicht berechenbar" — nie 0. Durch Test abgesichert.
   - **Zwei echte Fehler gefunden, weil ich den Bericht selbst ausgeführt habe** (gegen eine lokale Test-DB mit gemockten Bestellungen): (a) die Aggregation nahm den **Produkt-EK statt des Varianten-EK** — bei stele-110 (EKs 2,15–7,69€) wäre der Gewinn je Verkauf für fast jede Variante falsch gewesen; (b) die Mengenspalte der nicht zugeordneten Positionen zeigte `undefined`. Beides behoben, (a) durch Regressionstest abgesichert.
   - **Selbst ausgeführt**, nicht nur behauptet: kompletter Bericht end-to-end gegen Test-DB — Gewinn je Variante korrekt (Rot 3,15€ / Blau 5,85€ / Schwarz 8,69€), 90-Tage-Trennung korrekt, Ankeranteil 60%, 3 Varianten ohne Verkauf, Abgleich geht auf. **Gegen die ECHTE DB kann ich es nicht laufen lassen** (kein Produktions-Zugriff, `api.ebay.com` proxy-geblockt).
   - Typecheck (Server+App) grün, `bun test` 107/107 grün. `AUTO_PRICE_WRITE_ENABLED` unangetastet, kein Preis geschrieben, keine ändernde eBay-API, keine DB-Migration.
   - **Vor Merge:** Nutzer-Freigabe.

-7. **Preis-Fundament Teil 3 (2026-09-13, PR #89, GEMERGT):** "Varianten mit eigenen Preisen" — Geld-Logik + additive DB-Migration. Voraussetzung (Teil 2D/PR #88 gemergt, v1.11 live) vor Start verifiziert.
   - **Problem:** 20 Produkte haben >1 Variante, die App speichert aber nur EINEN sellPrice je Produkt → gleicher Preis für alle Varianten, dadurch stark unterschiedliche Gewinne (stele-110: 3,15 bis 8,69€, Spanne 5,54€).
   - **Preisregel umgesetzt** (`computeVariantSellPrices()` in shared/pricing.ts): Anker = teuerste Variante, behält EXAKT den heutigen sellPrice; deren Gewinn ist der Zielgewinn für alle Varianten; jede andere bekommt den gleich-Gewinn-Preis, `roundToNearest95()`. Zwei harte Schranken: nie über den heutigen sellPrice erhöhen; liegt der Ankergewinn unter `targetMarginEur`, gilt dieser als Untergrenze. Zusätzlich `profitAtSellPrice()` (Gewinn bei gegebenem Preis, exakt die Formel aus dem Auftrag). Die Umkehrrechnung nutzt bewusst `computeMinSellPrice()` wieder statt die Formel zu duplizieren.
   - **Datenmodell — eigene Spalte `products.variant_sell_prices`** (JSON-Map `{"<skuId>": 12.95}`, additive `ALTER TABLE ADD COLUMN`, keine bestehende Spalte geändert; an `variant_prices` nur der Kommentar präzisiert). Ich hatte zunächst vorgeschlagen, das vorhandene optionale `ebayPrice`-Feld weiterzuverwenden — der Nutzer hat sich am 13.09. für die eigene Spalte entschieden, sie ist nachgezogen. Gegen zwei konkurrierende Wahrheiten schützt eine feste Vorrang-Regel, die NUR über `resolveVariantSellPrice()` angewandt wird: neue Spalte > altes `ebayPrice` > kein gespeicherter VK. Neue Schreibvorgänge befüllen ausschließlich die Spalte, der Altbestand läuft aus. Dazu `parseVariantSellPrices()` (tolerant) + `serializeVariantSellPrices()`. End-to-end durch die echte Spalte belegt (Spaltenwert 15,95€ schlägt veralteten Altwert 99,99€). **Die Spalte wird in diesem PR NICHT befüllt.**
   - **Punkt 3 (Bestandsaufnahme, nichts geändert):** zwei Wege zu eBay dokumentiert mit Datei:Zeile — Weg A einzeln je SKU (Listing: index.ts:2145→ebay.ts:1299/1323/1340; Nachkorrektur: price-monitor.ts:113→135→197) und Weg B Einheitspreis auf ALLE SKUs (price-monitor.ts:231→248→252, aufgerufen aus index.ts:1308).
   - **Punkt 4 (warum manche Listings platt sind):** drei code-belegte Mechanismen benannt — (1) Weg B über ein Varianten-Produkt, das als Einzelartikel erkannt wird (index.ts:1262), (2) SKU-Zuordnung schlägt fehl → Einheits-Fallback für JEDE SKU (price-monitor.ts:128/133, Fehlerklasse aus PR #82), (3) historisch der P-27/P-28-Bug vor PR #77 (wirkt nicht rückwirkend). Welcher pro Produkt greift, entscheidet der Bericht über die Spalten `SKU_Match` + `Live_Preisspanne_heute` — aus der Sandbox nicht feststellbar.
   - **Bericht (Punkte 5+6):** `scripts/export-variant-price-plan.ts` (NUR LESEND, eBay nur GET) — je Variante SKU/EK/heute/neu/Gewinn heute/Gewinn neu/Differenz/Absenkung%/SKU_Match/Verkäufe-90T, je Produkt Summe der Preissenkungen. Verkäufe je SKU aus `getAllOrders()` (eBay-Standardfenster ~90 Tage). **Konnte ich NICHT gegen die echte DB laufen lassen** — kein Produktions-DB-Zugriff, und `api.ebay.com` ist vom Sandbox-Proxy geblockt (403, wörtlich belegt). Stattdessen end-to-end gegen eine lokale Test-DB mit den 6 realen stele-110-EKs geprüft (Summe Preissenkungen 29,00€).
   - **Punkt 7 erfüllt:** stele-110 ergibt exakt 19,95 / 15,95 / 14,95 / 13,95 / 12,95 / 12,95 (committeter Test). Dabei unabhängig bestätigt: der BEFUND des Auftrags (Gewinne 3,15–8,69€, Spanne 5,54€) reproduziert sich exakt → belegt sellPrice 19,95€, Zoll 4,00€, adRate 5%. Die Zollpauschale kürzt sich in der Gleich-Gewinn-Rechnung heraus (eigener Test).
   - **Entschieden am 13.09.2026 (Nutzer):** (a) die 8%-Senkungsbremse gilt AUCH für die Varianten-Umstellung → Zielpreise werden schrittweise erreicht; neue `planCappedPriceSteps()` rechnet das vorab durch, Bericht zeigt `Zielpreis` / `naechster_Lauf_Preis` / `Laeufe_bis_Ziel` / `Ziel_erreichbar`. Nachgerechnet statt geschätzt: größter stele-110-Sprung braucht **7 Läufe** (meine frühere Schätzung „~5" war falsch, korrigiert). (b) Anker bleibt der gespeicherte `sellPrice` — war bereits so umgesetzt. **Nebenbefund:** bei sehr günstigen Artikeln kann die Bremse ein Ziel gar nicht erreichbar machen (1,95€ → Grenzwert 1,794€ → gerundet wieder 1,95€); gemeldet als `reachesTarget=false` statt Endlosschleife, im Bericht als `Ziel_erreichbar=nein` gezählt.
   - **Abweichung gemeldet:** in Teil 2D war für stele-110 ein eBay-Live-Preis von 20,95€ genannt, die Gewinnzahlen dieses Auftrags reproduzieren sich aber nur mit 19,95€ → gespeicherter sellPrice und eBay-Live-Preis laufen auseinander. Der Bericht macht das sichtbar (`Ankerpreis` vs. `Live_Preisspanne_heute`).
   - Regressionsgetestet: Ankerregel testweise umgedreht (kleinster statt größter EK) → 4 Tests schlagen korrekt fehl; danach zurückgesetzt, 83/83 grün.
   - `buildVariantSku()` aus `updateEbayVariantPricesIndividually()` extrahiert (reiner Extract, identische Logik, bestehende PR-#82-Tests weiter grün), damit der Bericht gegen dieselbe SKU-Zuordnung läuft wie der spätere Schreibvorgang.
   - Typecheck (Server+App) grün, `bun test` 97/97 grün. `AUTO_PRICE_WRITE_ENABLED` unangetastet, kein Preis geschrieben, keine eBay-Anzeige geändert, `applyDecreaseCap` unverändert, Migration rein additiv.
   - **Vor Merge:** Nutzer-Freigabe nötig (Geld-Logik, CLAUDE.md-Regel).

-6. **Preis-Fundament Teil 2D (2026-09-10, PR #88, GEMERGT):** "Senkungsbremse" — Geld-Logik, KEINE DB-Migration. Voraussetzung (Teil 2C/PR #87 gemergt) vor Start verifiziert.
   - **Problem:** `computeMinSellPrice()` liefert eine UNTERGRENZE, keinen Zielpreis — die automatischen Pfade behandelten sie als Zielpreis und hätten laufende Angebote in EINEM Lauf darauf heruntergezogen (real: stele-141 23,95€→10,95€ = −54%, stele-110 20,95€→10,95€ = −48%).
   - **Neu:** `MAX_PRICE_DECREASE_PERCENT = 8` (shared/constants.ts) + `applyDecreaseCap(currentPrice, computedMinPrice, maxDecreasePercent)` (shared/pricing.ts, gibt `{price, wasCapped, uncappedPrice}` zurück). Anheben wird NIE gedeckelt; kein `currentPrice` (Erst-Listing) → unverändert.
   - **Angewandt auf:** `price-monitor.ts checkOne()`, `index.ts check-all-prices`, `recalculate-preview` (beide Zweige) und `recalculate-apply` (beide Zweige). **Nicht** auf `/ebay/list` (Erst-/Re-Listing, kein alter Preis) und nicht auf die Anzeige-/Import-Rechner (lieferanten.tsx, index.tsx, produkte.tsx) — laut Auftrag.
   - **Vorschau:** `recalculate-preview` liefert pro Zeile jetzt zusätzlich `uncappedNewPrice` + `wasCapped`, damit sichtbar ist, dass der Preis bei einem erneuten Lauf weiter fällt. `note`-Feld entsprechend ergänzt.
   - **Rundungs-Präzisierung (Abweichung von der wörtlichen Vorgabe, bewusst und begründet):** `roundToNearest95()` auf den 8%-Grenzwert angewandt kann den Preis UNTER den Grenzwert drücken (stele-141: Grenzwert 22,034€ → 21,95€ = 8,35% statt 8%) und damit genau die geforderte Erwartung "kein Produkt fällt um mehr als 8%" verletzen. Deshalb: erst `roundToNearest95()`, nur bei Unterschreitung stattdessen `roundUpToX95()`. Regressionstest hält den Fall fest. Rückfrage an den Nutzer in der PR gestellt, falls doch die wörtliche Variante gewünscht ist.
   - **Verifikation:** `scripts/teil2d-decrease-cap-audit.md` (2 reale Produkte aus dem Auftrag + die 3 Pflicht-Testfälle a/b/c) + `scripts/export-decrease-cap-audit.ts` (NEU, nur lesend — erzeugt die volle Tabelle für ALLE live gelisteten Produkte gegen echte DB + eBay-Live-Preise, prüft selbst auf >8%-Verletzungen). **Die ≥10-Produkte-Tabelle konnte ich nicht selbst erzeugen** (kein Live-DB-/eBay-Zugriff aus der Sandbox) — nur die 2 real mitgeteilten Produkte stehen drin, der Rest wurde NICHT erfunden.
   - **Zusatz-Korrektur (vom Nutzer gemeldet):** in `scripts/teil2c-target-margin-audit.md` stand im Vergleich "Teil 2B vs 2C" ein falscher Gewinn von 2,23€. Nachgerechnet: **2,08€**. Beim Nachrechnen fiel auf, dass auch die Teil-2B-Zeile falsch war (4,23€ → korrekt **3,61€**) — beides korrigiert und im Dokument als Korrektur gekennzeichnet. Die Haupttabellen #5/#6 wurden gegengeprüft und sind korrekt.
   - Regressionsgetestet: Bremse testweise deaktiviert → 2 Tests schlagen korrekt fehl; danach zurückgesetzt, 74/74 grün.
   - Typecheck (Server+App) grün, `bun test` 74/74 grün. `AUTO_PRICE_WRITE_ENABLED` unangetastet (Wert per Diff geprüft), kein Preis geschrieben, kein eBay-Schreib-Call, keine DB-Migration.
   - **Vor Merge:** Nutzer-Freigabe nötig (Geld-Logik, CLAUDE.md-Regel).

-5. **Preis-Fundament Teil 2C (2026-09-10, PR #87, GEMERGT):** "Zielgewinn trifft exakt" — SICHERHEITSKRITISCH, betrifft Geld-Logik + DB-Migration. Voraussetzung (Teil 2A/PR #85 + Teil 2B/PR #86 gemergt) vor Start verifiziert.
   - **Ursache 1 behoben — Sicherheitspuffer:** `DEFAULT_PRICING_CONFIG.safetyBufferEur` (shared/pricing.ts) fest auf **0** gesetzt (vorher 1,50€, ZUSÄTZLICH zum Zielgewinn addiert). `PRICE_SAFETY_BUFFER_EUR` (shared/constants.ts) bleibt als Konstante stehen (History), wird aber von keiner Kalkulation mehr referenziert — Import aus `pricing.ts` entfernt, alle Vorkommen geprüft.
   - **Ursache 1 behoben — Rundung:** alle produktiven `'up95'`-Aufrufstellen (price-monitor.ts ×2, index.ts ×4, ebay.ts ×1 — die 7 automatischen/Listing-Pfade aus Teil 2A) auf `'nearest95'` umgestellt (rundet auch ABWÄRTS). `lieferanten.tsx` (Einzelbutton `'cent'`, Varianten-Tabelle `'nearest95'`) sowie `index.tsx`/`produkte.tsx` (`'none'`, reine Anzeige) waren bereits puffer-/up95-frei bzw. unbetroffen — unverändert.
   - **Ursache 2 behoben — Zielgewinn pro Produkt persistiert:** neue additive Spalte `target_margin_eur REAL DEFAULT 2.00` an `products` (schema.ts + migrate.ts, reine `ALTER TABLE ADD COLUMN`, keine bestehende Spalte angefasst). `POST /api/products` (lieferanten.tsx `handleSave()`) schreibt jetzt `targetMarginEur: minGewinn` mit. Alle Kalkulationen lesen `product.targetMarginEur ?? DEFAULT_PRICING_CONFIG.targetMarginEur` statt des globalen `MIN_GEWINN_EUR`-Fallbacks — `computeVariantPriceRows()` (price-monitor.ts) bekam dafür einen neuen 5. Parameter, `EbayListingInput` (ebay.ts) ein neues optionales `targetMarginEur`-Feld (durchgereicht von `index.ts`s `/ebay/list`).
   - **Definition Zielgewinn geprüft:** `computeMinSellPrice()` bildet "netto in der Hand" bereits vollständig ab (EK, Lieferantenversand, Zoll, eBay-Gebühr+Anzeigentarif+MwSt. auf beide, Fixgebühr+MwSt.) — keine fehlende Position gefunden, siehe `scripts/teil2c-target-margin-audit.md`.
   - **Verifikation:** `scripts/teil2c-target-margin-audit.md` — Tabelle für 10 reale Produkte (stele-98/110/141, Zielgewinn 2€): tatsächlicher Gewinn nach Rückrechnung weicht max. 38 Cent vom Ziel ab (Ø ±21,6 Cent), ausschließlich durch ,95-Rundung erklärt, keine Abweichung >0,40€. Zusatzfall Zielgewinn 4€ für 3 Produkte: VK steigt sichtbar/korrekt proportional (z.B. stele-98 9,95€→11,95€). `scripts/export-target-margin-audit.ts` (NEU, nur lesend) — analoges Live-Skript für ALLE live gelisteten Produkte, muss der Nutzer selbst gegen die Produktions-DB laufen lassen (kein Live-Zugriff aus der Sandbox); lädt/importiert/queryt nachweislich korrekt (gegen Dummy-DB verifiziert, scheitert nur am fehlenden Tabellen-Schema dort).
   - `pricing.test.ts` (23 Tests, u.a. neues "nearest95"-Fall-Describe für die automatische Preisprüfung statt "up95", neuer Teil-2B-vs-2C-Vergleichstest) + `price-monitor.test.ts` (2 neue Tests: `targetMarginEur`-Parameter wird honoriert, `null` fällt auf Default zurück) — alle Fixtures frisch mit `bun` nachgerechnet, nicht geraten.
   - Regressionsgetestet: `safetyBufferEur` testweise zurück auf 1,50€ → 9 Tests schlagen korrekt fehl; EINE reale Aufrufstelle (`price-monitor.ts computeVariantPriceRows()`) testweise zurück auf `'up95'` → der neue Parameter-Test schlägt korrekt fehl (mit absichtlich divergenzfähiger Fixture 2,55€, nicht der ursprünglich gewählten 2,15€, die zufällig identisch rundete — im ersten Anlauf bemerkt und korrigiert). Beides danach zurückgesetzt, 66/66 wieder grün.
   - Typecheck (Server+App) grün, `bun test` 66/66 grün.
   - `AUTO_PRICE_WRITE_ENABLED` unangetastet (weiterhin `false`, per Diff verifiziert) — kein Preis geschrieben, kein eBay-Call ausgelöst.
   - **Vor Merge:** Nutzer-Freigabe nötig (Geld-Logik + DB-Migration, CLAUDE.md-Regel, explizit im Auftrag als SICHERHEITSKRITISCH markiert).

-4. **Preis-Fundament Teil 2B (2026-09-10, PR #86, GEMERGT):** Gebühren-Konstanten auf real gemessene Werte umgestellt — SICHERHEITSKRITISCH, betrifft Geld-Logik.
   - `DEFAULT_PRICING_CONFIG` (shared/pricing.ts): `ebayFeeRatePercent` 13→**15**, `ebayFixedFeeEur` 0,45→**0,30**, MwSt-Faktor unverändert 1,19. Alle 8 Aufrufstellen aus Teil 2A lesen bereits aus dieser Konfiguration (keine eigenen Literale mehr) — Korrektur wirkt sich überall gleichzeitig aus. TODO-Kommentare aus Teil 2A entfernt; `index.tsx`/`produkte.tsx`, die bisher eigene 17/18%-Literale hatten, zeigen jetzt ebenfalls auf `DEFAULT_PRICING_CONFIG`.
   - **Anzeigengebühr im Preise-Tab** (`index.tsx`): freies "Anzeigegebühr"-Textfeld entfernt, ersetzt durch Produkt-Auswahl (Dropdown, lädt `GET /api/products`) — Anzeigengebühr kommt jetzt aus `adRate` des gewählten Produkts. Kein Produkt gewählt oder `adRate` null → 0%, sichtbar als "ohne Anzeigengebühr" gekennzeichnet (kein stiller Platzhalter).
   - **Default Herkunft** (`index.tsx`): `ausChina` startet jetzt `true` (vorher `false`), da Lieferant-Default "aliexpress" ist; Lieferant-Dropdown setzt `ausChina` beim Wechsel reaktiv mit (`val === "aliexpress"`).
   - **SICHERHEITSKRITISCH — `AUTO_PRICE_WRITE_ENABLED = false`** (neuer Flag, `shared/pricing.ts`): die neuen Werte sind nur gegen 6 von 13 realen Bestellungen rückgerechnet (±0,05€ Unsicherheit), noch nicht gegen einen vollen Preiszyklus bestätigt. Gated: `price-monitor.ts checkOne()` (8h-Cron) und `index.ts POST /products/check-all-prices` — beide schreiben `sellPrice` nur noch und pushen nur noch zu eBay, wenn der Flag `true` ist; `buyPrice`/`priceChanged` (Tatsachen-Sync/Anzeige-Flag) bleiben unverändert aktiv. Menschlich bestätigte Preview→Übernehmen-Abläufe (`recalculate-apply`, `/ebay/list`, Varianten-Reparatur) bleiben bewusst UNGEGATED, da dort vor jedem Schreibvorgang eine Vorschau mit den neuen Zahlen gezeigt wird — per Diff verifiziert, dass genau diese vier Stellen (2× DB-Write, 2× eBay-Push) den Flag prüfen.
   - **Verifikation:** `scripts/export-pricing-comparison.ts` (NUR LESEND, Vorher/Nachher-Margen-CSV für alle live gelisteten Produkte) + `scripts/teil2b-order-audit.md` (Formel gegen 6 von 13 real bekannten Bestellungen: Ø 1,33 Cent Abweichung, größte Einzelabweichung 4 Cent — beides innerhalb ±0,05€ Toleranz). **7 der 13 Bestellungen fehlen** — keine Daten dafür im Chat, keine erfundenen Werte verwendet. Die vollständige Vorher/Nachher-Produkttabelle muss der Nutzer selbst per Skript gegen die Produktions-DB erzeugen (kein Live-Zugriff aus der Sandbox).
   - `pricing.test.ts` (19 Tests) mit neu berechneten Fixture-Werten (15%/0,30€) fortgeschrieben — alte Teil-2A-Werte (13%/0,45€) durch echte Neuberechnung ersetzt, nicht geraten.
   - Typecheck (Server+App) grün, `bun test` 61/61 grün.
   - **Vor Merge:** Nutzer-Freigabe nötig (Geld-Logik, CLAUDE.md-Regel, explizit SICHERHEITSKRITISCH im Auftrag markiert).

-3. **Preis-Fundament Teil 2A (2026-09-10, PR #85, GEMERGT):** reiner Struktur-Refactor, WERTNEUTRAL — kein Rechenergebnis geändert, nur sieben-plus-eins unabhängige Formel-Kopien (Teil-1-Bestandsaufnahme + ein achter, dort übersehener Fund in `ebay.ts`) auf eine einzige Kalkulationsfunktion umgestellt.
   - `shared/pricing.ts` in-place ersetzt (nicht der im Auftrag vorgeschlagene Pfad `src/lib/pricing.ts` — existiert in diesem Projekt nicht, `shared/` ist der etablierte, von Backend UND Frontend importierbare Ort): neue `computeMinSellPrice(input: PricingInput): PricingResult` — alle Eingaben Pflichtparameter, keine Defaults in der Funktion. Defaults leben separat in `DEFAULT_PRICING_CONFIG` (13% Basis-Gebühr, 0,45€ Fix, 1,19 MwSt, 5% adRate-Default, 2€ Mindestgewinn, 1,50€ Sicherheitspuffer, 4€ China-Zoll).
   - Alle 8 Fundstellen umgestellt: `price-monitor.ts` (`computeVariantPriceRows`, `checkOne`), `index.ts` (`recalculate-preview`, `recalculate-apply`, `/ebay/list`, `check-all-prices` — dort NUR der Funktionsaufruf ersetzt, sonst nichts, wie vorgegeben), `ebay.ts` (Varianten-Listing-Preis-Fallback), `lieferanten.tsx` (Einzelprodukt-Button + Varianten-Tabelle `recommendedFor()`), `index.tsx` (Preise-Tab-Verhandlungsrechner), `produkte.tsx` (PriceBadge).
   - Die drei abweichenden Gebühren-Annahmen (13/17/18%) bewusst NICHT vereinheitlicht — jede Aufrufstelle übergibt weiterhin ihren bisherigen Wert, markiert mit `// TODO Teil 2B: auf gemessene 15% + 0,30 EUR umstellen`. Neuer `RoundingMode`-Parameter (`'up95'`/`'nearest95'`/`'cent'`/`'none'`) bildet alle bisher unterschiedlichen Rundungsverhalten weiterhin exakt ab (inkl. `roundToNearest95()` aus lieferanten.tsx, die bewusst auch abrunden kann — jetzt zentral statt lokal dupliziert).
   - **Snapshot-Tests** (`pricing.test.ts`, 17 Tests): reale Einkaufspreise aus stele-98 (1) + stele-110 (6 Varianten) + stele-141 (5 Varianten) = 12 echte Datenpunkte, vom Nutzer in Teil 1 direkt mitgeteilt (shippingCost/adRate/shipsFrom dafür unbekannt, mit App-Standardwerten angenommen, klar dokumentiert). **stele-152 NICHT enthalten** — keine reale Zahl dafür im Chat verfügbar, keine erfundenen Werte verwendet. "Vorher"-Werte sind feste Fixtures (vor dem Löschen der alten Formel einmalig berechnet), nicht zur Laufzeit neu berechnet.
   - Regressionsgetestet: `DEFAULT_PRICING_CONFIG.ebayFeeRatePercent` testweise 13→15 geändert → 8 von 17 Tests schlagen korrekt fehl, danach zurückgesetzt (17/17 wieder grün) — Beweis, dass die Tests echte Wertänderungen erkennen würden.
   - Math.min()-Logik für Varianten-EK (5 Stellen, `aliexpress.ts` ×4 + `lieferanten.tsx` Anzeige) unangetastet (Teil 3). `check-all-prices` unverändert bis auf den Funktionsaufruf.
   - Typecheck (Server+App) grün, `bun test` 59/59 grün.
   - **Vor Merge:** Nutzer-Freigabe nötig (Preis-Logik, CLAUDE.md-Regel) — Nutzer prüft Diff + Testausgabe selbst.

-2. **P-27/P-28 PR 6 (2026-09-09, Draft #83, wartet auf manuelle Freigabe):** Reparatur-Job robust + Voll-Audit-Skript + AliExpress-Verfügbarkeits-Check mit eBay-Auto-Deaktivierung.
   - **Teil A.1 (Job-Persistenz):** Fortschritt von `POST /ebay/listings/repair-variant-prices` wird jetzt in `app_settings` (Key `repair_variant_prices_offset`) statt nur im Client-Modal gespeichert — ein Server-Neustart setzt den Lauf fort statt bei Produkt 1 neu zu beginnen. Frontend (`listings.tsx`) umgebaut auf `runRepairLoop()` mit drei Einstiegen: Hauptbutton (löscht Anzeige + startet vom persistierten Offset), "Fortsetzen"-Button bei Fehler (behält Anzeige, setzt vom persistierten Offset fort — verwirft KEINE bereits erfolgreich reparierten Ergebnisse, siehe PR-#79-Anforderung), "Von vorne beginnen"-Link (erzwingt Offset 0).
   - **Teil A.2 (Voll-Audit-Skript):** `scripts/audit-all-variant-skus.ts` — NUR LESEND, prüft ALLE live gelisteten Varianten-Produkte gegen die echten eBay-SKUs (`getInventoryItemGroupSkus` + dieselbe Matching-Logik wie der PR-#82-Fix), keine PUT-Aufrufe, kein 502-Risiko (läuft direkt in der Shell, nicht über einen Browser-Request). Ersetzt die UI-Klick-Methode für Verifikationszwecke. **Muss der Nutzer selbst gegen die Produktions-DB ausführen** (kein eBay-/DB-Zugriff aus der Sandbox, gleiches Muster wie `verify-ships-from-fix.ts` aus PR #82): `bun run scripts/audit-all-variant-skus.ts` (optional `--json`).
   - **Teil B (AliExpress-Verfügbarkeit → eBay-Auto-Deaktivierung, NEU 09.09.2026):** `classifySourceUnavailability()`/`checkSourceAvailability()` (`aliexpress.ts`) erkennen HTTP 404 oder einen von vier bestätigten "nicht verfügbar"-Textmarkern als eindeutigen Nachweis — alles andere (Timeouts, 403/429/5xx, uneindeutiger Seiteninhalt) gilt explizit NICHT als nicht verfügbar (False-Positive-Schutz, per Regressionstest bewiesen: Fix testweise entfernt → genau der False-Positive-Test schlägt fehl). Wird in `checkOne()` (`price-monitor.ts`) NUR als letzter Schritt aufgerufen, NACHDEM die komplette bestehende Scrape-Fallback-Kette bereits vollständig fehlgeschlagen ist — kann also nie einen erfolgreichen Scrape verhindern/überschreiben. Neue Funktion `markProductSourceUnavailable()`: beendet eine ggf. aktive eBay-Anzeige (neue, aus 3 vorherigen Duplikaten konsolidierte `endListing()` in `ebay.ts`) und setzt `ebayStatus: 'unavailable'` + `ebayError: <Grund>` (kein Schema-Migration nötig — bestehende, unconstrainte Spalte). Neue, separate Zähler-Kachel "AliExpress nicht verfügbar" im Produkte-Tab (`produkte.tsx`) — bestehende "Fehler"/"Preisalarm"-Zähler unverändert.
   - **Teil C (Produkt 85 "invalid category ID"):** untersucht, kein Fix umgesetzt (Auftrag: nur Diagnose). Root-Cause-Hypothese: `index.ts:2064` vertraut einem einmal gesetzten `product.ebayCategory` dauerhaft, ohne ihn je gegen eBays Taxonomy-API neu zu validieren — `suggestCategory()` (liest die LIVE-Kategorie-Baum-Struktur) wird nur als Fallback aufgerufen, wenn `ebayCategory` leer ist. eBay reorganisiert/entfernt Kategorien periodisch; eine früher gültige gespeicherte ID kann so unbemerkt ungültig werden. Zweiter Risikofaktor: das freie "eBay Kat-ID"-Textfeld (`produkte.tsx`) hat keine Validierung. Sowie `POST /ebay/listings/bulk/categories` (`index.ts:1442`), das `body.categoryId` ungeprüft an `reviseCategory()` durchreicht. Vorschlag (nicht umgesetzt, braucht Freigabe wegen Live-eBay-Kategorie-Auswirkung): gespeicherte Kategorie-ID vor Verwendung gegen die Taxonomy-API validieren, bei Ungültigkeit automatisch auf einen frischen `suggestCategory()`-Aufruf zurückfallen. Kann produkt-85-spezifisch nicht abschließend bestätigt werden ohne den tatsächlich gespeicherten `ebayCategory`-Wert (kein Live-DB-Zugriff aus der Sandbox).
   - **Schritt 1 (Folge-Fix, selbe PR):** `scrapeWithPlaywright()` hing bei toten/blockierten Quellen live nachgewiesen >3 Min. ohne je aufzulösen (kein Timeout, kein Fehler) — vermutlich in `chromium.launch()`/`browser.close()`, nicht in den bereits intern begrenzten `goto()`/`waitForTimeout()`-Schritten. Fix: generischer, exportierter `raceWithHardTimeout()`-Wrapper (18s hartes Außen-Timeout) umschließt jetzt die GESAMTE Funktion, unabhängig davon wo genau sie hängt. Zusätzlich `classifySourceUnavailability()` um DS-API-Fehlercode 482 ("isv.SHIP_TO_COUNTRY_PROHIBITED") als zusätzliches Verfügbarkeits-Signal erweitert (dritter Parameter `dsApiErrorCode`), neue `checkDsApiUnavailability()` prüft das gezielt (wiederverwendet `fetchDsApiRaw()`, aus `scrapeWithDsApi()` extrahiert — keine Logik-Duplikation), in `checkSourceAvailability()` verkettet. Regressionsgetestet (Timeout deaktiviert → Hang-Test schlägt nach 5s Test-Timeout korrekt fehl; 482-Erkennung deaktiviert → alle 4 zugehörigen Tests schlagen korrekt fehl).
   - **Schritt 2 (baut auf Schritt 1 auf):** neuer täglicher Cron `runAvailabilityCheck()`/`startAvailabilityCheckCron()` (`price-monitor.ts`, in `server.ts` registriert, erster Lauf nach 5 Min, dann alle 24h) — prüft JEDES `ebayStatus='listed'`-Produkt aktiv gegen `checkSourceAvailability()` (unabhängig vom passiven Nebeneffekt-Check im 8h-Preis-Cron), nutzt bei Nichtverfügbarkeit die bestehende `markProductSourceUnavailable()` wieder (keine eigene Beenden-/DB-Logik). Vollständig DI-testbar (Mock-Produktliste + Mock-Funktionen, `pauseMs:0` für schnelle Tests), Rate-Limiting-Pause (1,5s Default) zwischen AliExpress-Abrufen. Regressionsgetestet (unavailable-Flag-Prüfung deaktiviert → 5 Tests schlagen korrekt fehl, inkl. dem zentralen False-Positive-Test).
   - **Schritt 3 (unabhängig, rein UI):** Produktkarte (`produkte.tsx`) bekommt bei `ebayStatus='unavailable'` einen orangen Rahmen (#FB923C) + Hintergrund (#FFF7ED) und einen Hinweistext "AliExpress-Quelle nicht verfügbar — neue Quelle muss gesucht werden"; Stats-Kachel + StatusBadge ebenfalls auf Orange (#C2410C/#FFEDD5) statt Rot umgestellt. Kein Backend-/Preis-/DB-Änderungsbedarf. **Test:** kein echter Live-Screenshot möglich (kein lokal bootfähiges DB-Schema/eBay-Zugangsdaten in der Sandbox) — stattdessen Stilprobe mit exakt denselben Hex-/Layout-Werten via Playwright/Chromium gerendert und per Screenshot an den Nutzer geschickt; visuell klar von Preisänderung (Gelb) und Fehler (Rot) unterscheidbar. Bitte nach Merge live gegenprüfen.
   - Typecheck (Server+App) grün, `bun test` 47/47 grün (24 in `aliexpress.test.ts`, 18 in `price-monitor.test.ts`, 5 in `pricing.test.ts`).
   - **Vor Merge:** Nutzer-Freigabe nötig (CLAUDE.md: Auto-Deaktivierung einer Live-eBay-Anzeige = Preis-/Listing-Logik mit direkter Umsatzwirkung).
-1. **P-27/P-28-Fix "individuelle Varianten-Preise" (2026-09-08/09):** PR #77 (Code-Fix), PR #78 (Reparatur-Endpunkt) und PR #79 (Chunking nach Produktzahl) **GEMERGT**. PR #81 (SKU-gewichtetes Chunking, `maxVariantsPerBatch`) — Status s.o., letzter bekannter Stand Draft.
   - **NEU (09.09., per Diagnose-Skript gegen Produktions-DB + echte eBay-SKUs verifiziert):** dritter, unabhängiger Bug gefunden — `updateEbayVariantPricesIndividually()` baute die erwartete SKU aus ALLEN `row.attrs`, ohne die "Ships From"/Herstellungsland-Blacklist anzuwenden, die `ebay.ts` beim Listing-Erstellen schon kennt. Enthält `row.attrs` "Ships From" (z.B. "China Mainland"), verlängert sich die erwartete SKU um ein nicht existierendes Segment → kein Match → Preis bleibt für die Variante(n) unverändert. Live bestätigt: Produkte 71 (10/10 SKUs), 77 (2/2), 92 (2/2), 95 (1/1) betroffen; Referenz 138 (kein "Ships From") 3/3 korrekt.
   - **PR #82 (Draft, wartet auf manuelle Merge-Freigabe):** Blacklist aus `ebay.ts` konsolidiert (2 lokale Kopien → 1 exportierte `NON_VARIATION_ASPECTS`), `updateEbayVariantPricesIndividually()` filtert `row.attrs` jetzt davor. Committete Tests (a/b/c aus dem Auftrag) + Regressions-Check (Fix testweise entfernt → Tests schlagen exakt mit dem Live-Symptom fehl). Typecheck + `bun test` (18/18) grün.
   - **Live-SKU-Verifikation (Punkt 4 des Auftrags) noch offen** — kann aus der Sandbox nicht durchgeführt werden, nur nach Merge+Deploy durch den Nutzer möglich.
   - Separater, NICHT Teil dieses Fixes: Produkt 70 hat gespeicherte attrs, die nicht zu den echten eBay-Varianten passen (Datenproblem, kein Matching-Bug) — muss separat manuell geprüft werden.
   - Verwandtes, separat angefragtes PR 2/2 (analoger Fix in `/products/check-all-prices`, das aktuell GAR KEINE Varianten-Erkennung hat) weiterhin nicht begonnen.
0. **P-112 (2026-09-08, Root Cause gefunden, PR #73 gemergt):** Alle bisherigen Fixes (P-109-P-111) hatten am falschen Ort angesetzt (`aliexpress.ts`/`scrapeWithDsApi()`) — der Import-Endpunkt nutzt tatsächlich `getAliProductByApi()` aus `aliexpress-api.ts`, eine dritte, nie berührte Extraktion (`result.store_info?.store_name`). Diagnose-Logging jetzt am richtigen Ort deployed (PR #73). **Wartet auf:** Nutzer scrapt Produkt 1005010280178344 erneut und schickt die `[AliExpress API] P-112-Diagnose:`-Logzeilen aus Render. Danach: numerische Shop-ID gefunden → ID-basierter Abgleich; keine ID → anderer Datenweg (z.B. dedizierter Shop-Info-API-Call).

0b. **P-66-Bypass — manueller Freigabe-Button (2026-09-08, PAUSIERT, keine Entscheidung):** Auftrag: Button "Manuell freigeben" bei der Compliance-Sperre, unabhängig von der (kaputten) Verkäufer-Erkennung, inkl. DB-Flag `manuallyApprovedRegulated`+Zeitstempel. Vor Umsetzung Rückfrage gestellt, da das eine bewusste "kein Bypass"-Entscheidung aus P-66 (GPSR-Haftungsgrund, Commit `3bf9088`) aufheben würde + DB-Migration (CLAUDE.md-Bestätigungspflicht). Nutzer hat die Rückfrage weggeklickt, dann kam Preis-Krise (Punkt 1 unten) dazwischen — **keine Entscheidung getroffen**, nichts umgesetzt. Hängt inhaltlich mit P-112 zusammen: falls die Verkäufer-Erkennung bald tatsächlich funktioniert, braucht es den Bypass evtl. nur als Übergangslösung.

1. **Preis-Krise "Teil 1-4" (2026-09-08, TEILWEISE UMGESETZT — PR #74 GEMERGT, deployed):** Auslöser: stele-98-Variante "White 1pcs" verkaufte real mit Verlust. Root-Cause-Suche fand DREI unabhängige Preis-Bugs — alle jetzt auf einen zentralen `shared/pricing.ts` konsolidiert und in **PR #74** (gemergt nach expliziter Nutzer-Freigabe, inkl. committeter `pricing.test.ts`) behoben:
   - (a) `ebay.ts:1249` Fallback auf rohen Einkaufspreis bei fehlendem `ebayPrice` → behoben (berechnet jetzt live nach oder blockiert mit Fehlermeldung)
   - (b) adRate-Default-Inkonsistenz (`?? 0` vs `?? 5`) → überall auf `5` vereinheitlicht
   - (c) Dritte Formel in `/products/check-all-prices` (fester 18%, kein Puffer, automatischer Live-Push) → nutzt jetzt die zentrale Funktion
   - `/ebay/list` berechnet Preis jetzt frisch unmittelbar vor dem eBay-Call (Erst-Listing + Re-Listing)
   - Import-Preisvorschlag (`lieferanten.tsx`) bewusst UNVERÄNDERT gelassen (kein Puffer) — regressionsgetestet, exakt gleiche Ergebnisse wie vorher
   - Typecheck (Server+App) grün, alle Test-Erfolgsbedingungen bestätigt (20,95€ mit/18,50€ ohne Puffer bei buyPrice=10/versand=2/adRate=5, 6 Regressionsfälle exakt identisch zur alten Formel)
   - **Noch offen/bewusst nicht angefasst:** weitere Formel-Kopien in `lieferanten.tsx` (Bulk-Varianten-Vorschau/Validierungsanzeige, ~Zeilen 1749/1808/1843) — nur UI-Anzeige, kein gespeicherter Preis, nicht im ursprünglichen Auftrag
   - **Teil 1 (Sofortkorrektur stele-98) weiterhin blockiert auf Nutzer:** kein eBay-API-Zugriff aus der Sandbox — manuelle Korrektur im Seller Hub nötig; nach Deploy von PR #74 sollte ein Re-Listing der Variante den Preis jetzt aber korrekt berechnen
   - **Teil 2 (feste 2,00€-Gewinn-OBERGRENZE statt Minimum)** und **Teil 4 (Sicherheitsgate >5€)** aus dem ursprünglichen Ziel: NICHT Teil von PR #74 (das war ein separates, neueres, engeres Konsolidierungs-Ziel) — weiterhin nur als Plan vorhanden, keine Design-Entscheidung getroffen, kein Code geschrieben
   - **Nebenbei erledigt:** CLAUDE.md-Versionszähler war seit v1.6/2026-08-31 über mehrere Sessions nicht gepflegt worden (Standing-Rule) — anhand `git log --merges` #63-74 rekonstruiert und nachgeholt, PR #75 (gemergt): v1.6 → v1.8, neuer Zähler "2 von 4 seit v1.8"
   - **Vor Merge von PR #74:** Nutzer-Freigabe nötig (Geld-Logik, Standing-Regel)
   - Alte P-27/P-28-Detailfunde bleiben als Kontext gültig, siehe Punkt 1b unten (id=137 weiterhin ungeklärt)

1b. **P-27/P-28-Gebührensatz-Check (2026-09-06, Altfunde — durch Punkt 1 oben ergänzt, NICHT mehr "abgeschlossen"):** Nutzer meldete realen Testkauf mit ~20,27% eBay-Gebühr (3,03€/14,95€). Mit expliziter einmaliger Nutzer-Freigabe wurde der echte Produktions-DB-Export (Gmail-Backup 5.9. 23:35 Uhr) ausgewertet:
   - **Gebührensatz-Ergebnis (Katalog-Snapshot vom 5.9.):** ALLE 32 live gelisteten Produkte hatten `adRate=5` (DB-Default) → Formel nahm einheitlich 21,4% Gebühr an — das lag ÜBER den beobachteten 20,27%. Kein katalogweiter Unterpreisungs-Fehler durch den Gebührensatz ALLEIN — aber siehe Punkt 1 oben für die seitdem gefundenen strukturellen Bugs.
   - **Ausreißer: id=137** ("XXL Vakuumbeutel ohne Pumpe") — gespeichert 10,95€, korrekt wären 19,95€ (+9,00€, fast halber Preis!). Root Cause: `lastPriceCheck` stand seit 27.08. fest (9+ Tage) — Cron scheiterte vermutlich durchgehend beim Scraping für dieses eine Produkt. **Weiterhin unkorrigiert, weiterhin riskant — noch offen.**
   - P-27/P-28-Dauerlösung (`calcSellPrice()`/`computeVariantPriceRows()`) ist grundsätzlich korrekt gebaut (Commit `98c0a16`, PR #47) — die jetzt gefundenen Bugs (Punkt 1) liegen in ANDEREN, parallelen Code-Pfaden (Listing-Erstellung, `check-all-prices`), nicht in der Kernformel selbst.
   - **Noch offen:** (a) id=137 manuell korrigieren; (b) pro-Produkt-Sichtbarkeit für "seit N Tagen nicht erfolgreich geprüft" (kein Teil des ursprünglichen Katalogs, nicht ungefragt umgesetzt).
2. **eBay "Käufe/Verkäufe außerhalb eBay"-Fehlalarm (2026-09-06, neu entdeckt):** Artikel 198601064695 ("Katzenstreuschaufel Set Groß & Klein") wurde laut Gmail 5× in 2 Tagen (04.-05.09.) von eBay automatisch ausgeblendet wegen vermutetem Verstoß gegen den Grundsatz zu Käufen/Verkäufen außerhalb eBay — vermutlich ein Fehlalarm der automatischen Erkennung (Text/Bild löst fälschlich an). Während ausgeblendet: nicht kaufbar, aber auch keine Angebotsgebühr. Noch nicht untersucht, welcher Listing-Inhalt den Trigger auslöst — separates Thema von P-27/P-28.
3. Live-Test-Bestätigung ausstehend für PR #63 (P-100/P-101, gemergt) — mind. eine der 8 betroffenen Bestellungen (z.B. Caner San) soll nach dem Fix einen Bewertungsbitte-Entwurf oder eine P-99-Warnung zeigen
4. GPSR structured fields — `ae_store_info` liefert nur den Shop-Namen; Hersteller-Adressdaten (Name/Adresse/Stadt/E-Mail/Telefon) werden nicht automatisch aus der API-Antwort geparst → GPSR-Felder bleiben manuelle Pflege im Produkte-Tab
5. Playwright/Chromium auf Render Free Tier (ETXTBSY) — kein Fix möglich ohne Paid Tier, ScrapingAnt trägt den Scraping-Pfad
6. Gemini Overload fallback — läuft, aber intermittierend (503 bei Überlastung, Fallback-Beschreibung greift automatisch)
7. Preis-Cron "Alert bei Änderung" — aktuell nur visuelles Flag (gelber Rahmen in Produkte-Tab), keine aktive Benachrichtigung bei Preissprüngen
8. P-Nummernkollisionen zwischen parallelen Sessions kommen wiederholt vor (u.a. P-90, P-93 doppelt vergeben) — beim Vergeben einer neuen Nummer vorher `P-UEBERSICHT.md` UND `git log --oneline origin/main | grep -i "P-"` gegenprüfen
9. Gmail-DB-Backup-Anhänge (stele-db-*.json) lassen sich aus dieser Sandbox nicht mehr programmatisch dekodieren — der Auto-Mode-Classifier blockiert das Parsen der rohen MIME-Nachricht (auch wenn der Anhang nur Produkt-/Preisdaten ohne Kundendaten enthält). Eine frühere Session (P-27/P-28-Commit) konnte das offenbar noch. Für künftige Live-Verifizierung ggf. den Nutzer um die konkreten Werte bitten statt den vollen Export zu ziehen.

## LETZTE GRÖSSERE ARBEIT (diese Session, chronologisch)
- ✅ P-89 bis P-92: eBay-Pflichtfeld-Fehler (errorId 25002) bei Varianten-Listings behoben, generische Selbstheilung mit Kandidatenliste für unbekannte Pflichtaspekte
- ✅ P-95 (im Auftrag "P-88" genannt, Nummer bereits vergeben): manuelles Eingabefeld im Produkte-Tab für Pflichtfelder, die die Selbstheilung nicht befüllen konnte (Dropdown/Freitext je nach eBay-Werteliste)
- ✅ P-94-Updates: Workflow-Vorlage um "Phase 0 — Session-Start-Rundgang", drei Live-Durchlauf-Ergänzungen (Schritt 3/7) und Schritt-10-Sicherheitshinweis ("NIEMALS automatisch versenden") erweitert
- ✅ P-96/P-97/P-99: Zustellungs-Erkennung für Bewertungsbitten verbreitert (mehr E-Mail-Formulierungen erkannt) + Zeitfallback 21→11 Tage gesenkt (eBay bietet keine Zustellstatus-API, recherchiert) + Warnung statt Vorschlag bei möglicherweise verlorener Sendung (25+ Tage ohne Zustellmail)
- ✅ P-98: eBay-Sendungsnummer-Übermittlung wird jetzt persistent nachverfolgt (`tracking_ebay_submitted`) statt nur per Toast — verhinderte vorher fälschliche "übermittelt"-Anzeige bei eBay-seitigem Fehlschlag
- ✅ P-100/P-101 (PR #63, gemergt): `shippedAt` wird jetzt auch beim normalen Sendungsnummer-Flow gesetzt (nicht nur bei explizitem "Als verschickt markieren"), Bestandsfälle per `updatedAt`-Fallback abgedeckt; eBay-Bestellsync nutzt jetzt expliziten 2-Jahre-Filter statt des impliziten 90-Tage-API-Defaults

Vollständige Commit-Historie: `git log --oneline origin/main` (168 Commits seit Projektstart, laufend gepflegt über PRs).

## ARCHITEKTUR (Kurzfassung — Details in `docs/ARCHITECTURE.md`)
- Shop: stele-e-transfer (eBay DE), Kleingewerbe §19 UStG, Wiesbaden
- Stack: Bun + Hono + React + Turso (libSQL)/Drizzle
- Deploy: Render (auto auf push) → stele-e-transfer.onrender.com
- Preisformel (`price-monitor.ts calcSellPrice()`): `sellPrice = (buyPrice + versand + zoll + MIN_GEWINN + 0.45×1.19) / (1 - (13+adRate)/100×1.19)`, aufgerundet auf ,95
- Mindestgewinn: **2,00 €** (`MIN_GEWINN_EUR`, seit 14.07.2026 — vorher 1,60€)
- China-Zoll: **4,00 €** (`CHINA_ZOLL_EUR`, seit 27.07.2026 — vorher 3,00€)

## BEKANNTE ISSUES
- Playwright/Chromium: ETXTBSY auf Render Free → ScrapingAnt Fallback aktiv
- Gemini 2.5 Flash Lite: gelegentlich überlastet → Fallback-Beschreibung
- `render.yaml` (env: node) und `Dockerfile` (installiert Chrome für Playwright) widersprechen sich — nur `render.yaml` ist laut Render-Konfiguration aktiv

## ENV RENDER
Bestätigt gesetzt (aus vorherigem Stand übernommen, nicht neu verifiziert — kein Render-Dashboard-Zugriff von hier):
- ALIEXPRESS_ACCESS_TOKEN ✅
- EBAY credentials ✅
- SCRAPINGANT_API_KEY ✅

Weitere laut Code erforderlich, Render-Status nicht geprüft: GEMINI_API_KEY, RESEND_API_KEY,
SESSION_SECRET, BACKUP_API_KEY, DATABASE_URL/DATABASE_AUTH_TOKEN (Turso), ALIEXPRESS_APP_KEY/SECRET,
EBAY_APP_ID/DEV_ID/CERT_ID/USER_TOKEN — die App läuft produktiv, was dafür spricht, dass sie gesetzt
sind, aber das ist keine Bestätigung.
