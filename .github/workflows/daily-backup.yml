name: Daily Backup

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:

jobs:
  backup:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger backup on Render
        run: |
          curl -fsS https://stele-e-transfer.onrender.com/api/backup/run
