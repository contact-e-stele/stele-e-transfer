# Stele DS App — P-Feature Übersicht (Master-Liste)

Diese Datei ist die **einzige Quelle der Wahrheit** für alle P-Themen (gebaut + Ideen).
Wird bei jeder neuen P-Nummer / jedem Status-Update aktualisiert. Backup zusätzlich auf Google Drive.

> Architektur/Code-Details (Diagramm, Routen, Schema) stehen in [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md) —
> diese Datei hier ist die Feature-/Roadmap-Sicht, nicht die technische Referenz.

## ✅ Gebaut & live — Features

| P | Feature | Details |
|---|---------|---------|
| P2 | Varianten-Bugfix | skuVariants ohne 'name'-Feld — Name aus Farbe/Größe zusammengebaut |
| P3 | EU-Default Lieferland | Standard Deutschland bei Import |
| P4 | China-Zoll-Logik | Pauschale bei shipsFrom=China (ab 01.07.2026 EU-Regel), aktuell 4,00€ (siehe `shared/constants.ts`) |
| P5 | shipsFrom-Feld | Schema + Migration, Versandland pro Produkt |
| P6 | eBay Kategorie-Suche | Taxonomy API + UI Quick-Lookup |
| P8 | Preise-Tab Grundfunktionen | Import, Preiskalkulation, Mindestgewinn |
| P12 | AliExpress-Direktlink | Button im Listings-Tab neben eBay/Bearb./Ende |
| P13 | Bestellungen-Tab | eBay Orders API, Rechnungs-PDF (pdf-lib), Tracking, Netto-Ergebnis, manueller Einkaufspreis |
| P14 | Google Drive Integration | OAuth, DB-Backup, Rechnungen-Upload, Bild/Datei-Proxy (Content-Type-Fix) |
| P30 | "Meine Shops" gruppiertes Dropdown | Chip-Liste ersetzt durch gruppiertes Dropdown nach Kategorie im Suche-Tab |
| P31 | Duplikat-Warnung beim Import | Warnt vor dem Import, wenn AliExpress-Item bereits in der DB existiert |
| **P66** | Lieferanten-Compliance | Schritt 1: Badge/Filter/Bearbeiten für Compliance-Status je Lieferant. Schritt 2: **Import-Gate** — regulierte Produktgruppen (Keyword-Matching) blockieren den Import hart, kein Bypass |
| P69 | Echte Versandkosten | Versandkosten werden beim AliExpress-Scraping (inkl. DS-API-Fallback-Pfad) tatsächlich abgefragt statt geschätzt, zentrales Feld statt Duplikation |
| P71 | eBay-Listing-Fehler behoben | Mehrere Ursachen gefixt: Set-/Mengen-Varianten als eigener Aspekt, EAN/GTIN-Handling ergänzt, genereller Offer-Cleanup + Retry/Backoff bei transienten eBay-Fehlern |
| P73 | Bewertungs-Ampel | Kombiniert Bewertungsanzahl + Sternebewertung im AliExpress-Import, schlechterer Wert gewinnt |
| P74/75 | ,95-Preisrundung + Sammel-Button | Rundet Varianten-Preisvorschläge auf ,95-Endung; ein Button setzt alle Varianten-Preisvorschläge auf einmal |
| P76 | Responsive UI | TabNav (Icons-only Tablet, Hamburger+Drawer Handy), Grid-Layouts, Varianten-Preistabelle, Bewertungs-Ampel — alles responsiv angepasst |
| — | Shops manuell + Suche-Dropdown | Manuelles Shop-Formular (Link+Name+Kategorie), Dropdown im Suche-Tab |
| — | GPSR-Handbuch/Zertifizierung-Upload | Upload läuft über `/api/upload-file` → Google Drive (lokaler Speicher nur Fallback) — löst die früher hier gelistete Idee |
| — | Bestellbenachrichtigung per E-Mail | `order-notifier.ts` + stündlicher GitHub-Actions-Cron (`order-check.yml`) — benachrichtigt den Shop-Betreiber (nicht den Käufer) bei neuen eBay-Bestellungen, dedupliziert über `notificationSentAt` |
| **P87** | PWA zeigt nach Update alte Inhalte | Kein Service Worker vorhanden (Prämisse "Caching-Problem durch SW" widerlegt) — Ursache war fehlender `Cache-Control`-Header: `index.html` konnte beliebig lange gecacht bleiben und zeigte dann auf längst ersetzte Vite-Asset-Hashes. Fix in `server.ts`: `index.html` immer `no-cache, no-store, must-revalidate`, `/assets/*` (hash-benannt) `immutable`-Langzeit-Cache |
| **P88** | Standard-Varianten-Gruppenname | Import-Tab: bei genau einer Varianten-Gruppe wird der von AliExpress gelieferte Name (oft pauschal "Color", auch bei Größen/Sets) beim Scrapen durch "Varianten" ersetzt — bei mehreren Gruppen unverändert. Nutzer kann wie bisher frei umbenennen |
| — | GPSR "Aus Zwischenablage einfügen" | Import-Tab: neuer Button neben "Vorlage einfügen"/"Löschen", liest `navigator.clipboard.readText()` und ersetzt das GPSR-Textfeld direkt (kein manuelles Löschen des Platzhaltertexts mehr nötig); klare Fehlermeldung bei verweigerter Berechtigung/fehlender Unterstützung |
| **P89** | EAN-Fehler (25002) bei Varianten-Listings behoben | `listOnEbayWithVariants()` schrieb den EAN/GTIN-Sentinel (P-71 Ursache 2) nur ins Inventory Item, nie in den zugehörigen Offer (`itemSpecifics` fehlte dort komplett, anders als im Einzelartikel-Pfad `createOffer()`) — eBay validiert beim `publish_by_inventory_item_group` aber den Offer, nicht das Inventory Item, daher Fehler unabhängig von Kategorie/Sentinel-Wert. Fix: dieselben Aspekte (inkl. EAN) jetzt auch im Varianten-Offer-Body. Live verifiziert: `stele-128` erfolgreich gelistet |
| **P90** | Selbstheilung für fehlende Pflichtmerkmale (Varianten-Publish) | Direkt nach P-89 live entdeckt: `stele-129` scheiterte am selben errorId 25002, aber für ein anderes Merkmal ("Produktart") — eBays Taxonomy-API meldet nicht zuverlässig jedes Kategorie-Pflichtfeld als "required". `publishOfferByInventoryItemGroup` läuft jetzt in einem Retry-Loop: liest eBays deutsche Fehlermeldung ("Das Artikelmerkmal X fehlt") per Regex aus, trägt X bei allen Offers nach, publisht erneut — bis zu 5 verschiedene fehlende Merkmale automatisch. **Nachbesserung noch am selben Tag:** erster Versuch nutzte "Nicht angegeben" als Fallback-Text — schlug bei `stele-129` weiterhin fehl, weil "Produktart" ein Auswahlfeld mit fester Werteliste ist (Freitext wird abgelehnt, exakt das gleiche Muster wie bei EAN in P-89). Fix: Heal-Loop schlägt jetzt den echten von eBay erlaubten Wert für das Merkmal nach (auch wenn es nicht als "required" gemeldet war) und nutzt Freitext nur, wenn der Aspekt tatsächlich keine Werteliste hat. Mit gemocktem Live-Aufruf verifiziert (Kategorie meldet "Produktart" absichtlich nicht als required, aber mit Werteliste ["Wäschebeutel","Sonstige"] — Heal-Loop trägt "Wäschebeutel" nach, nicht "Nicht angegeben") |
| **P91** | Universelle Pflichtfeld-Heilung (Einzelartikel + lesbare Fehlermeldung) | Verallgemeinert P-89/P-90 statt weiterer Einzelfall-Fixes: `publishWithAspectHealing()` als eine gemeinsame Funktion für **beide** Listing-Pfade (`listOnEbay()` Einzelartikel *und* `listOnEbayWithVariants()`, inkl. der 25005-Kategorie-Fallback-Schleife) — bisher hatte nur der Varianten-Pfad die P-90-Heilung. Zusätzlich neue `prettifyEbayError()`: wandelt eBays rohen JSON-Fehlerblob (der bisher unverändert im Produkte-Tab landete) in eine lesbare Meldung um — nennt bei einem verbliebenen 25002-Aspektfehler explizit das fehlende Feld, extrahiert sonst eBays `longMessage`/`message`-Text statt des ganzen JSON. Mit gemocktem Live-Aufruf verifiziert: Einzelartikel-Pfad heilt jetzt ebenfalls ein nicht-required gemeldetes Pflichtmerkmal automatisch; `prettifyEbayError()` getestet für Aspektfehler, generische eBay-Fehlermeldung und unbekanntes Format |
| **P92** | Heal-Loop: mehrere Kandidatenwerte statt einem Versuch pro Aspekt | Live-Fund direkt nach P-91: `stele-129` zeigte weiterhin exakt die neue lesbare P-91-Fehlermeldung für "Produktart" — die Heilung selbst griff nicht. Ursache war ein echter Bug, kein Kategorie-Sonderfall: (1) der Heal-Loop erlaubte pro Aspekt nur EINEN Versuch (`Set`, danach sofort aufgeben), obwohl `getAspectAllowedValue()` für "Produktart" gar keinen erlaubten Wert in der Taxonomy-Antwort fand und der Freitext-Fallback prompt abgelehnt wurde; (2) beim erneuten Versuch griff zusätzlich `if (!aspectsCopy[aspectName])` — der bereits (falsch) gesetzte Wert wurde nie überschrieben. Fix: pro Aspekt jetzt eine Kandidatenliste (echter erlaubter Wert → Identifier-Sentinel falls zutreffend → "Sonstige" als bei eBay verbreiteter Catch-all-Wert → Freitext), die der Reihe nach durchprobiert wird, mit unbedingtem Überschreiben statt einmaligem Setzen. Mit gemocktem Live-Aufruf reproduziert (exakt der stele-129-Fall: 1. Kandidat "Sonstige" abgelehnt, 2. Kandidat "Nicht angegeben" erfolgreich) und gegen den alten Code als Regressionstest bestätigt (schlägt dort exakt hier fehl) |
| **P96** | Zustellungs-Erkennung (P-85) verbreitert + Zeit-Fallback | Nutzer meldete: eBay zeigte 8+ Bestellungen als "Zugestellt", die Gmail-basierte Bewertungsbitte-Erkennung hatte dafür keine Entwürfe erstellt. Recherche: eBay liefert **keinen** abfragbaren Zustellstatus per API — die Sell Fulfillment API (`getOrders`/`getShippingFulfillment`) kennt nur, was wir selbst als Tracking eintragen; ein echter `DELIVERED`-Status existiert nur in der Post-Order-**Return**-API für Käufer-Rücksendungen, nicht für die ursprüngliche Lieferung. Also zwei Verbesserungen ohne neue API/Credentials: (1) `parseDeliveryEmail()` erkennt jetzt mehr Formulierungsvarianten ("wurde erfolgreich zugestellt", "ist zugestellt worden" statt nur der exakten Phrase "wurde zugestellt"), Gmail-Suchbegriff von der exakten Phrase auf das Wort "zugestellt" verbreitert, Zeitfenster 14→30 Tage; weiterhin ausgeschlossen bleibt die Vorstufe "wird zugestellt". (2) Neuer Zeit-Fallback in `/gmail/review-request-suggestions`: findet sich 21+ Tage nach dem lokal gesetzten `shippedAt` keine Zustellmail, wird trotzdem ein Vorschlag erzeugt (`source: "zeit-schaetzung"`, im Bestellungen-Tab als "wahrscheinlich zugestellt — bitte prüfen" von den mail-bestätigten unterschieden) — reiner Vorschlag, kein Automatismus. Mit Testskript verifiziert: alle Formulierungsvarianten erkannt, "wird zugestellt" weiterhin korrekt ausgeschlossen, gegen alten Code als Regressionstest bestätigt (neue Varianten schlagen dort fehl) |
| **P98** | eBay-Sendungsnummer-Bestätigung persistent nachverfolgen | Live-Fund (Phase-0-Rundgang, Bestellung Engel): App zeigte "Sendungsnummer übermittelt", eBay Seller Hub hatte gar keine hinterlegt. Root Cause: `PATCH /order-notes/:id` speichert trackingNumber immer lokal, auch bei fehlgeschlagenem eBay-Übermittlungsversuch — vorher nur ein 4-Sekunden-Toast, danach sah ein Fehlschlag in der DB exakt wie ein Erfolg aus, und P-86 (Danke-Nachricht) prüfte ausschließlich die lokale trackingNumber. Fix: neue Spalten `tracking_ebay_submitted`/`tracking_ebay_submitted_error` persistieren das tatsächliche Ergebnis (NULL = unbekannt/Altbestand, kein rückwirkendes Raten); persistente Warnung + "Erneut übermitteln"-Button im Bestellungen-Tab statt nur Toast; P-86-Trigger blockiert jetzt bei bekanntem Fehlschlag. Mit Testskript gegen die echte Hono-App verifiziert (Login + PATCH + GET, eBay-Call gemockt als Fehlschlag) und als Regressionstest bestätigt |
| **P97** | Zeitfallback-Schwelle 21→11 Tage gesenkt | Live-Fund: 8 laut eBay bereits zugestellte Bestellungen (2-8 Tage her) wurden von der P-96-Zeitschätzung noch nicht erfasst. Seller-Hub-Scraping als präzisere Alternative geprüft und bewusst nicht umgesetzt (Login-Session mit vollem Kontozugriff statt begrenztem API-Scope, eBay-ToS-Risiko, Wartungsaufwand — Nutzerentscheidung). Stattdessen risikofrei die Schwelle auf 11 Tage gesenkt, orientiert an der vom Nutzer bestätigten realen Lieferzeit ("10-12 Tage war bisher jede Sendung da"). Mit Testskript verifiziert (8-Tage-Sendung korrekt noch nicht vorgeschlagen, 12-Tage-Sendung korrekt vorgeschlagen) |
| **P99** | Warnung statt Bewertungsbitte bei möglicherweise verlorener Sendung | Live-Fund: zwei Bestellungen liefen seit ~3 Monaten nur "Etikett erstellt", nie "Zugestellt" — der Zeitfallback hätte trotzdem eine Bewertungsbitte vorgeschlagen. Da eBays echter Zustellstatus nicht abfragbar ist (siehe P-96/P-97), Ersatzsignal genutzt: 25+ Tage seit Versand ohne jede gefundene Zustellmail → Warnhinweis im Bestellungen-Tab ("bitte manuell prüfen, evtl. verloren") statt Bewertungsbitte-Vorschlag. Neues `warnings`-Array in der `/gmail/review-request-suggestions`-Response. Mit Testskript verifiziert (90-Tage-Sendung ohne Mail → Warnung statt Vorschlag; 15-Tage-Sendung weiterhin normaler Zeitfallback-Vorschlag) und als Regressionstest bestätigt |
| **P100** | shippedAt-Lücke bei Sendungsnummer-Flow ohne "Als verschickt markieren" | Live-Fund (finaler Phase-0-Rundgang): P-97-Zeitfallback griff nur bei Bestellungen mit explizitem "Als verschickt markieren"-Klick — nur dabei wurde `shippedAt` gesetzt. Bestellungen, bei denen nur die Sendungsnummer über den normalen Flow eingetragen wurde (z.B. aus P-84-Gmail-Vorschlag übernommen), bekamen nie `shippedAt`, wodurch der komplette Zeitfallback für sie dauerhaft übersprungen wurde (Live-Fund: Caner San u.a., seit 21 Tagen unterwegs, von eBay längst als "Zugestellt" bestätigt, aber nie ein Vorschlag/Warnung). Fix zweiteilig: schreibseitig setzt eine gespeicherte Sendungsnummer jetzt ebenfalls `shippedAt`, wenn noch nicht vorhanden; leseseitig nutzt die Zeitfallback-Route für Bestandsfälle (trackingNumber gesetzt, `shippedAt` weiterhin NULL) `updatedAt` als Näherung — kein DB-Backfill nötig. Mit Testskript gegen die echte Hono-App verifiziert und als Regressionstest bestätigt |
| **P101** | eBay-Bestellsync: 90-Tage-Limit durch expliziten Filter aufgehoben | Live-Fund: Bestellung Fred Seifert (~91 Tage alt) verschwand aus der App-Liste. Root Cause: eBays Sell-Fulfillment-API `getOrders` liefert ohne expliziten `creationdate`-Filter standardmäßig nur Bestellungen der letzten 90 Tage (offiziell dokumentiertes API-Verhalten, kein eigener Bug) — eine echte Lücke, die ältere unbearbeitete Bestellungen unbemerkt aus der Liste fallen lässt. Fix: expliziter `creationdate`-Filter bis 2 Jahre zurück (eBays erlaubtes Maximum) in `getAllOrders()`. Mit Testskript verifiziert (Aufruf ohne Filter schlägt im Mock fehl, mit Fix nachweislich vorhanden). **ROLLBACK 2026-09-03 (P102):** Filter löste einen Produktivausfall aus, siehe P102 |
| **P102** | Notfall-Rollback: "0 Bestellungen"-Komplettausfall nach P101 | Live-Meldung direkt nach P101-Deploy: Bestellungen-Tab zeigte "0 Bestellungen gefunden" statt 14 — kompletter Ausfall der Bestellliste. Root Cause zweiteilig: (1) der neue P101-`creationdate`-Filter wurde von eBay vermutlich abgelehnt, wahrscheinlich weil `encodeURIComponent` Strukturzeichen kodierte, die eBays Filter-Parser wörtlich erwartet; (2) ein bereits vorher vorhandener Bug in `getAllOrders()` verschluckte jeden eBay-API-Fehler stumm (`console.error` + `break` statt Fehler zu werfen) — dadurch war ein echter API-Fehler von "wirklich 0 Bestellungen" nicht mehr unterscheidbar. Fix: P101-Filter vollständig zurückgerollt (bewusst keine Blindkorrektur der Filter-Syntax ohne Live-Verifizierbarkeit), `getAllOrders()` wirft jetzt bei einer Fehlerantwort statt still abzubrechen — alle 5 Aufrufstellen haben bereits try/catch, die den echten Fehler jetzt sichtbar machen. Mit Testskript reproduziert (exakter "0 Bestellungen"-Bug gegen alten Code bestätigt, gegen neuen Code behoben). PR #64 |
| **P103** | Sendungsnummer-Bestätigungsstatus: NULL fälschlich als "in Ordnung" behandelt | Live-Fund (Bestellung Engel): App zeigte "Versendet" ohne Warnung, eBay Seller Hub hatte aber gar keine Sendungsnummer hinterlegt. Root Cause: die P98-Warnung prüfte nur `trackingEbaySubmitted === false` (bestätigter Fehlschlag) — der Zustand `NULL`/`undefined` (unbekannt, weil vor P98 eingetragen) wurde nie behandelt und damit stillschweigend wie "erfolgreich" dargestellt. Fix: Bedingung auf `!== true` geändert, neuer eigenständiger gelber Zustand "nie verifiziert — bitte prüfen" (statt des roten "eBay hat NICHT bestätigt"), damit Altbestand nicht fälschlich als Fehler alarmiert wird, aber trotzdem sichtbar zur Ein-Klick-Nachprüfung per bestehendem "Erneut übermitteln"-Button steht |
| **P104** | shippedAt-Fallback: `updatedAt` durch unveränderliches `orderDate` ersetzt | Live-Fund (Teil B der Sammel-Diagnose): 10 laut eBay längst zugestellte Bestellungen bekamen trotz P100 keinen Bewertungsbitte-Vorschlag. Root Cause: der P100-Fallback für Altbestand (trackingNumber gesetzt, `shippedAt` weiterhin NULL) nutzte `note.updatedAt` als Näherung — das wird aber bei JEDER `PATCH /order-notes/:id`-Änderung neu gesetzt, auch bei völlig unabhängigen Bearbeitungen (Notiz, Preiskorrektur, bloßes Anzeigen von Vorschlägen). Die Live-Tests am selben Tag setzten `updatedAt` bei mehreren überfälligen Bestellungen unbeabsichtigt auf "jetzt" zurück und ließen den Tage-Zähler nie starten. Fix: `order.orderDate` (von eBay, unveränderlich) statt `note.updatedAt` — eine Bestellung kann nicht vor ihrem Bestelldatum verschickt worden sein, das ist also eine sichere, durch keine Bearbeitung mehr verschiebbare untere Schranke. Reine Leselogik, kein DB-Backfill nötig. Mit Testskript verifiziert (Bestellung mit `orderDate` vor 20 Tagen, aber `updatedAt` durch eine unabhängige Bearbeitung auf "jetzt" zurückgesetzt, wird weiterhin korrekt erkannt) und als Regressionstest gegen den alten `updatedAt`-Code bestätigt |
| **P105** | Doppelte Bewertungsbitten vermeiden (eBay Trading-API GetFeedback) | Ergänzung zur Sammel-Diagnose (Teil C): vor jedem Bewertungsbitte-Vorschlag (Mail-Treffer *und* Zeitfallback) wird jetzt einmal per eBay Trading-API `GetFeedback` (`FeedbackType=FeedbackReceivedAsSeller`, gleiches XML/Header-Muster wie die bereits produktiv genutzten Trading-API-Calls `ReviseFixedPriceItem`/`GetItem`/`GetSellerList`) abgefragt, welche Käufer bereits bewertet haben; ein Treffer (Käufername + ItemID) unterdrückt den Vorschlag, um den Käufer nicht doppelt anzufragen. Best effort: schlägt der GetFeedback-Call fehl, wird einfach nicht gefiltert statt die ganze Route scheitern zu lassen (keine Live-Verifizierung der exakten XML-Antwortstruktur möglich, da developer.ebay.com in dieser Sandbox nicht erreichbar ist — Feldnamen per Recherche abgeleitet). Zentrale Workflow-Vorlage (P94, Phase 0/Schritt 4) um den entsprechenden Hinweis ergänzt (additiv, bestehender Text bleibt erhalten). Mit Testskript verifiziert (Käufer mit vorhandenem Feedback-Eintrag korrekt ausgeschlossen, anderer Käufer ohne Eintrag weiterhin vorgeschlagen) und als Regressionstest gegen den Code ohne GetFeedback-Prüfung bestätigt |
| **P106** | Sendungsnummer-Verwechslung Hoffmann/Engel + P-84-Root-Cause (Landkreis statt Gemeinde) | Live-Fund: eBay lehnte die Sendungsnummer-Übermittlung für Heinrich Engel (02-15114-51502) mit "Tracking number already used" ab — dieselbe DHL-Nummer (00340434886287887677) war bereits bei Karl Hoffmanns Bestellung (24-15076-13627) hinterlegt (beide Bestellungen: identischer Artikel "Lebensmittelhaube", nur andere Varianten-Packgröße). Per Gmail-Originalmails (eBay-Verkaufsbestätigung + AliExpress-Logistik-Mails) zweifelsfrei geklärt: die Nummer gehört zu Hoffmann (Adresse "Mittelgasse 5" — exakte Übereinstimmung, Paket bereits am 2026-09-01 zugestellt); Engels tatsächliche Sendung läuft unter einer ANDEREN Nummer (00340434886288250852, AliExpress-Bestellung 3076033233417211, Adresse "Im Steinbühlsfeld 8" — exakte Übereinstimmung, Stand 2026-09-01 noch unterwegs). Root Cause für Teil 3 (P-84-Bug-Check): `addressMatchesEmail()`s Ortsprüfung ist eine strikte Textgleichheit — AliExpress meldet im "Ort"-Feld bei kleineren Gemeinden aber manchmal den übergeordneten LANDKREIS statt der Gemeinde selbst (bestätigt an beiden Fällen: "Suedwestpfalz" statt "Eppenbrunn", "Vogelsbergkreis" statt "Alsfeld-Liederbach"). Das führte NICHT zu einer falsch automatisch vorgeschlagenen Zuordnung (der automatische Abgleich lieferte für beide Bestellungen 0 Treffer, P-84s Sicherheitsprinzip griff korrekt) — sondern dazu, dass gar kein automatischer Vorschlag zustande kam und stattdessen riskante manuelle Eingabe nötig wurde, bei zwei nahezu identischen Bestellungen genau das Risiko, das zur Verwechslung führte. Fix: neue `addressMatchesEmailByStreetOnly()` (Straße+Hausnummer, optional Telefon — ohne Ort-Gate) als Fallback an beiden Aufrufstellen (`/gmail/tracking-suggestions`, `/gmail/review-request-suggestions`), aber NUR genutzt, wenn die strikte Prüfung 0 Treffer liefert, und weiterhin nur bei genau einem eindeutigen Treffer übernommen (gleiches "0 oder mehrere → nichts vorschlagen"-Prinzip wie zuvor). Datenkorrektur der beiden betroffenen Bestellungen konnte aus dieser Sandbox nicht selbst vorgenommen werden (kein Live-DB-/Netzwerkzugriff, per Proxy-Test bestätigt) — **am 2026-09-05 vom Nutzer live durchgeführt und bestätigt**: Engel bekam die korrekte Sendungsnummer, Hoffmanns Nummer wurde erfolgreich erneut an eBay übermittelt, beide Bestellungen zeigen keine Warnung mehr. Mit Testskript reproduziert (exakt der Landkreis-vs-Gemeinde-Fall) und als Regressionstest gegen den Code ohne Fallback bestätigt (schlägt dort fehl) |
| **P107** | Zweite P-84/P-85-Root-Cause: AliExpress-Mails teils komplett auf Englisch statt Deutsch | Bei der Live-Verifizierung von P106 (echte Gmail-Inhalte für Hoffmanns Bestellung per Gmail-Lesezugriff geladen, nicht nur Adressdaten) zusätzlich entdeckt: AliExpress verschickt dieselben Logistik-Mail-Typen je nach Bestellung auf DEUTSCH ODER ENGLISCH (Locale offenbar pro Bestellung verschieden — Hoffmanns komplette Mail-Kette lief auf Englisch: "Package X: left the departure region", "Package X has been delivered", Anschriftsblock "Ship to" statt "Versand nach:"; Engels Kette dagegen komplett auf Deutsch). `parseTrackingEmail()`, `parseDeliveryEmail()` und `parseVersandNachBlock()` erkannten bisher AUSSCHLIESSLICH die deutschen Formulierungen — englische Mails wurden komplett verpasst (weder Betreff-Regex noch Adress-Anker passten), unabhängig vom P106-Ortsproblem. Mit dem ECHTEN Produktiv-HTML von Hoffmanns "has been delivered"-Mail verifiziert: `parseDeliveryEmail()` lieferte vor dem Fix `null`, obwohl Sendungsnummer, Adresse und Datum vollständig in der Mail enthalten waren. Fix: Betreff-Regex und Adress-Anker (`Ship to` zusätzlich zu `Versand nach:`) um die englischen Varianten ergänzt (Departure, Delivered, inkl. "is out for delivery" als Vorstufe analog zu "wird zugestellt"), serverseitiger Gmail-Suchbegriff auf eine OR-Gruppe beider Sprachen erweitert. Verifiziert direkt gegen das reale, per Gmail-Lesezugriff geladene HTML aller 3 relevanten Hoffmann-Mails (Abflug/Zustellung/Vorstufe) UND gegen Engels reale deutsche Mail (Regressionsschutz — weiterhin korrekt erkannt) sowie als Regressionstest gegen den Code ohne die Sprach-Erweiterung bestätigt (schlägt dort exakt hier fehl) |
| **P95** | Manuelles Eingabefeld für nicht automatisch heilbare eBay-Pflichtfelder | (Im Auftrag als "P-88" bezeichnet, dann als P-93 dokumentiert — beide Nummern waren inzwischen von parallelen Sessions vergeben (P-93: Verfügbarkeits-Monitor + AliExpress-Rechnungs-Dateinamensschema; P-94: Workflow-Text editierbar), hier nach Rebase auf aktuellen `main`-Stand als P95 fortlaufend dokumentiert.) Ergänzt P-91/P-92: wenn selbst der Heal-Loop kein Pflichtfeld befüllen kann, landet der exakte Feldname jetzt separat in `products.ebay_missing_aspect` (statt nur in der lesbaren Fehlermeldung). Das Produkte-Tab zeigt dafür direkt ein Eingabefeld — lädt eBays erlaubte Werteliste per Taxonomy API nach (`GET /ebay/aspect-options/:id?aspect=…`) und zeigt ein Dropdown, sonst ein Freitextfeld. Gespeicherter Wert landet in neuer Spalte `products.manual_aspects` (JSON) und wird von `buildAspects()` beim nächsten Listen-Versuch als unbedingter Override eingesetzt — noch vor jedem Auto-Default und vor dem Heal-Loop, sodass das Feld gar nicht erst als fehlend gemeldet wird. Mit gemocktem Live-Aufruf verifiziert (Kategorie meldet "Produktart" gar nicht als Aspekt — ohne `manualAspects` würde das Feld fehlen; mit `manualAspects: {"Produktart":"Tasche"}` erscheint es korrekt im Inventory-Item *und* im Offer-Body) und als Regressionstest gegen den alten Code bestätigt (schlägt dort exakt hier fehl, da `manualAspects` dort noch nicht existierte) |

## 🔧 Technische Wartung / Bugfixes (P-Nummern ohne eigenen Feature-Charakter)

| P | Was |
|---|-----|
| P7 | Preis-Monitor-Formel an `lieferanten.tsx` angeglichen |
| P9/P11/P13 (Sammel-Commit) | Varianten-Ausschluss bei Preis-Neuberechnung, ,95-Rundung + Update-Schwellenwert, shipsFrom-Fix — **Nummernkollision**, siehe Hinweis unten |
| P14 | Variations-Listings zuverlässig erkennen (nicht per Trading API überschreiben) |
| P15 | `bulk/price` nutzt `updateEbayPriceTrading()` statt eigener Trading-API-Logik |
| P18 | Drive-OAuth-Callback + Bild-Proxy von Session-Auth ausgenommen |
| P23 | Preis-Monitor auf 8h-Intervall, Backup-Scheduler auf 2×/Tag reduziert (Ressourcenverbrauch) |
| P25 | Eigener Backend-Typecheck eingerichtet |
| P32 | Lücke im Backup geschlossen — `trusted_suppliers` fehlte, ist jetzt vollständig enthalten |
| P35, P50, P51 | Kleinere Fixes: fehlender Import, Frontend-Typecheck-Skript-Pfad, unbenutzte Imports/Variablen entfernt |

### ⚠️ Hinweis zur P-Nummerierung
**P-11 ist doppelt vergeben.** Als Idee (Tabelle unten) steht P11 für "Promoted Listings/Anzeige-Rate"
(weiterhin ungebaut — verifiziert, `setAdRate()` in `ebay.ts` nutzt unverändert das von eBay
ignorierte `AdvancedMarketing/BidPercentage`-Feld). In einem Commit wurde P-11 zusätzlich für die
,95-Preisrundung/Update-Schwellenwert verwendet (siehe Wartungstabelle oben). Beide Verwendungen
bleiben hier stehen, statt eine davon stillschweigend zu überschreiben — bei der nächsten neuen
P-Nummer nicht wieder 11 vergeben.

## 💡 Ideen / noch NICHT gebaut

| P | Feature | Details / Warum wichtig |
|---|---------|--------------------------|
| P1 | *(nie vergeben)* | — |
| P7 | *(nie vergeben — Nummer ist durch Wartungs-Commit belegt, siehe Tabelle oben)* | — |
| **P9** | Trackerbot-Ersatz | Tracking-Konvertierung Amazon/AliExpress→eBay + automatische **Käufer**-Nachrichten (Bestellstatus, Bewertungsanfrage). Trackerbot gekündigt am 2026-07-02, macht User aktuell manuell. **Nicht zu verwechseln** mit der bereits gebauten Bestellbenachrichtigung — die geht an den Shop-Betreiber, nicht an den Käufer |
| P10 | *(nie vergeben)* | — |
| **P11** | Promoted Listings / Anzeige-Rate | eBay Marketing API nötig — `AdvancedMarketing/BidPercentage` wird von eBay ignoriert, echte Anzeigen-Rate (5%) kommt aktuell nicht an. Verifiziert weiterhin offen (Stand 2026-08-19) |
| — | Preisaktualisierung Cron-Job | **Größtenteils gebaut** (`price-monitor.ts`: scrapt AliExpress-Preis alle 8h, vergleicht mit DB, passt eBay-Preis inkl. Marge-Logik automatisch an). Offen bleibt nur der ursprünglich geplante **Alert bei Änderung** — aktuell nur ein visuelles Flag (gelber Rahmen in Produkte-Tab), keine aktive Benachrichtigung |
| — | Vorschau-Modal editierbar | **Teilweise gebaut**: Titel ist im Produkte-Tab per Klick inline editierbar. Beschreibung/Bullets sind weiterhin nicht direkt im Vorschau-Modal änderbar |
| — | EU-Lager-Filter bei AliExpress-Suche | **Teilweise gebaut**: Such-Dropdown hat jetzt mehrere Länder (DE, ES, FR, IT, PL, NL, CZ, CN, Alle) statt nur Deutschland — aber weiterhin manuelles Single-Select, kein automatischer "nur EU-Lager"-Filter |
| — | Zweites eBay-Konto für Temu | Getrennt vom Haupt-Konto stele-e-transfer, wegen unzuverlässigerer Lieferzeiten |

## Bekannte technische Einschränkungen
- Playwright/Chromium auf Render Free Tier: ETXTBSY-Fehler → ScrapingAnt als Fallback aktiv, PDF-Generierung läuft über pdf-lib (kein Browser nötig)
- Gemini 2.5 Flash Lite: gelegentlich überlastet → Fallback-Beschreibung greift automatisch
- GPSR structured fields: `ae_store_info` liefert nur den Shop-Namen, Hersteller-Adressdaten werden nicht automatisch geparst — GPSR-Felder müssen weiterhin manuell im Produkte-Tab gepflegt werden

---
*Letzte Aktualisierung: 2026-09-03 — P-102 (Notfall-Rollback "0 Bestellungen"), P-103 (NULL-Status bei Sendungsnummer-Bestätigung), P-104 (shippedAt-Fallback auf orderDate umgestellt), P-105 (GetFeedback-Prüfung gegen doppelte Bewertungsbitten), P-106 (Sendungsnummer-Verwechslung Hoffmann/Engel + P-84-Landkreis-Fallback), P-107 (AliExpress-Mails teils Englisch statt Deutsch — P-84/P-85-Parser bilingual) ergänzt*
