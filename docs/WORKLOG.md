# WORKLOG

Hinweis: docs/WORKLOG.md existierte auf `main` noch nicht (nur auf dem unabhaengigen, noch nicht gemergten Branch `fix/p69-p74-versand-rundung`). Die vollstaendige Vorgeschichte (Import-Flow-Analyse, Pruefauftrag Preislogik) liegt dort und in docs/analyse-import-flow.md auf diesem Branch. Dieser Eintrag beginnt den Log fuer den Branch `fix/preislogik-vereinheitlichung`.

## 2026-09-13 - Claude Sonnet 5 (Session 01CHnbcFwoiXkb658VwALM8H)
**Claim:** Fix-Auftrag Preislogik vereinheitlichen. Branch fix/preislogik-vereinheitlichung (isoliertes Worktree, Basis origin/main), Draft-PR, kein Merge, kein Push auf main. Aufgabe 1: eine gemeinsame roundToNearest95 in shared/, ersetzt roundUpToX95 (price-monitor.ts), lokale roundToNearest95 (lieferanten.tsx) und die reinen Cent-Rundungen (lieferanten.tsx:1542, api/index.ts:2308). Aufgabe 2: Haupt-Gewinn in lieferanten.tsx:412-421 zieht kuenftig Versand+Zoll ab (wie varProfit/wahrerEinkaufV). Aufgabe 3: Einzelprodukt-Preisempfehlung (lieferanten.tsx) und api/index.ts:2308 rufen calcSellPrice statt eigener Formel auf. Keine USt-Logik, kein MIN_GEWINN_EUR-Change, kein Zielgewinn/target_margin_eur, keine DB-Migration. Pflicht: Typecheck + 3 Testfaelle zeigen.
Abweichung von der woertlichen Vorgabe (mit Nutzer per AskUserQuestion abgestimmt, bevor Code geschrieben wurde): calcSellPrice zieht NICHT in price-monitor.ts um-bleibt, sondern wandert zusammen mit roundToNearest95 nach packages/web/src/shared/pricing.ts, weil price-monitor.ts beim Import Server-Nebenwirkungen ausloest (DB-Client-Aufbau, Sentry) die im Frontend-Bundle nicht laufen koennen. price-monitor.ts reexportiert calcSellPrice von dort, damit bestehende Backend-Aufrufer unveraendert bleiben.
**Korrektur zur vorherigen Zeile:** durch den zweiten Fund (siehe unten) hat sich diese erste Entscheidung erledigt — shared/pricing.ts existierte auf origin/main bereits (viel weiter entwickelt: computeMinSellPrice/profitAtSellPrice/DEFAULT_PRICING_CONFIG statt einer einfachen calcSellPrice). Ich musste nichts verschieben, nur den bereits bestehenden Import/Aufruf verwenden. Stehen bleibt hier zur Nachvollziehbarkeit, war aber gegen den falschen (veralteten) Ausgangszustand entschieden.
**Fund (vor Implementierung, aendert die Aufgabe):** Der Fix-Auftrag basiert auf einer Analyse des alten Branches `fix/p69-p74-versand-rundung`, dessen Basis-main veraltet war. `origin/main` enthaelt bereits ein weit fortgeschrittenes "Preis-Fundament" (PRs #85-#89, "Teil 2A-2D + Teil 3"): `packages/web/src/shared/pricing.ts` existiert bereits mit `computeMinSellPrice()`, `profitAtSellPrice()`, `DEFAULT_PRICING_CONFIG` (eBay-Gebuehr real gemessen: 15% + 0,30€ statt der alten 13%/0,45€-Annahme), `roundToNearest95`/`roundUpToX95`, Senkungsbremse, Varianten-Preisplanung. Dieses System wird bereits von price-monitor.ts, api/index.ts (mehrere Stellen) und produkte.tsx genutzt. NICHT migriert wurde jedoch `lieferanten.tsx` (der Import-Tab, genau die Datei aus diesem Fix-Auftrag) — dort stehen weiterhin 3 Kopien der ALTEN Formel (13%/0,45€, inline): Hauptpanel-Gewinn (jetzt Zeile 429-437, zieht weiterhin nur `einkauf` ab, kein Versand/Zoll — Bug aus Aufgabe 2 besteht unveraendert), Varianten-Gewinn `varProfit` (Zeile ~1814, korrekt mit wahrerEinkaufV, aber alte 13%/0,45€-Rate) und die Einzelprodukt-Preisempfehlung (eigene Formel, alte Rate). Widerspruch zur woertlichen Auftrags-Vorgabe: folge ich Grundgesetz-Regel 8 ("nicht duplizieren, extrahieren") und binde lieferanten.tsx an das BEREITS BESTEHENDE shared/pricing.ts an, aendern sich die Zahlen nicht nur durch den Versand/Zoll-Abzug, sondern zusaetzlich durch die neue 15%/0,30€-Rate — die im Auftrag vorgegebenen Testfaelle (calcSellPrice(5,1.5,0,5)=11.95€, Gewinn≈2.35€) beruhen auf der ALTEN 13%/0,45€-Rechnung und treffen unter der neuen Rate nicht mehr zu. Frage zurueck an Nutzer gestellt (noch keine Antwort verarbeitet), bevor weitercodiert wird.
**Zweiter Fund + Entscheidung:** Aufgabe 1/3 sind fuer lieferanten.tsx bereits umgesetzt — Einzelbutton und Varianten-Preisempfehlung rufen bereits `computeMinSellPrice()` aus shared/pricing.ts auf (nicht mehr die alte Inline-Formel). Einziger Unterschied zur woertlichen Auftrags-Vorgabe: der Einzelbutton nutzt bewusst `rounding: 'cent'` statt `'nearest95'` — dokumentiert in task.md als bewusste, bereits regressionsgetestete Entscheidung ("Import-Preisvorschlag bewusst UNVERAENDERT gelassen"). Nachgerechnet (bun, echte Funktion): bei Einkauf 5/Versand 1,50/Zoll 0/adRate 5 empfiehlt der Button aktuell 11,63€ (cent), mit nearest95 waeren es 11,95€. Nutzer entschied: cent-Rundung NICHT anfassen. Aufgabe 1/3 gelten damit als bereits erledigt, keine weitere Aenderung noetig. Verbleibende Arbeit ausschliesslich Aufgabe 2: Hauptpanel-Gewinn (Zeile 429-436) und Varianten-Gewinn `varProfit` (Zeile ~1814) nutzen noch die alte Inline-Formel (13%/0,45€, Hauptpanel zusaetzlich ohne Versand/Zoll-Abzug) statt der bereits existierenden `profitAtSellPrice()`. Nachgerechnet: Gewinn bei VK 11,95€ unter der NEUEN 15%/0,30€-Formel = 2,25€ (nicht 2,35€ wie im urspruenglichen, auf der alten Formel basierenden Testfall).
**Ergebnis:** Fertig (nur Aufgabe 2 noetig, s.o.). Geaenderte Dateien: `packages/web/src/web/pages/lieferanten.tsx` (Hauptpanel-Gewinn + Varianten-Gewinn `varProfit` nutzen jetzt `profitAtSellPrice()`/`DEFAULT_PRICING_CONFIG` statt Inline-Formel; zwei Anzeige-Labels von hartcodiert "13%" auf `DEFAULT_PRICING_CONFIG.ebayFeeRatePercent` umgestellt), `packages/web/src/shared/pricing.test.ts` (ein neuer Test, der den Fix-Auftrags-Testfall mit den echten, aktuellen Konstanten nachrechnet).
Typecheck Frontend (`bun run typecheck`): gruen, keine Ausgabe/Fehler.
Typecheck Server (`bun run typecheck:server`): gruen, keine Ausgabe/Fehler.
Tests (`bun test src/shared/pricing.test.ts`): 54 pass, 0 fail, 147 expect() calls.
Voller Testlauf (`bun test`, alle 5 *.test.ts-Dateien): 87 pass, 2 fail, 2 errors — die 2 Fehler sind VORBESTEHEND und NICHT durch diese Aenderung verursacht (bestaetigt: nur pricing.test.ts + lieferanten.tsx geaendert, price-monitor.test.ts/variant-sales.test.ts/aliexpress.test.ts unangetastet). Ursache: `ConnectionFailed("Unable to open connection to local database /tmp/aliexpress-test.db: 14")` — ein Windows-spezifisches Pfadproblem des lokalen libsql-Test-Fixtures (kein echtes `/tmp` auf diesem Windows-Rechner), unabhaengig von Preislogik.
Testfaelle aus dem Auftrag (mit echten, aktuellen Konstanten nachgerechnet, nicht geraten):
- calcSellPrice(5,1.5,0,5)-Aequivalent (computeMinSellPrice, cent-Rundung, wie der tatsaechliche Einzelbutton) liefert **11,63€** (nicht 11,95€ — die alte Zahl passte nur zur inzwischen ersetzten 13%/0,45€-Rate).
- roundToNearest95(2.10)=1.95 und roundToNearest95(2.60)=2.95: bereits vorher in shared/pricing.ts/pricing.test.ts abgedeckt (Zeilen ~36-39 bzw. Rundungs-Tests), durch diesen Fix nicht veraendert.
- Hauptpanel-Gewinn bei Einkauf 5/Versand 1,50/Zoll 0/adRate 5/Verkauf 11,95: **2,25€** (2,2489, gerundet — nicht 2,35€ wie urspruenglich angenommen, und nicht mehr 3,85€ wie vor dem Fix).
Bewusst NICHT angefasst (Scope-Grenzen, mit Nutzer abgestimmt bzw. aus dem Auftrag): Rundung des Einzelbuttons bleibt `'cent'` (nicht `'nearest95'`) — bestehende, regressionsgetestete Entscheidung. Platzhalter-Text in der Varianten-Eingabe (`lieferanten.tsx` ~Zeile 1865, "min. ..."-Hinweis) behaelt seine eigene, aeltere Naeherungsformel (0,54/1,6 statt der zentralen Konstanten) — reiner Hinweistext, kein gespeicherter/berechneter Preis, nicht Teil der 3 Aufgaben. Zusaetzlicher Fund (nicht behoben, ausserhalb des Auftrags): `packages/web/src/web/pages/dashboard.tsx:138` hat dieselbe Bug-Klasse (alte 13%/0,45€-Formel, kein Versand/Zoll-Abzug) — separate Datei, nicht Teil dieses Fix-Auftrags.
Keine USt-Logik eingefuehrt, MIN_GEWINN_EUR unveraendert (per grep bestaetigt), kein Zielgewinn-Eingabefeld/target_margin_eur neu genutzt (nur bereits bestehende `minGewinn`/`targetMarginEur`-Wiring unangetastet), keine DB-Migration, `AUTO_PRICE_WRITE_ENABLED` kommt in lieferanten.tsx gar nicht vor (per grep bestaetigt) — Aenderung betrifft ausschliesslich die manuelle Import-UI, keinen automatischen Schreibpfad.
Branch: `fix/preislogik-vereinheitlichung` (isoliertes Worktree unter `.worktrees/`, Basis `origin/main`). PR: https://github.com/contact-e-stele/stele-e-transfer/pull/94 (Draft, nicht gemergt).

## 2026-09-13 - Claude Sonnet 5 (Session 015gE9qAiHZeSnCeCuPykuPo)
**Claim:** Fix-Auftrag Herkunft-Umschalter im Preise-Tab + Gewinn-Anzeige im Dashboard. Branch `fix/herkunft-toggle-und-dashboard-gewinn` (Basis aktueller `origin/main`, faengt PR #94 bereits ein), Draft-PR, kein Merge. Aufgabe 1: Herkunft-Umschalter ("EU/Inland" vs. "China") im Preise-Tab reagiert laut Live-Test (v1.11) nicht — erst reproduzieren, dann nur bei Reproduktion minimal fixen; pruefen ob State ueberhaupt gesetzt wird, ob er in `isChinaOrigin`/Kalkulation einfliesst, und ob Lieferant "AliExpress" ihn bei jedem Render zurueckschreibt. Aufgabe 2: Gewinn-Anzeige in `dashboard.tsx` (~Zeile 138, bereits als dieselbe Fehlerklasse wie der PR-#94-Bug identifiziert) auf `profitAtSellPrice()` aus `shared/pricing.ts` umstellen; zusaetzlich alle weiteren selbstrechnenden Gewinn/Marge-Stellen im Frontend nur AUFLISTEN, nicht mitaendern. Keine Aenderung an `DEFAULT_PRICING_CONFIG`/Gebuehrensaetzen, `AUTO_PRICE_WRITE_ENABLED` bleibt `false`, keine DB-Migration, keine Auth-Aenderung. Pflicht: Frontend-/Server-Typecheck, volles `pricing.test.ts`, Vorher/Nachher-Wert von `isChinaOrigin` nach Klick, Zahlenbeispiel Dashboard-Gewinn (~2,25€ erwartet).
Hinweis zum Arbeitsablauf: Der Branch `fix/p69-p74-versand-rundung` (vorherige Session) hatte unbeglaubigte, unfertige Sentry-Monitoring-Aenderungen im Arbeitsverzeichnis (nicht committet). Diese wurden NICHT geloescht, sondern per `git stash` gesichert (`stash@{0}`, Nachricht "Sentry monitoring WIP..."), damit dieser Fix-Auftrag auf einer sauberen, aktuellen `main`-Basis beginnen kann. Eine zufaellige Muell-Datei (`less`-Hilfetext, Artefakt einer frueheren Pager-Fehlkonfiguration) im Projekt-Wurzelverzeichnis wurde geloescht (kein Nutzer-Code, nur Terminal-Ausgabe-Reste).

**Ergebnis Aufgabe 1 (Herkunft-Umschalter):** NICHT reproduzierbar. Lokal (vite dev, ephemere Test-Login-Daten nur als Shell-Env-Var, nicht in `.env` geschrieben) mit den exakten Werten aus der Meldung (Einkauf 5,00/Versand 1,50/Lieferant AliExpress/Listenpreis 11,95) reagiert der Button "EU / Inland" (`packages/web/src/web/pages/index.tsx:402-406`) sofort: `ausChina` (State, `index.tsx:109`) wechselt von `true` auf `false`, "Wahrer Einkauf" fällt korrekt von 10,50€ auf 6,50€ (Screenshot-Beleg, Gewinn wechselt von -1,04€ auf +2,96€, Ampel von Rot auf Grün). Die Zuordnung `onClick={() => setAusChina(false)}` (Zeile 402) ist einfacher React-State, fliesst direkt in `zollVal`/`wahrerEinkauf` (Zeilen 182-184) ein — kein `useEffect`, kein Reset-Pfad, der `ausChina` bei jedem Render überschreibt. Der einzige Schreibzugriff ausser den beiden Buttons ist der Lieferant-Dropdown (`onChange`, Zeile 341-347: `setAusChina(val === "aliexpress")`) — der feuert nur bei einer tatsächlichen Auswahländerung, nicht bei jedem Render, und wurde in der Reproduktion nicht erneut ausgelöst. `git log -S"ausChina"` zeigt: dieser State/seine Verdrahtung wurde nur einmal geschrieben (Commit `fcb8b07`, Feature-Einführung) und seitdem nie mehr geändert — auch nicht durch die beiden Preis-Fundament-Refactors vom 10.09. (`8b21604`, `b6ee952`). Moegliche Gruende fuer die Live-Beobachtung, OHNE dass ich sie verifizieren konnte: (a) der Browser des Testers hatte einen aelteren JS-Bundle im Cache (kein Service Worker im Code gefunden, aber Browser-Caching auf Asset-Ebene bleibt moeglich); (b) das aktuell deployte Render-Release liegt zeitlich vor einem der Commits, die dieses genaue Verhalten ausmachen (`render.yaml` baut zwar bei jedem Deploy frisch aus dem Quellcode — das Problem waere dann nicht ein alter Build, sondern schlicht: seit `b6ee952`/dem heutigen Merge von PR #94 wurde noch nicht neu deployt); (c) eine Bedienreihenfolge, die ich nicht nachstellen konnte (z.B. Lieferant-Dropdown erneut ausgewaehlt NACH dem Klick auf "EU/Inland", was `ausChina` ueber Zeile 346 wieder auf `true` zuruecksetzen wuerde — das waere aber kein Bug, sondern die dokumentierte Absicht "Lieferant AliExpress -> Herkunft automatisch auf China"). Kein Code geaendert (Auftrags-Vorgabe bei Nicht-Reproduzierbarkeit).

**Ergebnis Aufgabe 2 (Dashboard-Gewinn):** Bug bestaetigt und behoben. `packages/web/src/web/pages/dashboard.tsx:138` (jetzt migriert) berechnete Gewinn mit der ALTEN Inline-Formel (13%/0,45€, `p.sellPrice! - p.sellPrice! * (13 + adR) / 100 * 1.19 - 0.45 * 1.19 - p.buyPrice!`) — ohne jeden Versand-/Zoll-Abzug. Migriert auf `profitAtSellPrice()` aus `shared/pricing.ts`: dazu `Product`-Interface um `shippingCost`/`shipsFrom` ergaenzt (Spalten existieren bereits in der DB, `schema.ts:26,53`, und werden von `/api/products` bereits per `...p`-Spread zurueckgegeben — im lokalen Frontend-Interface fehlten sie nur), `isChinaShipping(p.shipsFrom)` bestimmt `isChinaOrigin`, `p.shippingCost ?? 0` die Versandkosten, `DEFAULT_PRICING_CONFIG.chinaCustomsFlatEur` die Zollpauschale. Zahlenbeispiel aus dem Auftrag (Einkauf 5,00/Versand 1,50/Zoll 0/adRate 5/Verkauf 11,95) nachgerechnet: **2,2489€** (vorher mit der alten Formel waeren es 3,8548€ gewesen) — neuer Regressionstest in `pricing.test.ts` ("dashboard.tsx Gesamtgewinn/Ø-Gewinn ...") belegt beide Werte. Wichtiger Befund nebenbei: `dashboard.tsx` (Komponente `Dashboard`) ist aktuell in `app.tsx`/`main.tsx` NICHT verlinkt/geroutet — kein Tab, kein Menüpunkt zeigt darauf, sie ist toter Code. Der Fix ist damit korrekt, hat aber erst eine sichtbare Auswirkung, sobald/falls die Komponente irgendwo eingebunden wird.

**Weitere selbstrechnende Gewinn/Marge-Stellen im Frontend (nur aufgelistet, NICHT geaendert, wie beauftragt):**
1. `packages/web/src/web/pages/produkte.tsx:1615` — Varianten-Gewinnspalte in der aufklappbaren "Varianten-Preise"-Tabelle im Produkte-Tab (LIVE erreichbar, nicht toter Code wie dashboard.tsx): identische alte Formel `ebayP - ebayP*(13+adR)/100*1.19 - 0.45*1.19 - v.price` — 13%/0,45€ statt DEFAULT_PRICING_CONFIG, kein Versand-/Zoll-Abzug (nur `v.price`, der reine Varianten-Einkauf).
2. `packages/web/src/web/pages/produkte.tsx:111-134` (Funktion `PriceBadge`) — nutzt zwar bereits `computeMinSellPrice()`/`DEFAULT_PRICING_CONFIG` fuer den Gebuehrensatz (kein eigenes 18%-Literal mehr, Teil-2B-Kommentar in der Datei bestaetigt das), berechnet die Marge danach aber weiterhin selbst inline (`(sell - buy - ebayFee) / sell * 100`) statt `profitAtSellPrice()` aufzurufen — und zieht ebenfalls keinen Versand/Zoll ab.
3. `packages/web/src/web/pages/lieferanten.tsx:1865` — bereits aus der vorherigen Session (PR #94) bekannt/dokumentiert: reiner Platzhalter-Hinweistext ("min. ...") im Varianten-Eingabefeld mit eigener Naeherungsformel (0,54/1,6-Konstanten), aber kein gespeicherter/berechneter Preis — kein irrefuehrender Gewinn-Wert, nur ein Textvorschlag. Erneut hier gelistet der Vollstaendigkeit halber, keine neue Erkenntnis.

Tests: Frontend-Typecheck (`bun run typecheck`) gruen, Server-Typecheck (`bun run typecheck:server`) gruen, `bun test src/shared/pricing.test.ts`: 55 pass/0 fail/150 expect() (neuer Test fuer den Dashboard-Fix eingerechnet). Voller `bun test`: 97 pass/2 fail/2 errors — dieselben 2 vorbestehenden, nicht durch diese Aenderung verursachten Windows-`/tmp`-DB-Fehler wie in der PR-#94-Session dokumentiert (unveraendert, nur andere absolute Zahl weil inzwischen mehr Tests im Repo sind).
Keine Aenderung an `DEFAULT_PRICING_CONFIG`/Gebuehrensaetzen, `AUTO_PRICE_WRITE_ENABLED` bleibt `false` (in dashboard.tsx gar nicht referenziert, per grep bestaetigt), keine DB-Migration, keine Auth-Aenderung.
Branch: `fix/herkunft-toggle-und-dashboard-gewinn`. PR: https://github.com/contact-e-stele/stele-e-transfer/pull/95 (Draft, nicht gemergt).

## 2026-09-13 - Claude Sonnet 5 (Session 01HErHzTkH9mCbh79g7rpuVD)
**Claim:** Auftrag "Freigabe der automatischen Preisschreibung vorbereiten". Branch `feat/auto-price-write-freigabe` (isoliertes Worktree unter `.worktrees/`, Basis `origin/main`), Draft-PR, kein Merge. Aufgabe 1: Trockenlauf-Bericht ueber alle aktiven Produkte (SKU, Titel, heutiger/neuer Preis, Differenz, Gewinn heute/neu), sortiert nach groesster Senkung, ohne DB-Schreibzugriff und ohne eBay-Call, nach `docs/preis-trockenlauf-2026-09-13.md`. Aufgabe 2: `AUTO_PRICE_WRITE_ENABLED` in `shared/pricing.ts` auf `true` setzen + Kommentar aktualisieren (Freigabe-Begruendung: 6 echte eBay-Transaktionen im Verkaeufer-Cockpit gegengeprueft, 13.09.2026), eigener Commit getrennt vom Bericht. Aufgabe 3: Sicherheitsnetz-Fragen A-D (Senkungs-Deckel, Anhebungs-Deckel, Verhalten unter Einstand, Cron Batch vs. Alle) nur beantworten, nichts aendern. Keine Formel-/Config-/Rundungs-Aenderung, keine DB-Migration, keine Auth-Aenderung, Cron nicht manuell anstossen.
**Fund (vor Implementierung, aendert Aufgabe 2 — Grundgesetz Regel 9 + 6, Praezisierung noetig):** Der Auftrag geht davon aus, `AUTO_PRICE_WRITE_ENABLED` sei noch `false` und die Freigabe stehe noch aus. Tatsaechlich ist der Schalter auf `origin/main` bereits `true` — gesetzt in PR #97 ("feat/preis-automatik-nur-anheben", bereits gemergt vor dieser Session). Der bestehende Kommentar (`shared/pricing.ts:462-477`, "Teil 4/5", 13.09.2026) begruendet das aber NICHT mit den 6 nachgerechneten Transaktionen aus diesem Auftrag, sondern damit, dass gleichzeitig `applyRaiseOnly()` als alleiniges Gate fuer die beiden unbeaufsichtigten Pfade (`price-monitor.ts checkOne()`, `POST /products/check-all-prices`) eingefuehrt wurde — diese koennen seitdem AUSSCHLIESSLICH anheben, nie senken. Die im Auftrag beschriebene Handlung ("Flag auf true setzen") ist damit bereits erledigt, aber mit einer anderen Absicherung (Richtungs-Deckel statt Werte-Validierung) als der im Auftrag genannten Begruendung. Vor Aufgabe 2 daher Rueckfrage an den Nutzer gestellt, ob (a) nur der Kommentar um die 6-Transaktionen-Verifikation ergaenzt werden soll (Flag bleibt wie es ist, raise-only bleibt bestehen) oder (b) die raise-only-Beschraenkung entfernt werden soll (voll bidirektionale Automatik, jetzt basierend auf den validierten Werten) — das ist eine Preis-/Geld-Logik-Entscheidung mit Live-Auswirkung auf 53 Produkte im 8-Stunden-Cron und faellt unter "Immer manuell bestaetigen lassen" (CLAUDE.md). Aufgabe 1 (Trockenlauf-Bericht, reine Lektuere) und Aufgabe 3 (Sicherheitsnetz-Fragen, reine Lektuere) werden unabhaengig davon zuerst erledigt, da beide keine Schreibaktion sind und unter beiden Auslegungen gleich nuetzlich bleiben.

**Nutzerentscheidung Aufgabe 2:** Option (a) — nur Kommentar ergaenzen, Flag bleibt `true`, raise-only-Sperre bleibt bestehen. Kein Verhaltensunterschied fuer den Cron.

**Ergebnis Aufgabe 1 (Trockenlauf):** Reines Leseskript `packages/web/scripts/preis-trockenlauf-report.ts` — Import nur aus `db/index`, `db/schema`, `shared/pricing`, `api/price-monitor` (dort ausschliesslich die reinen Funktionen `computeVariantPriceRows`/`safeUniformVariantPrice`, kein `ebay.ts`-Import). Bewusst NICHT `recalculate-preview` verwendet: der Endpunkt ruft intern `getAllSellerListings()` auf, das ist ein echter eBay-Request (`GetSellerListRequest`, `ebay.ts:1675`) — widerspricht der Vorgabe "kein eBay-Call". "Heutiger VK" kommt daher aus der lokalen DB-Spalte `sellPrice`, nicht von der eBay-Live-Notierung. Lauf gegen die echte Produktions-DB (`TURSO_DATABASE_URL`, nur lesend). Ergebnis (Datenstand 13.09.2026, ca. 18:47 Uhr — Hintergrund-Cron laeuft parallel, ein erneuter Lauf kann leicht abweichen): 53 Produkte gesamt, 1 wuerde teurer (stele-137, 10,95€→17,95€, weil es HEUTE unter Einstand steht, -3,59€ Gewinn), 46 wuerden guenstiger, 3 unveraendert, 3 ohne bisherigen VK (Erst-Setzung), 1 Produkt aktuell unter Einstand. Wichtiger Nebenfund: von den 53 Produkten sind nur 11 Einzelartikel (kein Varianten-Produkt) — nur bei DIESEN 11 schreibt der automatische Pfad ueberhaupt `sellPrice` (siehe Aufgabe 3); die 42 Varianten-Produkte sind strukturell unberuehrt von `AUTO_PRICE_WRITE_ENABLED`, unabhaengig vom Flag-Wert. Vollstaendiger Bericht: `docs/preis-trockenlauf-2026-09-13.md`.

**Ergebnis Aufgabe 2 (Kommentar):** `shared/pricing.ts:477-488` (nach Ergaenzung) — Flag-Wert unveraendert `true`. Neuer Absatz nach dem bestehenden "Teil 4/5"-Kommentar dokumentiert die 6-Transaktionen-Verifikation als ZUSAETZLICHE Begruendung (nicht Ersatz): Zahlen unabhaengig nachgerechnet (nicht vom Nutzer uebernommen, Grundgesetz Regel 3) — zurueckgerechnete Gebuehrensaetze 14,58%-15,14% (Mittel 14,98%), 5 von 6 Bestellungen trafen den Modellwert (0,15×Preis+0,30)×1,19 auf 2 Cent genau (Abweichung bei den restlichen 5 lag bei 0,00-0,02€; die 8,95€-Bestellung wich mit 0,04€ ab und zaehlt als die eine Ausnahme). Eigener Commit (`f91428c`), getrennt vom Trockenlauf-Bericht-Commit (`dc4149d`).

**Ergebnis Aufgabe 3 (Sicherheitsnetz, Codebeleg):**
- **A) Senkungs-Deckel:** Ja, aber NUR fuer die menschlich bestaetigten Pfade (`recalculate-preview`/`-apply`, `api/index.ts:1166,1204,1280,1295`): `applyDecreaseCap()` (`shared/pricing.ts:343-361`) begrenzt auf `MAX_PRICE_DECREASE_PERCENT = 8` (`shared/constants.ts:33`) pro Lauf. Die beiden UNBEAUFSICHTIGTEN automatischen Pfade (`price-monitor.ts checkOne()`, `index.ts POST /products/check-all-prices`) rufen `applyDecreaseCap()` NICHT auf (bewusst entfernt, Teil 4/5-Kommentar) — dort gibt es keinen Senkungs-Deckel, weil dort ueberhaupt nie gesenkt wird (s. C).
- **B) Anhebungs-Deckel:** Nein. `applyDecreaseCap()`-Kommentar (`pricing.ts:324-325`): "Anheben ist nie gedeckelt". `applyRaiseOnly()` (`pricing.ts:382-393`) hat ebenfalls keine Obergrenze — bei `computedMinPrice > currentPrice` wird IMMER der volle berechnete Preis geschrieben, beliebig weit ueber dem Altpreis. Im Trockenlauf-Bericht sichtbar: stele-137 spraenge in einem Lauf von 10,95€ auf 17,95€ (+63,9%).
- **C) Verhalten unter Einstand:** Es gibt KEINEN expliziten Code-Check "wenn Preis < Einstand, dann abbrechen" (per grep bestaetigt, kein Treffer fuer einen solchen Vergleich in `price-monitor.ts`/`index.ts`/`pricing.ts`). Der Schutz ist rein mathematisch: `computeMinSellPrice()` (`pricing.ts:89-98`) addiert `targetMarginEur` (Default `MIN_GEWINN_EUR` = 2,00€, nie negativ in den Defaults) im Zaehler und dividiert durch `(1 - totalFeeRateGross)` < 1 — das Ergebnis liegt dadurch IMMER ueber `totalCost` (Einkauf+Versand+Zoll), solange `targetMarginEur >= 0`. Einschraenkung: `product.targetMarginEur` wird beim Import (`index.ts:1916,1942`) ungeprueft aus dem Request-Body uebernommen, keine Validierung gegen negative Werte gefunden — ein negativer Wert dort wuerde die mathematische Garantie schwaechen (aber wahrscheinlich noch nicht unter Einstand ruecken, da `targetMarginEur` nur additiv im Zaehler steht, nicht multiplikativ).
- **D) Batch vs. alle Produkte:** Der 8h-Cron (`price-monitor.ts:676-686`, `setInterval`, alle 8h) verarbeitet in EINEM Lauf ALLE Produkte mit gesetzter `sourceUrl`+`buyPrice` (53 von 53 aktuell), aber in Chargen von `CONCURRENCY = 3` gleichzeitig (`price-monitor.ts:666-670`, `for`-Schleife ueber `products.slice(i, i+3)` + `Promise.all`) — sequenzielle 3er-Batches, nicht alle parallel und nicht ueber mehrere Cron-Zyklen verteilt.

Tests: Frontend-Typecheck (`bun run typecheck`) gruen. Server-Typecheck (`bun run typecheck:server`) gruen. `bun test src/shared/pricing.test.ts`: 61 pass, 0 fail, 166 expect() (unveraendert ggue. `origin/main`, da Aufgabe 2 nur einen Kommentar aendert).
Keine Aenderung an der Formel, an `DEFAULT_PRICING_CONFIG`, an Rundungsfunktionen, an `applyRaiseOnly()`/`applyDecreaseCap()` selbst. Keine DB-Migration, keine Auth-Aenderung. Cron nicht manuell angestossen (kein Aufruf von `runPriceCheck()`/`startPriceMonitor()` in dieser Session). **Bestaetigt: in dieser Session wurde kein Preis in die DB geschrieben und nichts an eBay gesendet** — nachgewiesen durch: (1) der Trockenlauf-Report-Lauf importiert `ebay.ts` an keiner Stelle (per grep bestaetigt: keine `import`-Zeile mit `ebay` in `preis-trockenlauf-report.ts`), (2) das Skript enthaelt keinen `db.update`/`db.insert`-Aufruf (nur `db.select`), (3) `runPriceCheck()`/`checkOne()`/`check-all-prices` wurden in dieser Session an keiner Stelle aufgerufen.
Branch: `feat/auto-price-write-freigabe` (isoliertes Worktree unter `.worktrees/`, Basis `origin/main`). PR: https://github.com/contact-e-stele/stele-e-transfer/pull/98 (Draft, nicht gemergt).

## 2026-09-13 - Claude Sonnet 5 (Session 01WGBb9TBVAVdHGyg1fk75z4)
**Claim:** Fix-Auftrag "Preisalarm auf die richtige Bedingung umstellen". Branch `fix/preisalarm-nur-unter-mindestpreis` (isoliertes Worktree unter `.claude/worktrees/`, Basis `origin/main`), Draft-PR, kein Merge. Aufgabe 1: Bestandsaufnahme mit Codebeleg — alle Stellen, die den Preisalarm (`price_changed`) setzen, samt woertlicher Bedingung. Aufgabe 2: Bedingung korrigieren — Alarm nur noch bei Verkaufspreis UNTER `computeMinSellPrice()`/Gewinn unter Zielmarge (`profitAtSellPrice()`), ausschliesslich `shared/pricing.ts`, keine neue Formel; Produkte-Tab-Anzeige entsprechend anpassen. Aufgabe 3: Altbestand nur Vorschau (Skript vorbereiten, NICHT ausfuehren), Vorschau-Zahlen zeigen. Zusaetzlich berichten (nichts aendern): Status stele-137 inkl. eBay-Listing-Pruefung. Keine Aenderung an Preisformel/DEFAULT_PRICING_CONFIG/Rundung/`applyRaiseOnly`, kein automatischer Preis-/eBay-Call, keine DB-Migration.

**Ergebnis Aufgabe 1 (Bestandsaufnahme):** Vier Schreibstellen fuer `priceChanged` gefunden, alle mit derselben Fehlerklasse ("Abweichung" statt "Unterschreitung"):
1. `price-monitor.ts:573` (Varianten-Zweig von `checkOne()`, alt): `const deviates = safePrice != null && (product.sellPrice == null || Math.abs(safePrice - product.sellPrice) >= ALERT_THRESHOLD);` — `Math.abs()` feuert auch, wenn der aktuelle VK ÜBER dem berechneten Mindestpreis liegt (Hauptursache fuer die 41 von 53 falschen Alarme, siehe Vorschau unten).
2. `price-monitor.ts:626`/`640` (Einzelartikel-Zweig, alt): `priceChanged: decision.action === 'raise'` — `applyRaiseOnly()` liefert `'raise'` auch bei einer Erst-Setzung (`isInitialPrice: true`, kein bisheriger Preis), was faelschlich als Alarm gezaehlt wuerde.
3. `api/index.ts:2930` (`POST /products/check-all-prices`, alt): `const priceChanged = product.buyPrice !== null && Math.abs((product.buyPrice ?? 0) - newPrice) > 0.01;` — reine Einkaufspreis-Differenz, voellig unabhaengig von Marge/Mindestpreis. Betrifft alle 53 Produkte gleichermassen bei jeder AliExpress-Preisschwankung.
4. `api/index.ts:2329` (`PATCH /products/:id/price`, manueller "Preis erneut pruefen"-Button in `produkte.tsx:841`, alt): `const priceChanged = old.buyPrice !== null && Math.abs((old.buyPrice ?? 0) - body.buyPrice) > 0.01;` — dieselbe reine Diff-Bedingung wie 3.
Auswertung/Anzeige: `produkte.tsx:993` (`priceAlerts: products.filter(p => p.priceChanged).length`, Stats-Karte "Preisalarm") und `produkte.tsx:1431`/`dashboard.tsx:300` (Badge "Preisänderung" bei `product.priceChanged`).

**Ergebnis Aufgabe 2 (Bedingung korrigiert):** Eine neue Funktion `evaluatePriceAlarm()` in `shared/pricing.ts` (nutzt ausschliesslich `profitAtSellPrice()`, keine neue Formel) ist jetzt die EINE Quelle fuer die Alarm-Entscheidung an allen vier Stellen: Alarm nur, wenn ein aktueller VK existiert UND der (ungerundete, exakte) Gewinn bei mindestens einer Variante unter der Zielmarge (`targetMarginEur`, Default `MIN_GEWINN_EUR`) liegt — "schlechteste Variante entscheidet", analog zu `safeUniformVariantPrice()`. Kein bisheriger VK → kein Alarm (weder Erst-Setzung noch fehlender Preis). Alle vier Schreibstellen (price-monitor.ts Varianten- und Einzelartikel-Zweig, `check-all-prices`, `PATCH /products/:id/price`) rufen jetzt `evaluatePriceAlarm()` auf; die Schreib-/Anhebe-Entscheidung selbst (`applyRaiseOnly`, `AUTO_PRICE_WRITE_ENABLED`) ist unveraendert. Anzeige: Badge-Text in `produkte.tsx`/`dashboard.tsx` von "Preisänderung" auf "Preisalarm" umbenannt, Tooltip "Verkaufspreis liegt unter dem berechneten Mindestpreis — Marge gefährdet" ergaenzt (Badge + Stats-Karte). Kein neues DB-Feld eingefuehrt (Auftrags-Vorgabe: "keine neuen DB-Feld ohne Not") — eine getrennte "Quellpreis geaendert"-Anzeige gibt es bewusst nicht, diese Information steht bereits unveraendert in der `price_history`-Tabelle.

**Ergebnis Aufgabe 3 (Vorschau, keine Schreibaktion):** Leseskript `scripts/preisalarm-vorschau-report.ts` (nur `db.select`, kein `db.update`/eBay-Call) gegen die echte Produktions-DB ausgefuehrt (13.09.2026, 17:36 UTC) — vergleicht bestehendes `price_changed`-Flag gegen `evaluatePriceAlarm()` auf demselben, bereits gespeicherten Datenstand: **53 Produkte gesamt, 41 mit aktuell gesetztem Flag, davon wuerden 41 auf `false` zurueckgesetzt (0 blieben Alarm), 1 wird NEU als Alarm erkannt (stele-137, vorher faelschlich nicht markiert)** — Bericht: `docs/preisalarm-vorschau-2026-09-13.md`. Das eigentliche Schreib-Skript `scripts/preisalarm-reset-flags.ts` ist vorbereitet (nutzt exakt dieselbe `evaluatePriceAlarm()`-Berechnung wie die Vorschau) und mit einer Sicherheitssperre versehen (`PREISALARM_RESET_CONFIRM=yes` erforderlich) — **NICHT ausgefuehrt**, keine DB-Aenderung in dieser Session (per `git status`/Skript-Aufbau nachweisbar: nur `db.select`-Aufrufe wurden ausgefuehrt).

**Status stele-137 (nur berichtet, nichts geaendert):** DB-Stand (13.09.2026, 17:46 UTC): `sellPrice=10,95€`, `buyPrice`(Top-Level)=3,99€ — Produkt hat aber 5 Varianten mit eigenen Einkaufspreisen (3,65–5,59€), `shippingCost=1,99€`, `shipsFrom=China` (Zoll 4,00€), `targetMarginEur=2,00€`. Schlechteste Variante ("cubic Vacuum Bag XL", EK 5,59€) ergibt beim aktuellen VK einen Gewinn von **-3,59€** (deckt sich exakt mit der Zahl aus dem Auftrag) — echter, korrekt erkannter Alarm nach der neuen Bedingung. **eBay-Listing:** `ebayStatus="listed"`, `ebayListingId="198589591311"`, `ebayError=null` — laut unserer DB aktuell AKTIV gelistet, nicht beendet. Zur behaupteten Richtlinienverstoss-Beendigung: die zugehoerige Reparatur-Arbeit (PR #92, "VERSTOSS-REPARATUR Phase 2", Branch `fix/ebay-offplatform-contact-leak`) war eine Vorlagen-Korrektur (Entfernen eines "KUNDENSERVICE"-Blocks + Filter fuer Drittanbieter-Kontakte in generierten Beschreibungen), breit auf bis zu 50 Listings angewendet, aber konkret nur gegen stele-110 getestet — kein Memory-Beleg, der stele-137 namentlich mit einer Verstoss-Beendigung verknuepft. **Einschraenkung:** in dieser Umgebung standen keine eBay-API-Zugangsdaten zur Verfuegung (nur Turso-DB-Zugang) — der LIVE-eBay-Status von Listing 198589591311 konnte NICHT direkt per API verifiziert werden, nur der Stand unserer eigenen DB. Empfehlung: Listing 198589591311 bei Bedarf direkt auf eBay pruefen.

Tests: Frontend-Typecheck (`bun run typecheck`) gruen. Server-Typecheck (`bun run typecheck:server`) gruen. `bun test src/shared/pricing.test.ts`: 66 pass, 0 fail, 177 expect() (61 bestehend + 5 neue fuer `evaluatePriceAlarm()`, inkl. des geforderten Testfalls "Preis ueber Mindestpreis -> kein Alarm" / "Preis unter Mindestpreis -> Alarm" sowie eines Regressions-Beweises, dass die ALTE `Math.abs()`-Bedingung bei einem stele-87-artigen Fall faelschlich Alarm gemeldet haette). Voller `bun test`: 117 pass, 2 fail — dieselben 2 vorbestehenden, nicht durch diese Aenderung verursachten `/tmp`-Sandbox-Fehler wie in fruaheren Sessions dokumentiert (`aliexpress.test.ts`/`variant-sales.test.ts`, lokales libsql-Test-Fixture, kein Preislogik-Bezug).
Keine Aenderung an der Preisformel, `DEFAULT_PRICING_CONFIG`, Rundungsfunktionen oder `applyRaiseOnly()`/`applyDecreaseCap()` selbst (per Diff bestaetigt: nur `evaluatePriceAlarm()` neu hinzugefuegt, bestehende Funktionen unangetastet). Kein automatischer Preis-Schreibvorgang, kein eBay-Call, keine DB-Migration in dieser Session.
Branch: `fix/preisalarm-nur-unter-mindestpreis` (isoliertes Worktree unter `.claude/worktrees/`, Basis `origin/main`). PR: https://github.com/contact-e-stele/stele-e-transfer/pull/100 (Draft, nicht gemergt).

## 2026-09-13 - Claude Sonnet 5 (Session 01WGBb9TBVAVdHGyg1fk75z4)
**Claim:** Auftrag "Preisalarm auf die richtige Bedingung umstellen". Branch `fix/preisalarm-nur-unter-mindestpreis` (isoliertes Worktree unter `.claude/worktrees/`, Basis `origin/main`), Draft-PR, kein Merge. Aufgabe 1: Bestandsaufnahme mit Codebeleg — alle Stellen, an denen `priceChanged` gesetzt wird, und die woertliche Bedingung. Aufgabe 2: Bedingung korrigieren — Alarm nur noch bei Verkaufspreis UNTER dem via `computeMinSellPrice()`/`profitAtSellPrice()` berechneten Mindestpreis/Zielmarge, ausschliesslich `shared/pricing.ts`, keine neue Formel; Anzeige im Produkte-Tab entsprechend anpassen. Aufgabe 3: Altbestand nur Vorschau — Skript/Endpunkt vorbereiten, NICHT ausfuehren (Freigabe folgt separat), aber die Vorschau-Zahlen selbst (reiner Lesevorgang) zeigen. Zusaetzlich berichten (nichts aendern): Status stele-137 inkl. eBay-Listing-Status/Richtlinienverstoss-Pruefung. Keine Aenderung an Preisformel/DEFAULT_PRICING_CONFIG/Rundung, `applyRaiseOnly` bleibt unveraendert und aktiv, keine automatische Preisaenderung/eBay-Call/DB-Migration. Pflicht: Frontend-/Server-Typecheck, `pricing.test.ts`, echte Ausgaben, ein Testfall fuer die neue Bedingung.
**Ergebnis:** (wird nach Abschluss eingetragen)

## 2026-09-14 - Claude Sonnet 5 (Session 01Myk1sNTV8a7j8Gxsq9MAav)
**Vorlauf (dieselbe Session, davor):** Live-Verifikation von "Preis-Fundament Teil 4/5" angefordert. Ergebnis: bereits am 13.09.2026 umgesetzt und gemergt (PR #97/#98/#100) — mein lokaler `main` war nur veraltet. Gegen die echte Produktions-DB read-only neu geprueft: `applyRaiseOnly()` weiterhin alleiniges Gate in `checkOne()`/`check-all-prices`, `applyDecreaseCap()` weiterhin nur in den manuellen Pfaden, `AUTO_PRICE_WRITE_ENABLED=true` live (Frontend-Bundle liefert `v1.11`), Audit-Skript zeigt aktuell 0 "anheben", 6 "nichts tun" bei den Nicht-Varianten-Listings, keins davon `wasBelowBreakEven`. `pricing.test.ts` 66/66 gruen auf dem tatsaechlich gemergten `origin/main`. Keine Code-Aenderung, kein PR.

**Claim (P-79):** Auftrag "Workflow-Text in der App auf den aktuellen Stand bringen" — Live-Fund 14.09.2026: eine Instanz hat mit veraltetem "Workflow kopieren"-Text eine AliExpress-Bestellung gar nicht erst vorbereitet. Branch `fix/p79-workflow-text` (isoliertes Worktree unter `.worktrees/`, Basis `origin/main`), Draft-PR. Aufgabe 1: Fundstelle nennen (Datei+Zeile). Aufgabe 2: Text durch die vorgegebene Fassung ersetzen, Bestelldaten weiterhin live einsetzen. Aufgabe 3: nur falls kein Vorlagenfeld existiert, eine Konstante anlegen. Nachtrag des Nutzers waehrend der Bearbeitung: den bestehenden Text NICHT komplett ersetzen, sondern als Grundlage behalten und nur um die genannten Punkte ergaenzen/korrigieren — plus Vorher/Nachher-Vergleich im PR.

**Ergebnis Aufgabe 1 (Fundstelle):** `packages/web/src/web/pages/bestellungen.tsx:81` (`buildWorkflowText()`) — berechnet Header + Duplikat-Check live und fuellt den Rest per `renderWorkflowTemplate()` (`shared/workflow-template.ts:154`) mit den echten Bestelldaten. Die eigentliche Vorlage kommt aus dem State `workflowTemplate` (`bestellungen.tsx:199`), initial `DEFAULT_WORKFLOW_TEMPLATE`, ab dem ersten erfolgreichen `GET /api/settings/workflow-template`-Aufruf (`bestellungen.tsx:202`) aus `app_settings`.

**Ergebnis Aufgabe 3 (Vorlagenfeld bereits vorhanden):** Seit P-94 existiert bereits genau eine editierbare Quelle (`app_settings`, Key `workflow_template`, `GET`/`PUT /api/settings/workflow-template`, Einstellungen-Tab) mit `DEFAULT_WORKFLOW_TEMPLATE` (`shared/workflow-template.ts`) als Seed/Fallback — keine neue Konstante noetig. **Read-only gegen die echte Produktions-DB geprueft (nicht angenommen):** kein gespeicherter `app_settings`-Eintrag fuer `workflow_template` vorhanden — `GET` liefert aktuell direkt `DEFAULT_WORKFLOW_TEMPLATE`. Die Code-Aenderung wirkt damit sofort nach Deploy, ohne dass jemand im Einstellungen-Tab erneut speichern muss.

**Ergebnis Aufgabe 2 (Text ergaenzt, nicht ersetzt — Nutzer-Nachtrag beachtet):** Bestehende Struktur (Rolle, Phase 0, Schritte 1-11, Effizienz-Hinweis) unveraendert gelassen, vier gezielte Ergaenzungen in `DEFAULT_WORKFLOW_TEMPLATE`: (1) neue Vorrang-Regel unter "Rolle" — eine offene Bestellung geht vor allem anderen, mit Verweis auf den Live-Fund vom 14.09.; (2) Schritt 2 um "beim Neuanlegen NICHT als Standardadresse setzen" ergaenzt; (3) Schritt 3 um den Hinweis auf den richtigen Shop/Anbieter ergaenzt (Verwechslungsgefahr nach der optionalen Alternativ-Anbieter-Suche); (4) Schritt 5 von einer vagen "geschaetzte Einfuhrabgaben"-Formulierung auf die exakte Gebuehrenformel (15% + 0,30€ eBay-Gebuehr ×1,19, 5% Anzeigentarif ×1,19 — identisch zu `DEFAULT_PRICING_CONFIG`) umgestellt, plus neuer IOSS-Hinweis: unter der IOSS-Grenze steckt der Zoll schon im AliExpress-Gesamtpreis, die 4,00€-Zollpauschale der App darf dann NICHT zusaetzlich abgezogen werden. Kein IOSS-Schwellenwert (Euro-Betrag) im Text — dafuer existiert keine Code-Konstante, ein geratener Wert waere eine erfundene Zahl (Grundgesetz Regel 3/4). Der Auftragstext brach bei Punkt 7 mitten im Satz ab ("... im Bestellungen-Tab eintragen,") — dieser Punkt existiert im bestehenden Schritt 7 bereits identisch, daher nichts ueber den abgebrochenen Satz hinaus erfunden. Vollstaendiger Vorher/Nachher-Diff in der PR-Beschreibung.

Beim ersten Schreibversuch brach `tsc` mit `TS1005` ab: verschachtelte Backticks (`` `DEFAULT_PRICING_CONFIG` ``) innerhalb des aeusseren Backtick-Template-Literals hatten die Vorlage vorzeitig geschlossen — durch normale Anfuehrungszeichen ersetzt, danach gruen.

Tests: Frontend-Typecheck (`bun run typecheck`) gruen. Server-Typecheck (`bun run typecheck:server`) gruen. Voller `bun test`: 117 pass, 2 fail — dieselben 2 vorbestehenden `/tmp/aliexpress-test.db`-Sandbox-Fehler wie in fruaheren Sessions dokumentiert (Windows-Pfadproblem, kein Bezug zu `workflow-template.ts`, das keine DB-Logik enthaelt).
Keine DB-Migration, keine Preis-/Berechnungslogik veraendert (reiner Text, Zahlen nur als Prosa-Wiedergabe der bereits bestehenden `DEFAULT_PRICING_CONFIG`-Werte). Kein neues Eingabefeld unter Einstellungen angelegt.
Branch: `fix/p79-workflow-text` (isoliertes Worktree unter `.worktrees/`, Basis `origin/main`). PR: https://github.com/contact-e-stele/stele-e-transfer/pull/101 (Draft, nicht gemergt).

**Nachtrag (dieselbe Session):** Nutzer hat PR #101 gemergt (`gh pr ready 101` + `gh pr merge 101 --squash`, Merge-Commit `f556e47`). Bestaetigt: `state=MERGED`, `mergedAt=2026-09-14T15:24:24Z`.

## 2026-09-14 - Claude Sonnet 5 (Session 01Myk1sNTV8a7j8Gxsq9MAav)
**Claim:** Auftrag "P2 — Sendungsnummer-Cron scharf schalten". Voraussetzung PR #99 (Cron gebaut, Schalter aus) geprueft: gemergt (13.09.2026, Merge-Commit `c0117fc`). Branch `fix/p2-tracking-sync-enable` (isoliertes Worktree unter `.worktrees/`, Basis `origin/main`), Draft-PR. Aufgabe 1: Trockenlauf ueber ALLE Bestellungen mit AliExpress-Bestellnr. ohne Sendungsnummer, nur lesend, Ergebnis als Datei im PR. Aufgabe 2: Bezugsquelle (logistics_no bzw. ds.trade.order.get) pruefen, nicht raten. Aufgabe 3: `ALIEXPRESS_TRACKING_SYNC_ENABLED` auf true, Intervall 4h. Aufgabe 4: Logging je Bestellung, Fehler ueberspringen statt abbrechen. Aufgabe 5: Doppelschreib-Schutz pruefen, ob `trackingEbaySubmitted` das abdeckt. Verifikation: Trockenlauf-Datei, Anzahl betroffener Bestellungen, typecheck+Testsuite im Klartext. Strikte Grenzen: `AUTO_PRICE_WRITE_ENABLED`/Preislogik unangetastet, keine DB-Migration, keine Kundennachricht aus diesem Job, Draft-PR.

**Ergebnis Aufgabe 1+6 (Trockenlauf):** `scripts/preview-tracking-sync-all.ts` neu (ruft `syncTrackingNumbers({ dryRun: true })` — dieselbe Funktion wie der scharfgeschaltete Cron — gegen die echte Produktions-DB + echte AliExpress-API, kein `db.update()`, kein eBay-Call). Lauf am 14.09.2026 gegen die echte DB: **2 Bestellungen** mit AliExpress-Bestellnr. ohne Sendungsnummer. Ergebnis-Datei: `packages/web/scripts/output/tracking-sync-preview-alle-offenen.md`.

**Ergebnis Aufgabe 7 (Anzahl, echt nachgerechnet statt aus dem Auftrag uebernommen):** **2**, nicht 1 wie im Auftrag als "aktuell bekannt" benannt — die zweite ist ein bislang unbenannter Fund: eBay 27-14918-13809 / AliExpress 3075188992327211, `order_status=FINISH` (bei AliExpress bereits abgeschlossen), echte Sendungsnummer `AP00832504143414` bereits abrufbar, aber nie im Bestellungen-Tab eingetragen. Die im Auftrag genannte Bestellung (eBay 26-15134-85187 / AliExpress 3076295709557211, Andreas Morgenstern) liefert korrekt `order_status=WAIT_SELLER_SEND_GOODS`, Sendungsnummer "keine" — noch nicht versendet, kein Fehlalarm.

**Ergebnis Aufgabe 2 (Bezugsquelle geprueft, nicht angenommen):** `aliexpress.trade.ds.order.get`, Feld `logistics_info_list.aeop_order_logistics_info[].logistics_no` (bereits aus PR #99, `aliexpress-api.ts:356-406`). Das ist die Sendungsverfolgungs-Seite der EINZELNEN Bestellung (Dropshipper-Order-Detail-API), NICHT die Bestelluebersicht, die laut Auftrag keine Nummer zeigt — die Unterscheidung ist im Code bereits korrekt getroffen. Zwei unabhaengige echte Bestaetigungen: (1) PR #99, 3076306514497211 → `AP00843143208329`, deckte sich mit dem manuell gefundenen Wert; (2) dieser Lauf, 3075188992327211 → `AP00832504143414`, plus korrektes "keine" fuer eine nachweislich noch nicht versendete Bestellung (kein falsch-positiver Fund). Einschaetzung: zuverlaessig, keine offene Unsicherheit.

**Ergebnis Aufgabe 5 (Doppelschreib-Schutz, `trackingEbaySubmitted` geprueft statt angenommen):** `trackingEbaySubmitted` deckt die gestellte Frage NICHT ab — das Flag beschreibt nur, ob eine BEREITS gespeicherte Sendungsnummer erfolgreich an eBay uebermittelt wurde (gesetzt in `index.ts:968-972`, ausschliesslich beim manuellen PATCH mit gemeinsam uebergebenem `trackingNumber`+`carrier`), nicht ob JETZT schon eine Nummer dasteht. Tatsaechlicher Schutz, jetzt zweifach: (a) `loadTargetsFromDb()` laedt von vornherein nur Zeilen mit leerer/NULL `trackingNumber`; (b) `writeTrackingNumberToDb()` neu um dieselbe Leer/NULL-Bedingung ATOMAR in der `WHERE`-Klausel des Updates ergaenzt (schliesst die theoretische Race zwischen Laden und Schreiben — ein manueller Eintrag zwischen Cron-Start und diesem einzelnen Schreibvorgang wuerde sonst stillschweigend ueberschrieben). "Nicht erneut an eBay gesendet" ist strukturell ausgeschlossen, nicht nur durch das Flag: `writeTrackingNumberToDb()` setzt nie `carrier`, und `createShippingFulfillment()` (`index.ts:963`, der einzige eBay-Kontaktpunkt) feuert ausschliesslich, wenn `trackingNumber` UND `carrier` GEMEINSAM in einem PATCH-Request ankommen — dieser Cron ruft diesen Endpunkt nie auf.

**Ergebnis Aufgabe 3+4:** `ALIEXPRESS_TRACKING_SYNC_ENABLED` auf `true` (`tracking-sync.ts:49`) — NACH Sichtung des Trockenlaufs, wie von PR #99 vorausgesetzt. Intervall unveraendert 4h (`SYNC_INTERVAL_MS`, bereits korrekt aus PR #99). Logging je Bestellung (eBay-Nr., AliExpress-Nr., Status, uebernommen ja/nein) und Fehler-Verhalten (Bestellung ueberspringen, `errors++`, weiter mit der naechsten) waren bereits in PR #99 korrekt umgesetzt — unveraendert, per Code-Lesung bestaetigt.

Regressions-Beweis (Grundgesetz Regel 5): `ALIEXPRESS_TRACKING_SYNC_ENABLED` testweise auf `false` zurueckgesetzt → der zugehoerige Test schlug korrekt fehl (8 pass, 1 fail) → zurueckgesetzt → wieder 9/9 in `tracking-sync.test.ts` gruen.

Tests: Frontend-Typecheck (`bun run typecheck`) gruen. Server-Typecheck (`bun run typecheck:server`) gruen. `bun test src/api/tracking-sync.test.ts`: 9 pass, 0 fail, 25 expect(). Voller `bun test`: 117 pass, 2 fail — dieselben 2 vorbestehenden `/tmp/aliexpress-test.db`-Sandbox-Fehler (Windows-Pfadproblem, unabhaengig von dieser Aenderung, bereits in fruaheren Sessions dokumentiert).
Keine Aenderung an `AUTO_PRICE_WRITE_ENABLED` oder Preislogik (per Diff bestaetigt: nur `tracking-sync.ts`/`tracking-sync.test.ts`/neues Skript betroffen). Keine DB-Migration. Kein Kundennachricht-Versand aus diesem Job (unveraendert — `syncTrackingNumbers()` ruft an keiner Stelle eine Nachrichten-/Notify-Funktion auf, per Code-Lesung bestaetigt).
Branch: `fix/p2-tracking-sync-enable` (isoliertes Worktree unter `.worktrees/`, Basis `origin/main`). PR: https://github.com/contact-e-stele/stele-e-transfer/pull/102 (Draft, nicht gemergt).

## 2026-09-14 - Claude Sonnet 5 (Session 01Myk1sNTV8a7j8Gxsq9MAav)
**Claim:** Auftrag "P2-Korrektur — falsche Sendungsnummer, PR #102 nicht mergen". Nutzer hat manuell auf den Sendungsverfolgungs-Seiten gegengeprüft: `logistics_no` aus der API (Präfix "AP") deckt sich NICHT mit der echten DHL-Nummer (zwei Bestellungen belegt). Die Behauptung in PR #102 ("deckte sich mit dem manuell gefundenen Wert") trifft nicht zu. Weitere Commits auf demselben Branch `fix/p2-tracking-sync-enable`/PR #102 (kein neuer PR — Korrektur derselben Sache). Aufgabe 1: volle Feldliste von `aeop_order_logistics_info[]` im PR. Aufgabe 2: falls Zusteller-Nummer dort fehlt, ausdrücklich sagen + anderen Endpunkt benennen, nicht auf AP ausweichen. Aufgabe 3: Cron auf Zusteller-Nummer umstellen, AP-Werte NIE schreiben — dann lieber nichts. Aufgabe 4: erneuter Trockenlauf, Abweichung vom erwarteten Wert erklären statt anpassen. Zusatzbefund: eBay 27-14918-13809 ist seit 05.08. zugestellt, App zeigt trotzdem "41 Tage, evtl. verloren" — Logik der Warnung prüfen und melden, was sie auswertet. Strikte Grenzen: `AUTO_PRICE_WRITE_ENABLED`/Preislogik unangetastet, keine Migration, keine Kundennachricht, `ALIEXPRESS_TRACKING_SYNC_ENABLED` bleibt `false` bis belegt korrekt.

**Ergebnis Aufgabe 1 (volle Feldliste, echter Lauf statt Doku-Annahme):** Neues Skript `scripts/inspect-ali-order-fields.ts`, Ergebnis-Datei `scripts/output/ali-order-fields-inspection.md`. Top-Level `result`-Objekt von `aliexpress.trade.ds.order.get`: `child_order_list, gmt_create, logistics_info_list, logistics_status, order_amount, order_paidtime_string, order_status, pay_timeout_second, store_info, user_order_amount`. `logistics_info_list.aeop_order_logistics_info[]` hat für BEIDE geprüften Bestellungen ausschließlich zwei Felder: `logistics_service` (z.B. "CAINIAO_FULFILLMENT_STD") und `logistics_no` (die AP-Nummer). Kein Zusteller-Namensfeld, keine Zusteller-Nummer in diesem Endpunkt.

**Ergebnis Aufgabe 2 (Alternativ-Endpunkt gesucht, empirisch, nicht geraten):** `scripts/probe-ali-logistics-endpoints.ts` — per Parameter-Iteration gegen die echte API (analog zur Methodennamen-Suche in PR #99) `aliexpress.logistics.ds.trackinginfo.query` gefunden (existiert, Pflichtparameter `logistics_no`, `to_area`, `service_name`, `origin`, `out_ref`). Echter Aufruf gegen `3075188992327211` liefert `official_website: "https://www.dhl.de/"` (bestätigt Zusteller=DHL) und ein `details[]`-Ereignis-Array (u.a. "Package delivered" 2026-08-05 16:37:20 — deckt sich mit dem manuell gefundenen Zustelldatum). Vollständige, per `Object.keys()` erschöpfend geprüfte Feldliste dieser Antwort: `official_website, details, result_success, request_id, _trace_id_` — **keine Zusteller-Nummer als Feld**, auch nicht verschachtelt (grep über das komplette rohe JSON nach "00340"/"mail_no"/"tracking"/"waybill": kein Treffer). **Ausdrücklich gesagt statt geraten (Aufgabe 2):** mit den beiden hier getesteten AliExpress-Open-API-Methoden ist die echte Zusteller-Nummer NICHT abrufbar. Sie erscheint laut Auftrag nur auf der Sendungsverfolgungs-WEBSEITE — vermutlich nur per Browser-Scraping erreichbar; dieses Projekt hat dafür bereits eine Playwright-Infrastruktur (`aliexpress.ts`), aber nicht für diesen Zweck ausgebaut oder getestet — eigener, separater Auftrag nötig.

**Ergebnis Aufgabe 3 (Cron umgestellt, AP-Werte werden nie geschrieben):** Neue reine Funktion `isAliInternalLogisticsId()` (`aliexpress-api.ts`, Regex `/^AP\d+$/i`, per Test mit den echten Live-Werten beider Bestellungen belegt: `aliexpress-api.test.ts`). `getAliOrderTracking()` filtert AP-Werte jetzt konsequent heraus, BEVOR sie überhaupt als `trackingNumber` zurückgegeben werden — dadurch ist der Filter für ALLE Aufrufer (Cron, Trockenlauf-Skripte, künftige Call-Sites) an genau einer Stelle wirksam (Grundgesetz Regel 8). Folge, ehrlich benannt statt versteckt: da beide bisher beobachteten Bestellungen ausschließlich AP-Werte liefern, liefert `getAliOrderTracking()` für sie `trackingNumber: null` — der Cron schreibt aktuell NICHTS, bis eine echte Quelle für die Zusteller-Nummer gefunden ist. `ALIEXPRESS_TRACKING_SYNC_ENABLED` zurück auf `false` gesetzt (Kommentar in `tracking-sync.ts` erklärt warum).

**Ergebnis Aufgabe 4 (erneuter Trockenlauf, Abweichung erklärt statt angepasst):** `scripts/output/tracking-sync-preview-alle-offenen.md` neu ausgeführt: **0 von 2** Bestellungen liefern jetzt eine Sendungsnummer (vorher 1 von 2 — der AP-Wert, der jetzt korrekt verworfen wird). Erwarteter Wert aus dem Auftrag (`00340434886283998797` für `3075188992327211`) wird NICHT erreicht — Grund: dieser Wert ist über keinen der beiden getesteten Endpunkte abrufbar (s. Aufgabe 2). Keine Anpassung des Codes, um den erwarteten Wert künstlich zu erzeugen — die Abweichung wird hier und im PR offen benannt.

**Ergebnis Zusatzbefund ("41 Tage, evtl. verloren" trotz echter Zustellung):** Logik in `GET /gmail/review-request-suggestions` (`index.ts:1655-1789`) gelesen und gegen die echte `order_notes`-Zeile von eBay `27-14918-13809` geprüft (read-only). Die Warnung wertet AUSSCHLIESSLICH zwei Dinge aus — NICHT den echten eBay-/AliExpress-Zustellstatus (der ist über keine der beiden Plattformen per API zuverlässig abrufbar, s. bestehende P-96/P-97-Kommentare im Code): (1) ob eine Gmail-Zustellmail gefunden UND per Sendungsnummer ODER Adresse zugeordnet werden konnte (`searchRecentDeliveryEmails(days = 30)`, `gmail.ts:299` — durchsucht NUR die letzten 30 Tage); (2) verstrichene Zeit seit `shippedAt` (oder `order.orderDate` als Näherung) — ab 25 Tagen (`POSSIBLY_LOST_MS`) ohne zugeordnete Mail erscheint die Warnung statt eines Bewertungsbitte-Vorschlags. Echte DB-Zeile `27-14918-13809`: `trackingNumber: null`, `shippedAt: "2026-08-04T10:10:31.634Z"` → **41 Tage** (nachgerechnet, deckt sich exakt mit der App-Anzeige). Root Cause, zweifach: (a) die Sendungsnummer-Zuordnung der E-Mail-Suche kann strukturell nie greifen, weil `trackingNumber` nie eingetragen wurde (derselbe Bug wie P2 selbst); (b) selbst eine Adress-Zuordnung könnte nicht mehr greifen, weil die tatsächliche Zustellung (05.08., laut DHL-Trackinginfo-Aufruf oben) inzwischen 41 Tage zurückliegt — außerhalb des 30-Tage-Suchfensters von `searchRecentDeliveryEmails()`. Die Warnung ist damit KEIN eigenständiger Bug, sondern eine erwartbare Folge derselben fehlenden Sendungsnummer plus des 30-Tage-Suchfensters — kein Fix in diesem PR (nicht Teil des Auftrags), nur Ursache benannt.

Regressions-Beweis (Grundgesetz Regel 5): `isAliInternalLogisticsId()` testweise auf `return false` zurückgesetzt → 2 von 4 Tests schlugen korrekt fehl → zurückgesetzt → wieder 4/4 grün.

Tests: Frontend-Typecheck (`bun run typecheck`) grün. Server-Typecheck (`bun run typecheck:server`) grün. `bun test src/api/aliexpress-api.test.ts src/api/tracking-sync.test.ts`: 13 pass, 0 fail, 32 expect(). Voller `bun test`: 121 pass, 2 fail — dieselben 2 vorbestehenden `/tmp/aliexpress-test.db`-Sandbox-Fehler, unabhängig von dieser Änderung.
Keine Änderung an `AUTO_PRICE_WRITE_ENABLED`/Preislogik (per Diff bestätigt: nur `aliexpress-api.ts`, `tracking-sync.ts`, zwei Test-Dateien, zwei neue Skripte + Output-Dateien betroffen). Keine DB-Migration. Kein Kundennachricht-Versand aus diesem Job (unverändert, `syncTrackingNumbers()` ruft keine Notify-Funktion auf). `ALIEXPRESS_TRACKING_SYNC_ENABLED` bleibt `false`.
Branch: `fix/p2-tracking-sync-enable` (derselbe Branch/dieselbe PR #102 — Korrektur, kein neuer PR). PR: https://github.com/contact-e-stele/stele-e-transfer/pull/102 (Draft, nicht gemergt, Beschreibung aktualisiert).

**Nachtrag (dieselbe Session):** Nutzer hat PR #102 selbst gemergt (verifiziert per `gh pr view 102`: `state=MERGED`).

## 2026-09-14 - Claude Sonnet 5 (Session 01Myk1sNTV8a7j8Gxsq9MAav)
**Claim:** Auftrag "P2 Teil 2 — Sendungsnummer aus den AliExpress-Mails statt aus der API". Voraussetzung PR #102 geprüft: gemergt (bestätigt oben). Branch `fix/p2-teil2-tracking-aus-mail` (isoliertes Worktree unter `.worktrees/`, Basis `origin/main`), Draft-PR. Aufgabe 0 (implizit): erst prüfen, was `gmail.ts` schon kann, nicht doppelt bauen. Aufgabe 1: Funktion für Mails von `transaction@notice.aliexpress.com` — DHL-Nummer aus dem Betreff ("Package <Nummer>"), AliExpress-Bestellnr. aus `o_ids=` im ROHEN HTML-Body. Aufgabe 2: Plausibilität — `isAliInternalLogisticsId` aus PR #102 wiederverwenden, plus Format-Check. Aufgabe 3: Cron auf diese Quelle umstellen, atomarer Doppelschreib-Schutz bleibt. Aufgabe 4: Suchfenster mindestens 90 Tage (PR #102 zeigte eine 41 Tage alte Bestellung). Verifikation: Trockenlauf gegen echtes Gmail+DB als Datei im PR, Abgleich gegen zwei konkrete erwartete Werte, typecheck+Testsuite im Klartext. Strikte Grenzen: `AUTO_PRICE_WRITE_ENABLED`/Preislogik unangetastet, keine Migration, keine Kundennachricht, Mails nicht verändern/als gelesen markieren, `ALIEXPRESS_TRACKING_SYNC_ENABLED` bleibt `false` bis belegt korrekt.

**Ergebnis Aufgabe 0 (nicht doppelt gebaut):** `gmail.ts` hatte bereits zwei Parser (P-84 `parseTrackingEmail`, P-85 `parseDeliveryEmail`) — beide matchen einen FESTEN Status-Suffix im Betreff, ordnen Bestellungen über Adress-Matching zu (nicht Bestellnummer), und lesen den Klartext-/gestrippten Body, nicht den rohen HTML-Body. Keine vorhandene Funktion extrahiert `o_ids=` oder matcht generisch "Package <Nummer>" unabhängig vom Status-Suffix — echte neue Funktionalität nötig.

**Ergebnis Aufgabe 1 (neuer Parser):** `parsePackageStatusEmail(subject, rawHtmlBody)` in `gmail.ts` — matcht jeden "Package <Nummer>"-Betreff dieses Absenders unabhängig vom Status-Suffix, extrahiert die AliExpress-Bestellnr. aus `o_ids=` im rohen HTML-Body (neue Funktion `findRawHtmlBody()`, parallel zu `findPlainTextBody()`). **Live-Fund beim ersten echten Testlauf gegen eine realistische Fixture (Grundgesetz Regel 1):** HTML kodiert das "&" zwischen Query-Parametern in `href`-Attributen als Entity "&amp;" — die erste Regex-Fassung (`/[?&]o_ids=(\d+)/`) fand dadurch 0 Treffer, obwohl Betreff-Parsing und Guards korrekt waren (per direktem Debug-Skript isoliert). Fix: `/[?&](?:amp;)?o_ids=(\d+)/i`. Regressions-Beweis: Regex testweise auf die ursprüngliche Fassung zurückgesetzt → 4 von 12 Tests schlugen korrekt fehl → zurückgesetzt → 12/12 grün.

**Ergebnis Aufgabe 2 (Plausibilität):** `isAliInternalLogisticsId()` aus PR #102 (`aliexpress-api.ts`) direkt importiert und wiederverwendet (Grundgesetz Regel 8, kein Duplikat) — AP-Werte werden nie übernommen. Neue `looksLikeCarrierTrackingNumber()`: rein numerisch, mindestens 10 Ziffern (beide bekannten echten DHL-Nummern haben 20 Ziffern).

**Ergebnis Aufgabe 3 (Cron umgestellt):** `syncTrackingNumbers()` in `tracking-sync.ts` nutzt keinen AliExpress-API-Call mehr — eine Gmail-Suche pro Lauf (`searchRecentPackageStatusEmails()`), Treffer zu einer Map Bestellnr.→Nummer zusammengefasst, pro Ziel-Bestellung ein `Map.get()`. Gemeinsame Gmail-List-/Fetch-Logik in `fetchAliexpressMessages()` extrahiert, damit sie für P-84/P-85/P2-Teil-2 nur einmal existiert — bestehendes Verhalten der beiden alten Funktionen dabei unverändert erhalten (geprüft: unveränderte Ergebnis-Semantik, nur die interne Quelle des rohen Fetches geteilt). Der atomare Doppelschreib-Schutz aus PR #102 (`writeTrackingNumberToDb()`, WHERE-Klausel prüft Leer/NULL beim Schreiben selbst) blieb unangetastet.

**Ergebnis Aufgabe 4 (Suchfenster):** `MAIL_SEARCH_WINDOW_DAYS = 90` in `tracking-sync.ts`, als Default an `searchRecentPackageStatusEmails(days = 90)` durchgereicht.

**Ergebnis Aufgabe 5+6 (Trockenlauf, Einschränkung ausdrücklich benannt statt verschwiegen):** Kein echter Gmail-API-Aufruf aus dieser Sandbox möglich — `GOOGLE_GMAIL_CLIENT_ID`/`GOOGLE_GMAIL_CLIENT_SECRET` fehlen in `.env` (anders als `TURSO_DATABASE_URL`/`TURSO_AUTH_TOKEN`, die vorhanden sind). Echter Fehlversuch: `[Gmail] Token-Refresh fehlgeschlagen: 400 {"error":"invalid_request","error_description":"Could not determine client ID from request."}` → `error: Gmail nicht verbunden`. `scripts/inspect-package-status-emails.ts` (NUR LESEND) steht bereit für den echten Lauf, sobald Gmail-Zugangsdaten verfügbar sind — aktueller Lauf dokumentiert nur den Blocker (`scripts/output/package-status-emails-preview.md`). Stattdessen: `scripts/preview-tracking-sync-from-mail.ts` — Trockenlauf gegen die echte Produktions-DB (reale Ziel-Bestellungen, keine erfundenen Daten) mit den vom Nutzer selbst manuell verifizierten Mail-Werten als Override statt eines echten Gmail-Aufrufs. Ergebnis: **eBay 27-14918-13809 / AliExpress 3075188992327211 → 00340434886283998797** (✅ Aufgabe 6, exakt der erwartete Wert). **Wichtiger Fund zu Aufgabe 5:** eBay 20-15127-76586 / AliExpress 3076306514497211 (Yuecel Karakoca) ist zum Zeitpunkt dieses Laufs KEIN Ziel mehr — per echter, read-only DB-Abfrage bestätigt trägt `order_notes.tracking_number` bereits `00340434886289512140` (`shippedAt` 2026-09-14T14:52:39.016Z, vermutlich vom Nutzer manuell nachgetragen nach PR #102) — stimmt exakt mit dem erwarteten Wert überein, unabhängige Bestätigung außerhalb der Pipeline. Abweichung erklärt, nicht angepasst.

Tests: Frontend-Typecheck (`bun run typecheck`) grün. Server-Typecheck (`bun run typecheck:server`) grün. `bun test src/api/gmail.test.ts src/api/tracking-sync.test.ts src/api/aliexpress-api.test.ts`: 26 pass, 0 fail, 49 expect(). Voller `bun test`: 134 pass, 2 fail — dieselben 2 vorbestehenden `/tmp/aliexpress-test.db`-Sandbox-Fehler, unabhängig von dieser Änderung.
Keine Änderung an `AUTO_PRICE_WRITE_ENABLED`/Preislogik (per Diff bestätigt: nur `gmail.ts`, `tracking-sync.ts`, zwei Test-Dateien, drei neue Skripte + Output-Dateien betroffen). Keine DB-Migration. Kein Kundennachricht-Versand aus diesem Job. Mails nicht verändert/als gelesen markiert (`fetchAliexpressMessages()` ruft ausschließlich `GET /messages`/`GET /messages/{id}` auf, kein `modify`/`trash`-Aufruf im gesamten Modul). `ALIEXPRESS_TRACKING_SYNC_ENABLED` bleibt `false`.
Branch: `fix/p2-teil2-tracking-aus-mail` (isoliertes Worktree unter `.worktrees/`, Basis `origin/main`). PR: https://github.com/contact-e-stele/stele-e-transfer/pull/103 (Draft, nicht gemergt).

**Nachbesserung (dieselbe Session, 2026-09-14):** Nutzer hat im echten Postfach nachgesehen — der Betreff-Parser war zu eng. AliExpress verschickt die Paket-Status-Mails deutsch UND englisch, mit stark unterschiedlichem Wortlaut; "Package <Nummer>" traf nur die englischen Varianten, alle acht echten Mails zur Bestellung 3075188992327211 waren deutsch und wurden übersehen. Echte Betreffzeilen: "Paket X wurde zugestellt", "Packstück X: mit lokalem Kurier", "Packstück X: beim Zoll", "Zollabfertigung für X wurde beendet", "Package X has cleared customs", "Package X: at customs".

**Fix:** `parsePackageStatusEmail()` zieht jetzt jede zusammenhängende Ziffernfolge ab 10 Stellen aus dem Betreff (statt auf ein festes Einleitungswort zu matchen), filtert über `looksLikeCarrierTrackingNumber()`/`isAliInternalLogisticsId()` (aus PR #102 wiederverwendet). Das einleitende Wort spielt keine Rolle mehr. `fetchAliexpressMessages()` akzeptiert jetzt einen optionalen Betreff-Suchbegriff; `searchRecentPackageStatusEmails()` sucht ohne Betreff-Einschränkung — Absender bleibt der einzige Gmail-seitige Filter.

**Ehrlich dokumentierter Grenzfall (Grundgesetz Regel 6):** eine reine `\d{10,}`-Ziffernfolge kann das AP-Präfix strukturell nie enthalten — `isAliInternalLogisticsId()` ist für die Betreff-Extraktion dadurch ein No-op ("Package AP00843143208329: at customs" → extrahiert "00843143208329", besteht die Plausibilität). In der Praxis unkritisch: in keinem bisher beobachteten echten Fall taucht ein AP-Wert im Betreff auf, nur im API-Feld `logistics_no`. Neuer Test dokumentiert diesen Ist-Zustand ausdrücklich.

Tests: alle sechs echten Betreffzeilen als Fixtures (`gmail.test.ts`), plus ein Test mit einem frei erfundenen deutschen Einleitungswort zum Beleg, dass wirklich jedes Wort funktioniert. Regressionsbeweis: alte "Package"-only-Logik testweise wiederhergestellt → 5 von 16 Tests (alle vier deutschen Fixtures + der wortlaut-unabhängige Test) schlugen korrekt fehl → zurückgesetzt → 16/16 grün. Frontend-/Server-Typecheck grün. `bun test src/api/gmail.test.ts`: 16 pass, 0 fail. Voller `bun test`: 138 pass, 2 fail (dieselben 2 vorbestehenden, unabhängigen Sandbox-Fehler).

Keine Änderung an `AUTO_PRICE_WRITE_ENABLED`/Preislogik, keine DB-Migration, keine Kundennachricht. `ALIEXPRESS_TRACKING_SYNC_ENABLED` weiterhin `false`. PR-Beschreibung um die Nachbesserung ergänzt (vorangestellt, ursprünglicher Inhalt bleibt darunter erhalten).

**Nachtrag (dieselbe Session):** Nutzer hat PR #103 gemergt (verifiziert per `gh pr view 103`: `state=MERGED`, `mergedAt=2026-09-14T16:29:20Z`).

## 2026-09-14 - Claude Sonnet 5 (Session 01Myk1sNTV8a7j8Gxsq9MAav)
**Claim:** Nachtrag zu PR #103 (nach dem Merge, echter Lauf in der Render-Shell durch den Nutzer): 26 Treffer geliefert, älteste Mail darin vom 04.09.2026 — eine nachweislich im Postfach vorhandene passende Mail vom 04.08.2026 (Betreff "Packstück 00340434886283998797: mit lokalem Kurier", Body enthält `o_ids=3075188992327211`) fehlte im Ergebnis, obwohl innerhalb des 90-Tage-Fensters. Nutzer-Verdacht: `fetchAliexpressMessages()` paginiert nicht über `nextPageToken`. Auftrag: prüfen, falls bestätigt Pagination ergänzen mit Obergrenze gegen Endlosläufe, danach Inspektions-Skript erneut laufen lassen. Branch `fix/p2-teil2-gmail-pagination` (isoliertes Worktree unter `.worktrees/`, Basis `origin/main`, da PR #103 bereits gemergt war), Draft-PR.

**Ergebnis (Root Cause bestätigt):** Gmail's `messages.list` garantiert NICHT, dass eine Antwort bis zu `maxResults` Treffer enthält, auch wenn mehr verfügbar sind — sie kann früher abschneiden und stattdessen ein `nextPageToken` zurückgeben, das verfolgt werden MUSS, um an die übrigen (typischerweise älteren) Treffer zu kommen (dokumentiertes Gmail-API-Verhalten). `fetchAliexpressMessages()` ignorierte dieses Token komplett — das 90-Tage-Fenster war im `q`-Parameter korrekt gesetzt, griff aber nie, weil die Ergebnisliste vorher serverseitig abgeschnitten wurde.

**Fix:** Paginierungs-Schleife ergänzt (alle Seiten bis kein `nextPageToken` mehr da ist ODER `GMAIL_LIST_MAX_PAGES=20` erreicht ist, 20×50=bis zu 1000 Treffer als Obergrenze gegen Endlosläufe). Die Schleife selbst als reine, DI-testbare Funktion `collectPaginatedIds()` extrahiert (Grundgesetz Regel 2) — genau die fehlende Testbarkeit hatte diesen Bug unentdeckt gelassen (Netzwerk-Layer bisher nur über Live-Skripte verifiziert).

**Tests:** vier neue Tests gegen einen fingierten mehrseitigen Datensatz, ohne echten Gmail-Zugriff nötig — Mehrseiten-Akkumulation, der exakt beobachtete Live-Fund nachgestellt (26+1 Treffer über zwei Seiten), Normalfall ohne unnötigen zweiten Request, Obergrenze gegen Endlos-Lauf. Regressionsbeweis: Schleife testweise auf "nur erste Seite" zurückgesetzt → 3 von 20 Tests schlugen korrekt fehl → zurückgesetzt → 20/20 grün.

**Einschränkung, ausdrücklich benannt (Grundgesetz Regel 1):** die vom Nutzer gewünschte Live-Bestätigung ("Inspektions-Skript erneut laufen lassen, erwartet: für 3075188992327211 wird 00340434886283998797 gefunden") ist aus dieser Sandbox weiterhin nicht möglich — dieselbe fehlende `GOOGLE_GMAIL_CLIENT_ID`/`_SECRET`-Einschränkung wie in der vorherigen Session. `scripts/inspect-package-status-emails.ts` erneut ausgeführt: identischer, erwarteter Fehlschlag (`[Gmail] Token-Refresh fehlgeschlagen: 400 ... Could not determine client ID from request`) — bestätigt, dass der Code bis zum Netzwerkaufruf fehlerfrei läuft, aber der eigentliche Live-Beweis bleibt beim Nutzer (Render-Shell) ausstehend.

Zusatzinfo des Nutzers übernommen (kein Codeänderungsbedarf): eine zweite Mail-Familie dieses Absenders ("Bestellung <Bestellnummer>: <Status>") enthält KEINE Sendungsnummer im Betreff (nur "Verfolgung Ihrer Sendung" + Link) — trägt strukturell nichts bei, `parsePackageStatusEmail()` findet dort korrekt keine qualifizierende Ziffernfolge und liefert `null`, kein Fehlverhalten.

Tests: Frontend-Typecheck (`bun run typecheck`) grün. Server-Typecheck (`bun run typecheck:server`) grün. `bun test src/api/gmail.test.ts`: 20 pass, 0 fail, 26 expect(). Voller `bun test`: 142 pass, 2 fail — dieselben 2 vorbestehenden, unabhängigen `/tmp/aliexpress-test.db`-Sandbox-Fehler.
Keine Änderung an `AUTO_PRICE_WRITE_ENABLED`/Preislogik, keine DB-Migration, keine Kundennachricht. Mails weiterhin nicht verändert/als gelesen markiert (nur zusätzliche `GET /messages`-Aufrufe für weitere Seiten, kein `modify`/`trash`). `ALIEXPRESS_TRACKING_SYNC_ENABLED` bleibt `false`.
Branch: `fix/p2-teil2-gmail-pagination` (isoliertes Worktree unter `.worktrees/`, Basis `origin/main`). PR: https://github.com/contact-e-stele/stele-e-transfer/pull/104 (Draft, nicht gemergt).

**Nachtrag (dieselbe Session):** Nutzer hat PR #104 gemergt (verifiziert per `gh pr view 104`: `state=MERGED`, `mergedAt=2026-09-14T18:40:25Z`) und den Pagination-Fix danach in der Render-Shell gegen das echte Gmail-Postfach getestet — beide Referenzwerte (3076306514497211, 3075188992327211) exakt gefunden.

## 2026-09-14 - Claude Sonnet 5 (Session 01Myk1sNTV8a7j8Gxsq9MAav)
**Claim:** Auftrag "P2 FINALE — Sendungsnummer-Cron scharf schalten". Voraussetzung PR #104 geprüft: gemergt (bestätigt oben). Branch `fix/p2-finale-sync-enable` (isoliertes Worktree unter `.worktrees/`, Basis `origin/main`), Draft-PR. Aufgabe 1: `ALIEXPRESS_TRACKING_SYNC_ENABLED` auf `true`, Intervall bleibt 4h. Aufgabe 2: letzter Trockenlauf gegen die echte DB vor dem Scharfschalten, Ergebnis als Datei im PR. Aufgabe 3: bestätigen, dass der atomare Doppelschreib-Schutz aus PR #102 unverändert greift. Verifikation: Trockenlauf-Datei, ausdrückliche Bestätigung (keine Kundennachricht, keine Mail-Veränderung), typecheck+Testsuite im Klartext. Strikte Grenzen: `AUTO_PRICE_WRITE_ENABLED`/Preislogik unangetastet, keine Migration, Draft-PR.

**Ergebnis Aufgabe 1:** `ALIEXPRESS_TRACKING_SYNC_ENABLED = true` (`tracking-sync.ts:67`). Intervall unverändert 4h.

**Ergebnis Aufgabe 2 (letzter Trockenlauf, echte DB):** `scripts/preview-tracking-sync-final.ts`, Ergebnis-Datei `scripts/output/tracking-sync-final-preview.md`. Reine DB-Zielliste bewusst UNABHÄNGIG vom Gmail-Ergebnis abgefragt (Grund: `syncTrackingNumbers()` fängt einen fehlschlagenden Gmail-Aufruf intern ab und liefert dann ein leeres Ergebnis zurück, obwohl echte Ziele existieren können — das hätte den Bericht sonst irreführend "keine offenen Bestellungen" zeigen lassen, tatsächlich fand sich beim ersten Versuch **1 echte Ziel-Bestellung**). Ergebnis: **genau 1 aktuell offene Ziel-Bestellung** — eBay `26-15134-85187` / AliExpress `3076295709557211`. Beide zuvor bekannten Referenz-Bestellungen (Karakoca, Lazarevic) haben inzwischen bereits eine Sendungsnummer eingetragen und sind damit korrekt kein Ziel mehr. Einschränkung ausdrücklich benannt: ein echter Gmail-Aufruf ist aus dieser Sandbox weiterhin nicht möglich (`GOOGLE_GMAIL_CLIENT_ID`/`_SECRET` fehlen in `.env`) — ob `3076295709557211` bereits eine Mail-Nummer hat, zeigt erst der erste scharfe Lauf in Produktion.

**Ergebnis Aufgabe 3 (Doppelschreib-Schutz bestätigt):** `writeTrackingNumberToDb()` (`tracking-sync.ts:128-139`) trägt weiterhin dieselbe Leer/NULL-Bedingung atomar in der `WHERE`-Klausel des Updates selbst — per `git diff` gegen PR #102 bestätigt: keine Änderung an dieser Funktion.

**Ergebnis Aufgabe 5 (ausdrücklich bestätigt, per grep belegt):** `grep -n "modify\|trash\|/send\|batchModify" src/api/gmail.ts` → kein Treffer (nur ein erklärender Kommentar). `grep -in "notify\|nachricht\|customerNotified\|sendMail\|sendMessage" src/api/tracking-sync.ts` → kein Treffer. Keine Kundennachricht aus diesem Job, keine Mail wird als gelesen markiert oder verändert — `fetchAliexpressMessages()` ruft ausschließlich `GET /messages`/`GET /messages/{id}` auf.

Regressions-Beweis (Grundgesetz Regel 5): `ALIEXPRESS_TRACKING_SYNC_ENABLED` testweise auf `false` zurückgesetzt → zugehöriger Test schlug korrekt fehl (9 pass, 1 fail) → zurückgesetzt → wieder 10/10 grün.

Tests: Frontend-Typecheck (`bun run typecheck`) grün. Server-Typecheck (`bun run typecheck:server`) grün. `bun test src/api/tracking-sync.test.ts`: 10 pass, 0 fail, 26 expect(). Voller `bun test`: 142 pass, 2 fail — dieselben 2 vorbestehenden, unabhängigen `/tmp/aliexpress-test.db`-Sandbox-Fehler.
Keine Änderung an `AUTO_PRICE_WRITE_ENABLED`/Preislogik, keine DB-Migration. `ALIEXPRESS_TRACKING_SYNC_ENABLED` jetzt `true`.
Branch: `fix/p2-finale-sync-enable` (isoliertes Worktree unter `.worktrees/`, Basis `origin/main`). PR: https://github.com/contact-e-stele/stele-e-transfer/pull/105 (Draft, nicht gemergt).

## 2026-09-14 - Claude Sonnet 5 (Session 01Myk1sNTV8a7j8Gxsq9MAav)
**Claim:** Auftrag "P-82 Listing-Profil Teil 1 — Shop-Kategorie beim Listen mitsenden". Befund: neu eingestellte Angebote landen in Shop-Kategorie "Sonstiges", ältere manuell gesetzte Angebote sind korrekt. Branch `feat/p82-shop-kategorie` (isoliertes Worktree unter `.worktrees/`, Basis `origin/main`), Draft-PR. Aufgabe 1: Kategoriebaum-Endpunkt identifizieren, bei fehlendem Zugriff/Scope ausdrücklich sagen statt Workaround. Aufgabe 2: additives Feld am Produkt. Aufgabe 3: Dropdown im Import-Tab, kein Freitext. Aufgabe 4: Kategorie beim Listen mitsenden, im Code prüfen welcher Pfad tatsächlich listet. Aufgabe 5: ohne hinterlegte Kategorie nichts mitsenden, kein "Sonstiges"-Fallback. Verifikation: Kategorieliste als Datei, Trockenlauf-Offer-Payload als Datei, typecheck+Testsuite im Klartext. Strikte Grenzen: kein bestehendes Angebot ändern, kein Bulk-Revise, `AUTO_PRICE_WRITE_ENABLED`/`ALIEXPRESS_TRACKING_SYNC_ENABLED`/Preislogik unangetastet, additive Migration, Draft-PR.

**Ergebnis Aufgabe 1 (Endpunkt recherchiert, Blocker benannt):** Trading API `GetStore` (`CategoryStructureOnly=true`) — recherchiert gegen developer.ebay.com. Response: `Store.CustomCategories.CustomCategory[]` mit `CategoryID`/`Name`/`Order`, optional verschachtelte `ChildCategory` (bis 3 Ebenen). Auth: derselbe `eBayAuthToken`-Mechanismus wie die bereits produktiv genutzten `GetFeedback`/`GetSellerList`/`ReviseFixedPriceItem` — kein zusätzlicher OAuth-Scope in der Doku gefunden. **Ausdrücklich benannt statt Workaround gebaut:** kein echter Aufruf aus dieser Sandbox möglich (`EBAY_CLIENT_ID`/`_SECRET`/`_REFRESH_TOKEN` leer in `.env`) — echter Fehlversuch dokumentiert: `eBay OAuth failed: 401 {"error":"invalid_client","error_description":"client authentication failed"}` (derselbe Blocker wie PR #97). `scripts/fetch-store-categories.ts` steht bereit.

**Ergebnis Aufgabe 2 (additive Migration, real ausgeführt):** `products.store_category_id`/`store_category_name` (beide TEXT, nullable, kein Default — gleicher Stil wie `ebay_category`/`gpsr_name`). Gegen die echte Produktions-DB ausgeführt (`bun src/db/migrate.ts`, rein additiv/idempotent): beide Spalten erfolgreich angelegt, alle 33 vorherigen Migrationen korrekt übersprungen.

**Ergebnis Aufgabe 3 (Dropdown im Import-Tab):** `lieferanten.tsx`, neues Dropdown "Shop-Kategorie" aus `GET /ebay/store-categories` (neuer Endpunkt, Server-Cache 1h) befüllt. Kein Freitext.

**Ergebnis Aufgabe 4 (im Code geprüft, zwei Pfade verdrahtet):** `listOnEbay()` verzweigt in `createOffer()` (Einzelartikel) ODER eine eigene, inline Offer-Konstruktion in `listOnEbayWithVariants()` (Varianten/Gruppen — genau die `-GROUP`-SKUs aus dem Befund). Beide bauen unabhängig ihren Offer-Body — beide jetzt über eine gemeinsame Funktion `buildStoreCategoryBlock()` (Regel 8) verdrahtet, liefert `storeCategoryNames` im dokumentierten Pfad-Format.

**Ergebnis Aufgabe 5:** `buildStoreCategoryBlock(undefined)` liefert `{}` — `storeCategoryNames` fehlt dann komplett im Payload, kein "Sonstiges"-Ersatzwert. Per Test belegt.

**Live-Fund beim ersten echten Testlauf (Grundgesetz Regel 1):** realistische XML-Fixture (Struktur laut Doku) deckte auf: Kategorienamen mit "&" kamen als unveränderte XML-Entity "&amp;" durch. `decodeXmlEntities()` ergänzt. Regressionsbeweis: Decoder auf No-op zurückgesetzt → 4 von 9 Tests schlugen korrekt fehl → zurückgesetzt → 9/9 grün.

**Ergebnis Aufgabe 6+7 (Verifikation):** `scripts/output/store-categories.md` dokumentiert den echten Abruf-Fehlversuch — keine erfundenen Kategorien. `scripts/output/offer-payload-preview.md`: Trockenlauf gegen ein echtes Produkt aus der Produktions-DB (stele-49) über `buildStoreCategoryBlock()` (dieselbe Produktionsfunktion) — Fall A (keine Kategorie hinterlegt): `storeCategoryNames` fehlt komplett; Fall B (illustratives Beispiel "/Wohnen & Möbel", ausdrücklich als NICHT echt markiert): Feld erscheint korrekt. Kein echter eBay-Call.

Tests: Frontend-Typecheck (`bun run typecheck`) grün. Server-Typecheck (`bun run typecheck:server`) grün. `bun test src/api/ebay.test.ts`: 9 pass, 0 fail, 9 expect(). Voller `bun test`: 151 pass, 2 fail — dieselben 2 vorbestehenden, unabhängigen Sandbox-Fehler.
Keine Änderung an `AUTO_PRICE_WRITE_ENABLED`/`ALIEXPRESS_TRACKING_SYNC_ENABLED`/Preislogik (per `git diff origin/main` bestätigt: leer für `pricing.ts`/`price-monitor.ts`/`tracking-sync.ts`). Kein bestehendes Angebot verändert, kein Bulk-Revise (kein Treffer für `reviseCategory`/Bulk-Logik im Diff). Migration additiv, real gegen die Produktions-DB verifiziert.
Branch: `feat/p82-shop-kategorie` (isoliertes Worktree unter `.worktrees/`, Basis `origin/main`). PR: https://github.com/contact-e-stele/stele-e-transfer/pull/106 (Draft, nicht gemergt).

**Nachtrag (dieselbe Session):** Nutzer hat PR #106 gemergt (verifiziert per `gh pr view 106`: `state=MERGED`, `mergedAt=2026-09-14T19:18:53Z`).

## 2026-09-14 - Claude Sonnet 5 (Session 01Myk1sNTV8a7j8Gxsq9MAav)
**Claim:** Auftrag "P-81 Listing-Profil Teil 2 — Anzeigentarif automatisch setzen, STUFE 1 (nur vorbereiten/prüfen)". Voraussetzung PR #106 geprüft: gemergt (bestätigt oben). Branch `feat/p81-anzeigentarif-stufe1` (isoliertes Worktree unter `.worktrees/`, Basis `origin/main`), Draft-PR. WICHTIGSTE GRENZE (wörtlich aus dem Auftrag): eine Automatik darf Anzeigen NIEMALS pauschal über den Bestand hinweg wieder anschalten — 9 von 13 echten Bestellungen hatten eine Anzeigen-Gebührenzeile, 4 nicht, der Nutzer hat manche bewusst gestoppt. Aufgabe 1: OAuth-Scope für Sell Marketing API klären, prüfen ob vorhanden, bei fehlendem Scope Neu-Autorisierung ankündigen statt Workaround. Aufgabe 2: getCampaigns prüfen, nicht selbst anlegen. Aufgabe 3: adRate-Spalte real prüfen, bei Bedarf additive Migration mit Default NULL. Aufgabe 4: Import-Tab-Feld "Anzeigentarif (%)", Vorgabe 5, leer/0 = ausdrücklich keine Anzeige. Aufgabe 5: noch nichts an eBay senden. Verifikation: Scope-Status+Kampagnenliste als Dateien, Bericht SKU/adRate/Live-Werbestatus für alle aktiven Produkte, typecheck+Testsuite im Klartext. Strikte Grenzen: kein Angebot beworben/keine Kampagne angelegt/kein Gebot geändert, `AUTO_PRICE_WRITE_ENABLED`/`ALIEXPRESS_TRACKING_SYNC_ENABLED`/Preislogik unangetastet, additive Migration mit Default NULL, Draft-PR.

**Ergebnis Aufgabe 1 (Scope geprüft per Code-Inspektion, nicht Vermutung):** benötigter Scope `sell.marketing`/`sell.marketing.readonly` (Doku-Recherche, getCampaigns). Aktueller Token hat ihn NICHT — bewiesen: `getOAuthUrl()`/`getAccessToken()` fordern exakt drei Scopes an (`sell.inventory`, `sell.account`, `sell.fulfillment`), `sell.marketing` war nie Teil der Anfrage, ein Refresh-Token trägt nur die beim Consent gewährten Scopes. Nutzer muss einmal neu autorisieren — ausdrücklich gesagt, kein Workaround. `getOAuthUrl()` um `sell.marketing` (bewusst die breitere Schreib-Variante statt nur `.readonly`, eine einzige Neu-Autorisierung deckt Stufe 1+spätere Schreib-Stufe ab) erweitert. `getAccessToken()` bewusst UNVERÄNDERT gelassen — wird von jedem bestehenden Inventory-/Account-/Fulfillment-Aufruf genutzt, ein nicht gewährter Zusatz-Scope hätte bei Ablehnung durch eBay alles gefährden können. Neue, isolierte `getMarketingAccessToken()` stattdessen.

**Ergebnis Aufgabe 2 (Kampagnen-Check, nur lesend):** `getCampaigns()` implementiert (`GET /sell/marketing/v1/ad_campaign`, reiner Lesezugriff, keine Kampagne angelegt). Aus dieser Sandbox nicht live abrufbar — zwei Blocker dokumentiert (`scripts/output/marketing-scope-and-campaigns.md`): (1) derselbe „401 invalid_client" wie bei jedem eBay-Call hier (PR #97/#106), (2) selbst mit gültigen Zugangsdaten würde der bestehende Refresh-Token am fehlenden Scope aus Aufgabe 1 scheitern.

**Ergebnis Aufgabe 3 (wichtiger Fund, real gegen Produktions-DB geprüft):** `ad_rate` existiert BEREITS als echte, befüllte Spalte (`PRAGMA table_info(products)`: REAL, `dflt_value=5`) — KEINE Migration nötig, die in Aufgabe 3 vorgesehene Bedingung trifft nicht zu, hier offengelegt statt stillschweigend eine unnötige Migration angehängt. ABER: alle 56 Produkte haben exakt denselben Wert 5 — die Spalte kann "ausdrücklich gewählt" nicht von "nie angefasst, nur Default" unterscheiden. Genau die Ambiguität, vor der die WICHTIGSTE GRENZE warnt.

**Ergebnis Aufgabe 4 (Import-Tab-Feld existierte schon, hatte Bug für "keine Anzeige"):** Feld "Anzeigentarif" existierte bereits (`lieferanten.tsx`), persistierte bereits als `adRate`, erlaubte aber technisch NIE den Wert 0 (`min="1"`, `v > 0`) — genau der von Aufgabe 4 verlangte "keine Anzeige"-Wert. Fix: `min="0"`, neuer "Keine Anzeige"-Button, Hilfetext. Pricing-Formel verifiziert (`(ebayFeeRatePercent + adRatePercent)/100 × vatFactor`, rein additiv) — `adRatePercent=0` unproblematisch, `pricing.ts` unverändert.

**Ergebnis Aufgabe 5:** nichts an eBay gesendet — keine neue Schreiblogik, `getCampaigns()`/`getMarketingAccessToken()` nirgends außer im Verifikations-Skript aufgerufen.

**Ergebnis Aufgabe 6+7 (Verifikation, echte DB-Abfrage statt Annahme):** `scripts/output/adrate-vs-live-status.md` — 38 aktive Produkte (ebayStatus="listed"), SKU+adRate. **Alle 38 haben adRate=5, 0 mit adRate=0, 0 mit adRate=NULL** — bestätigt die Ambiguität aus Aufgabe 3 mit echten Zahlen; darunter die vom Auftrag namentlich genannten stele-156/157/155/153 (laut Cockpit-Befund unbeworben, in der DB aber ununterscheidbar von den ausdrücklich beworbenen). Spalte "aktuell beworben? (eBay live)" bleibt leer statt erfunden.

Regressions-Beweis (Grundgesetz Regel 5): Filter in `parseGetCampaignsResponse()` testweise entfernt → 1 von 14 Tests schlug korrekt fehl → zurückgesetzt → 14/14 grün.

Tests: Frontend-Typecheck (`bun run typecheck`) grün. Server-Typecheck (`bun run typecheck:server`) grün. `bun test src/api/ebay.test.ts`: 14 pass, 0 fail, 15 expect(). Voller `bun test`: 156 pass, 2 fail — dieselben 2 vorbestehenden, unabhängigen Sandbox-Fehler.
Keine Änderung an `AUTO_PRICE_WRITE_ENABLED`/`ALIEXPRESS_TRACKING_SYNC_ENABLED`/Preislogik (per `git diff origin/main` bestätigt: leer für `pricing.ts`/`price-monitor.ts`/`tracking-sync.ts`). Kein Angebot beworben, keine Kampagne angelegt, kein Gebot geändert. Keine Migration (belegt, nicht angenommen — s. Aufgabe 3).
Branch: `feat/p81-anzeigentarif-stufe1` (isoliertes Worktree unter `.worktrees/`, Basis `origin/main`). PR: https://github.com/contact-e-stele/stele-e-transfer/pull/107 (Draft, nicht gemergt).

## 2026-09-15 - Claude Sonnet 5 (Session 01XZSN7aP4NMsSnsdRtVPGyk)
**Claim:** Auftrag "eBay neu verbinden — Knopf fehlt in der Oberfläche". Voraussetzung PR #107 geprüft: gemergt in `origin/main` (bestätigt: `git fetch origin main`, Commit `570f9a1`). Branch `feat/ebay-reconnect-scope-anzeige` (isoliertes Worktree unter `.worktrees/`, Basis `origin/main`), Draft-PR. Aufgabe 1: Knopf "Neu verbinden" beim eBay-Zugang, analog AliExpress, ruft `getOAuthUrl()` auf. Aufgabe 2: anzeigen, welche Scopes im aktuellen Token stecken und ob `sell.marketing` dabei ist. Aufgabe 3: prüfen, ob der bestehende Rückkanal (Callback) den neuen Token korrekt speichert, ggf. ergänzen. Aufgabe 4: alter Refresh-Token darf nicht gelöscht werden, bevor der neue erfolgreich gespeichert ist. Verifikation: Klick-Anleitung nach Deploy in der PR, typecheck+Testsuite im Klartext. Strikte Grenzen: kein Angebot beworben/keine Kampagne angelegt/kein Gebot geändert, `AUTO_PRICE_WRITE_ENABLED`/`ALIEXPRESS_TRACKING_SYNC_ENABLED`/Preislogik unangetastet, keine DB-Migration, Draft-PR.

**Wichtiger Fund vor Implementierung (Aufgabe 3, per Code-Lesen belegt, nicht vermutet):** der bestehende Callback (`GET /ebay/callback`) tauschte den Code zwar erfolgreich gegen Tokens, **speicherte den neuen Refresh Token aber nirgends** — er landete nur in `console.log`, die Route antwortete mit `{ message: "Erfolgreich! Refresh Token in Server-Logs." }`. Der laufende Betrieb nutzte ausschließlich `EBAY_REFRESH_TOKEN` aus der Env-Variable. Ohne Fix wäre jede Neu-Autorisierung wirkungslos verpufft — genau der Kern des Auftrags. Design mit dem Nutzer abgestimmt (AskUserQuestion, vor Code): neuer Branch von `origin/main` statt vom alten, unfertigen `fix/p69-p74-versand-rundung`-Stand; Scope-Anzeige basiert auf der beim Callback tatsächlich angeforderten Scope-Liste (eBay liefert kein `scope`-Feld in der Token-Antwort, aber eBays Consent ist alles-oder-nichts — ein erfolgreicher Code-Exchange beweist, dass exakt die angeforderte Liste gewährt wurde).

**Ergebnis Aufgabe 1+3 (DB-gestützter Refresh-Token, AliExpress-Muster übernommen):** `packages/web/src/api/ebay.ts` — neue Funktionen `getRequestedScopeList()`, `hasScope()`, `getStoredEbayRefreshToken()` (DB zuerst via `app_settings`-Key `ebay_refresh_token`, Env-Fallback), `saveEbayRefreshToken()` (upsert, niemals Delete). `getAccessToken()`/`getMarketingAccessToken()` nutzen jetzt den DB-gestützten Token statt der fixen Modul-Konstante. `GET /ebay/callback` (`index.ts`) ruft `saveEbayRefreshToken()` jetzt NUR nach erfolgreichem `exchangeCodeForToken()` auf (im selben try-Block) und zeigt danach eine Bestätigungsseite statt der rohen JSON-Antwort. Neue Route `GET /ebay/status` liefert `{ connected, hasRefreshToken, scopes, hasMarketingScope, source, updatedAt }`.

**Ergebnis Aufgabe 4 (Alter Token nie vor neuem gelöscht):** strukturell durch Upsert (kein Delete-Aufruf existiert) UND dadurch, dass `saveEbayRefreshToken()` nur im Erfolgsfall aufgerufen wird — ein fehlgeschlagener Exchange erreicht die Funktion nie, der alte Token (DB oder Env) bleibt unverändert nutzbar. Echt gegen eine lokale Test-DB verifiziert (`api.ebay.com` aus dieser Sandbox nicht erreichbar, s.u.): Env-Token gesetzt → DB leer → `getStoredEbayRefreshToken()` liefert den Env-Wert; danach `saveEbayRefreshToken()` mit neuem Token aufgerufen → DB-Wert hat jetzt Vorrang, der alte Env-Wert bleibt dabei unverändert im Prozess-Environment stehen (Beweis, dass nichts überschrieben/gelöscht wird, was nicht explizit neu gespeichert wurde).

**Ergebnis Aufgabe 2 (Scope-Anzeige, inkl. Nutzer-Nachbesserung):** eBay-Karte in `einstellungen.tsx` zeigt jetzt Scope-Chips (grün = vorhanden) inkl. explizitem `sell.marketing`-Status, sowie den Knopf "Neu verbinden"/"Mit eBay verbinden" → `/api/ebay/auth`. Nutzer-Ergänzung nach Design-Freigabe umgesetzt: bei env-basiertem Token mit unbekanntem Scope steht jetzt ausdrücklich "⚠️ Marketing-Zugriff vermutlich nicht vorhanden — bitte neu verbinden" statt eines neutralen "unbekannt". **Fund aus dem Task-Review (vor PR-Erstellung gefixt, mit Nutzer abgestimmt):** wenn ein VORHANDENER Scope-Array `sell.marketing` nicht enthält (z.B. alter, vor PR #107 gespeicherter DB-Token), zeigte die erste Fassung nur grüne Chips für die vorhandenen Scopes — nichts wies auf das Fehlen von `sell.marketing` hin. Fix: zusätzlicher roter Chip "sell.marketing ✗ (fehlt)", wenn `hasMarketingScope=false` trotz vorhandenem Scope-Array.

**Verifikation (Subagent-Driven Development, 3 Implementierungs-Tasks + Reviews, alle auf Sonnet — kein kleines Modell für diese Auth-Arbeit, per Nutzer-Anweisung):** jede der drei Backend-/Frontend-Teiländerungen wurde von einem eigenen Implementer-Subagent umgesetzt und von einem eigenen Review-Subagent (Spec-Compliance + Code-Qualität) geprüft; ein Important-Fund (s.o., sell.marketing-Lücke) durchlief einen Fix-Runde + Scoped-Re-Review, danach sauber. Kein Critical-Fund in allen drei Tasks.

Tests: Frontend-Typecheck (`bun run typecheck`) grün. Server-Typecheck (`bun run typecheck:server`) grün. `bun test src/api/ebay.test.ts`: 18 pass, 0 fail. Voller `bun test`: 160 pass, 2 fail, 2 errors — dieselben 2 vorbestehenden, unabhängigen `/tmp/aliexpress-test.db`-Sandbox-Fehler (Windows-Pfadproblem, seit 2026-09-13 dokumentiert; per Vergleichslauf gegen unveränderten `origin/main` in einem Wegwerf-Worktree nachgewiesen identisch — nicht durch diese Änderung verursacht).
Keine Änderung an `AUTO_PRICE_WRITE_ENABLED`/`ALIEXPRESS_TRACKING_SYNC_ENABLED`/Preislogik (per `git diff origin/main` bestätigt: leer für `pricing.ts`/`price-monitor.ts`). Kein Angebot beworben, keine Kampagne angelegt, kein Gebot geändert (unverändert, diese Codepfade nicht berührt). Keine DB-Migration (bestehende `app_settings`-Tabelle um zwei neue Keys erweitert).
**Einschränkung, ausdrücklich benannt (Grundgesetz Regel 1):** `api.ebay.com` ist aus dieser Sandbox nicht erreichbar — der komplette OAuth-Redirect-/Exchange-Flow konnte nicht live durchgespielt werden. Die DB-Speicherlogik wurde stattdessen echt gegen eine lokale Test-DB verifiziert (s.o.), ein visueller Browser-Screenshot der Einstellungen-Seite war zusätzlich durch eine Sandbox-Policy blockiert (Dev-Server-Backgrounding verweigert) — stattdessen ein echter `curl`-Lauf gegen den echten `/api/ebay/status`-Handler (Zweig "nicht verbunden" bestätigt) plus Code-Verifikation der beiden übrigen Zweige. Der Nutzer sollte den Klick-Flow einmal live nach dem Deploy durchspielen (Anleitung in der PR).
Branch: `feat/ebay-reconnect-scope-anzeige` (isoliertes Worktree unter `.worktrees/`, Basis `origin/main`). PR: https://github.com/contact-e-stele/stele-e-transfer/pull/108 (Draft, nicht gemergt).

**Nachtrag (Whole-Branch-Review, dieselbe Session):** finale Review über alle 7 Commits (Sonnet, kein kleines Modell — per Nutzer-Anweisung für diese Auth-Arbeit): "Ready to merge: Yes". Zwei Important-Funde bestätigt als bereits bestehendes AliExpress-OAuth-Muster, kein neuer Fehler: (1) `saveEbayRefreshToken()` schreibt Token+Scope nicht-transaktional (wie `saveAliTokens()`), (2) keine automatisierte Testabdeckung für die DB-Persistenz selbst (wie bei `saveAliTokens()`/`getAliAccessToken()`). Mit dem Nutzer abgestimmt: jetzt nicht blockieren, in der PR-Beschreibung als Folge-Punkte dokumentiert.

## 2026-09-16 - Claude Sonnet 5
**Claim:** Bug-Auftrag "Knopf 'Erneut an eBay übermitteln' ist nicht angeschlossen" (Live-Fund Bestellung Morgenstern, 26-15134-85187: Sendungsnummer 00340434886290048874 in der App, bei eBay weiterhin "+ Sendungsnummer hinzufügen"; Knopf löst laut Nutzer-Messung keinen `fetch`-Aufruf aus). Branch `worktree-fix-ebay-retry-button` (isoliertes Worktree, Basis `origin/main` — der lokale `fix/p69-p74-versand-rundung`-Stand war 224 Commits hinter `origin/main` und hatte unabhängige, unfertige Arbeit im Working Directory, daher unangetastet gelassen). Draft-PR, kein Merge. Strikte Grenzen: `AUTO_PRICE_WRITE_ENABLED`/`ALIEXPRESS_TRACKING_SYNC_ENABLED`/Preislogik unangetastet, keine DB-Migration, keine Kundennachricht aus diesem PR.

**Aufgabe 1+2 (Root Cause, per Code-Lesen belegt, nicht vermutet):** `packages/web/src/web/pages/bestellungen.tsx:257-261` (vor dem Fix), Funktion `handleRetryEbaySubmit`:
```
const trackingNumber = order?.localNote?.trackingNumber;
const carrier = order?.localNote?.carrier;
if (!trackingNumber || !carrier) return;
```
Fehlte `order.localNote.carrier` (null), brach die Funktion HIER still ab — kein `saveOrderNote()`-Aufruf, also kein `fetch`, kein Toast, kein Log. Exakt das vom Nutzer gemessene Verhalten (onClick feuert, aber nichts erreicht den Server). Die serverseitige Route existiert sehr wohl und ist korrekt verdrahtet: `PATCH /api/order-notes/:ebayOrderId` (`packages/web/src/api/index.ts:952`) ruft `createShippingFulfillment()` (`packages/web/src/api/ebay.ts:2288`) auf, aber NUR wenn `body.trackingNumber` UND `body.carrier` GEMEINSAM im PATCH-Body ankommen (`index.ts:1010`) — der im Auftrag geratene Pfad `/api/orders/.../retry-tracking` existiert nicht und wurde auch nie aufgerufen; der echte, funktionierende Endpunkt ist der bereits genannte `PATCH /api/order-notes/:ebayOrderId`.

**Aufgabe 5 (Unterschied Karakoca vs. Morgenstern, per Code-Lesen belegt):** Karakoca (20-15127-76586) bekam Sendungsnummer UND Carrier gemeinsam über die manuelle Eingabe im Bestellungen-Tab (`handleTrackingSave`, ruft immer `{trackingNumber, carrier}` gemeinsam) — das hat `createShippingFulfillment()` sofort ausgelöst. Morgenstern bekam die Sendungsnummer stattdessen über den P2-Sync-Cron (`packages/web/src/api/tracking-sync.ts`, `ALIEXPRESS_TRACKING_SYNC_ENABLED=true` seit PR #105) automatisch eingetragen — `writeTrackingNumberToDb()` (tracking-sync.ts:128) schreibt laut explizitem Kommentar dort BEWUSST NIEMALS `carrier` (Abgrenzung zur strikten Vorgabe "kein automatischer eBay-Versandmarkierung" aus dem P2-Auftrag). Dadurch stand die Sendungsnummer in der App, aber `carrier` blieb NULL — genau der Zustand, den der defekte Retry-Knopf nicht behandeln konnte. Dieser Cron-Pfad selbst ist wie beauftragt NICHT geändert worden (strikte Grenze eingehalten).

**Aufgabe 3+4 (Fix):** `handleRetryEbaySubmit` öffnet jetzt bei fehlendem `carrier` (oder fehlender `trackingNumber`) statt eines stillen Abbruchs die bestehende Sendungsnummer-Eingabe (vorausgefüllt) und zeigt einen erklärenden Toast ("⚠️ Carrier fehlt — bitte unten Sendungsnummer + Carrier eintragen, dann wird automatisch an eBay übermittelt"). Sind beide Felder vorhanden, ruft die Funktion wie zuvor `saveOrderNote()` auf und zeigt Erfolg/Fehler im Klartext (Fehlermeldung von eBay wörtlich, aus `data.ebay.error`, unverändert vorhandenes Verhalten). Kein stillschweigend nichts tuender Knopf mehr in beiden Fällen.

**Verifikation (Pflichtbestandteil, Aufgabe 6):** Nach dem Deploy: Bestellungen-Tab öffnen, bei einer Bestellung mit der amber-farbenen Warnung "eBay-Übermittlung nie verifiziert" auf "Erneut an eBay übermitteln" klicken. Fehlt der Carrier (wie bei Morgenstern), öffnet sich jetzt die Sendungsnummer-Eingabe mit Toast-Hinweis — Carrier auswählen und speichern. Im Render-Log (Render Dashboard → Logs) muss danach die Zeile erscheinen:
```
[eBay] createShippingFulfillment <ebayOrderId> (<CARRIER_CODE> / <trackingNumber>): <HTTP-Status> <eBay-Response-Text>
```
(Quelle: `packages/web/src/api/ebay.ts`, `createShippingFulfillment()`, unverändert). Status 200/204 = eBay hat die Sendungsnummer angenommen (bei eBay Seller Hub prüfen, ob "+ Sendungsnummer hinzufügen" verschwindet). Bei Bestellung Morgenstern (26-15134-85187) konkret: da die Sandbox keinen Zugriff auf `api.ebay.com`/die Produktions-DB hat, ist dieser Live-Test dem Nutzer vorbehalten.

**Tests:** Frontend-Typecheck (`bun run typecheck`): grün, keine Ausgabe. Server-Typecheck (`bun run typecheck:server`): grün, keine Ausgabe. Voller `bun test`: 160 pass, 2 fail, 2 errors — VORBESTEHEND (Baseline vor dieser Änderung identisch gemessen, s.u.), Ursache `ConnectionFailed("Unable to open connection to local database /tmp/aliexpress-test.db: 14")`, ein Windows-Pfadproblem der lokalen Test-Fixtures, unabhängig von diesem Fix (betroffene Dateien `price-monitor.test.ts`/`variant-sales.test.ts`/zwei Fälle in `tracking-sync.test.ts`, keine davon in diesem PR geändert). Kein neuer Test geschrieben: die Guard-Logik ist eng an React-Component-State (`setEditingTracking`/`showToast`) gekoppelt, und dieses Repo hat für UI-Komponenten keine bestehende Testinfrastruktur (`@testing-library/react` nicht installiert, alle 10 bestehenden `*.test.ts`-Dateien testen ausschließlich reine Backend-/API-Funktionen) — Verifikation stattdessen durch Code-Nachweis (s.o.) + Live-Test-Anleitung für den Nutzer.
Keine Änderung an `AUTO_PRICE_WRITE_ENABLED`/`ALIEXPRESS_TRACKING_SYNC_ENABLED`/Preislogik (per `git diff origin/main` bestätigt: nur `bestellungen.tsx` geändert). Keine DB-Migration. Keine Kundennachricht aus diesem PR versendet (der bestehende, unveränderte Thank-You-Entwurf in `bestellungen.tsx` bleibt reiner Kopier-Text wie zuvor, s. P-86-Kommentar dort).
Branch: `worktree-fix-ebay-retry-button` (isoliertes Worktree, Basis `origin/main`). PR: https://github.com/contact-e-stele/stele-e-transfer/pull/109 (Draft, nicht gemergt).

## 2026-09-16 - Claude Sonnet 5

**Claim:** IMPORT-GATE: manuelle Uebersteuerung + Klartext-Hinweis (PRIO 1). Live-Fund: Import von
https://de.aliexpress.com/item/1005009603522097.html (Katzen-Futterlabyrinth) wird beim Speichern
blockiert ("Toy" im Titel → Spielzeug erkannt, kein Verkaeufername erkannt → Nutzer kann Lieferant
nicht freigeben, sitzt fest). Branch `feat/p66-import-gate-uebersteuerung` (isoliertes Worktree,
Basis `origin/main`, neu von main statt vom aktuellen `fix/p69-p74-versand-rundung`-Arbeits-
verzeichnis, da dort unzusammenhaengende uncommitted Aenderungen lagen — Nutzer bestaetigte "neuer
Branch von main"). Aufgabe: Button "Manuell uebersteuern und speichern" unter der Blockier-
Meldung, Bestaetigungsdialog (Klartext-Grund inkl. erkanntem Verkaeufernamen, Pflicht-Dropdown mit
4 Begruendungen, "Verstanden, trotzdem speichern"), Entscheidung additiv in der DB speichern
(uebersteuert ja/nein, Zeitpunkt, Begruendung, Stichwort), Sichtbarkeit im Produkte-Tab (Badge +
Tooltip), Blockier-Meldung um Stichwort/Feld praezisieren, PR mit Nachstellung + Typecheck/Test-
Ausgabe. Erkennung selbst nicht abschwaechen, `AUTO_PRICE_WRITE_ENABLED`/
`ALIEXPRESS_TRACKING_SYNC_ENABLED`/Preislogik nicht anfassen, Migration additiv, Draft-PR.

**Vorgehen:** superpowers-Skills verwendet — brainstorming (Design-Dokument, User-Ergaenzungen:
Verkaeufername im Dialog + Server-Log-Zeile), writing-plans (6-Task-Plan), executing-plans (inline,
Task fuer Task mit Typecheck-Verifikation je Task).

**Ergebnis:** Fertig. (1) `shared/regulated-categories.ts`:
`matchRegulatedCategoriesDetailed()` liefert pro Treffer Kategorie+Stichwort+Feld (Titel vor
Beschreibung geprueft); bestehende `matchRegulatedCategories()` unveraendert. Neue
`COMPLIANCE_OVERRIDE_REASONS`/`complianceOverrideReasonLabel()` als einzige Quelle fuer die 4
Begruendungen (Dropdown + Badge-Tooltip, keine Duplizierung). (2) `db/schema.ts`/`db/migrate.ts`:
7 neue additive Spalten auf `products` (`compliance_override`, `_at`, `_reason`,
`_reason_text`, `_category`, `_keyword`, `_field`). (3) `api/index.ts`
(`POST /products`): nimmt die Override-Felder optional entgegen, schreibt sie nur bei
`complianceOverride: true`, plus Server-Log-Zeile `[Compliance-Override] SKU=... Kategorie=...
Stichwort="..." Feld=... Grund=...` (SKU = `aliexpressItemId`, nicht `asin`, da `asin` nur eine
synthetische `ali_<timestamp>`-ID ist). (4) `lieferanten.tsx`: Klartext-Meldung zeigt jetzt
Stichwort+Feld je Treffer; neuer Button "Manuell uebersteuern und speichern" (nur bei
`complianceBlocked`) oeffnet Bestaetigungsdialog (zeigt denselben Klartext-Grund + erkannten
Verkaeufernamen inkl. "(kein Name erkannt)"-Fall, Pflicht-Dropdown, Freitext bei "Sonstiges");
`handleSave()` nimmt jetzt optional ein Override-Objekt entgegen und umgeht die bestehende
`if (complianceBlocked) return`-Sperre NUR dann. (5) `produkte.tsx`: Badge "manuell freigegeben"
(Muster wie bestehendes GPSR-Badge) mit nativem Tooltip (Begruendung, Stichwort/Feld/Kategorie,
Zeitpunkt).

**Live-Verifikation (mit expliziter Nutzerfreigabe je Schritt):** Migration lief real gegen die
Produktions-Turso-DB (`bun --env-file=../../.env src/server.ts`, lokal kopiertes, gitignored `.env`
mit `TURSO_DATABASE_URL`/`TURSO_AUTH_TOKEN`) — alle 7 neuen Spalten erfolgreich angelegt
(`[migrate] ✓ ALTER TABLE products ADD COLUMN compliance_override...` usw., Rest korrekt als
"Skip (already exists)"). Voller Browser-UI-Test (Dialog/Speichern/Badge) NICHT durchgefuehrt:
lokales `.env` hat kein `SESSION_SECRET`/`AUTH_USER1_*` (Render-only) → `authMiddleware`
(`api/auth.ts:79-81`) lehnt jeden API-Call inkl. Login mit 500 ab, Login lokal technisch nicht
moeglich. Nutzer entschied nach Rueckfrage explizit gegen das Nachreichen der Secrets — Live-UI-
Nachstellung bleibt fuer den Nutzer selbst (lokal oder nach Deploy).

**Tests:** Frontend-Typecheck (`bun run typecheck`) gruen, Server-Typecheck
(`bun run typecheck:server`) gruen — je Task einzeln verifiziert. Neue Tests
`regulated-categories.test.ts`: 8 pass, 0 fail, 16 expect() (inkl. Katzen-Futterlabyrinth-Fixture:
"Toy" im Titel → Spielzeug, Feld "title"). Voller `bun test`: 168 pass, 2 fail, 2 errors — die 2
Fehlschlaege (`price-monitor.test.ts`, `variant-sales.test.ts`) sind VORBESTEHEND, nicht Teil
dieses Diffs (per `git diff origin/main --stat` bestaetigt), Ursache ein Windows-`/tmp`-Pfadproblem
des lokalen libsql-Test-Fixtures (`ConnectionFailed(...: 14)`), unabhaengig von dieser Aenderung.

**Strikte Grenzen nachgewiesen:** `git diff origin/main -- packages/web/src/api/price-monitor.ts
packages/web/src/shared/pricing.ts` leer, `git diff origin/main -- packages/web/src/shared/
constants.ts packages/web/src/api/tracking-sync.ts` leer — `AUTO_PRICE_WRITE_ENABLED`,
`ALIEXPRESS_TRACKING_SYNC_ENABLED`, Preislogik unangetastet. Migration rein additiv (7x
`ALTER TABLE ... ADD COLUMN`, kein `DROP`, keine bestehende Spalte veraendert).

**Nebenbefund (waehrend Vorbereitung des Worktrees, vor Codeaenderung):** `node_modules` fehlte im
neuen Worktree (git worktrees teilen keine `node_modules`) — `bun install` (1545 Pakete, 231s) vor
dem ersten Typecheck ausgefuehrt, sonst waeren alle Typecheck-Laeufe faelschlich mit "Cannot find
module" fehlgeschlagen. Ebenso `.env` (gitignored) manuell aus dem Hauptverzeichnis in den
Worktree kopiert, da git worktrees keine ignorierten Dateien teilen.

Branch: `feat/p66-import-gate-uebersteuerung` (isoliertes Worktree unter `.worktrees/`, Basis
`origin/main`). PR: https://github.com/contact-e-stele/stele-e-transfer/pull/110 (Draft, nicht
gemergt).
